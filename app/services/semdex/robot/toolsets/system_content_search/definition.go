package system_content_search

const (
	ID          = "system.content_search"
	Name        = "Content search"
	Description = "Search published Storyden content and community members using broad or domain-specific discovery tools."
	Instruction = `Use the narrowest search tool that matches the request: broad content search for mixed results, thread or reply search for forum-specific work, post search across both, and member search for people. Search results are discovery evidence, not a substitute for full resource details. When a consequential answer or mutation depends on one result, use an available read tool to inspect it before proceeding. State when no relevant result was found instead of guessing.`
)
