import { css } from "@/styled-system/css";

export const navbarStyles = css({
  bgColor: "whiteAlpha.700",
  backdropBlur: "lg",
  backdropFilter: "auto",
  boxShadow: "sm",
  borderRadius: "lg",
});
