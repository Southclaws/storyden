import { SmileIcon, SmilePlusIcon } from "lucide-react";

import { styled } from "@/styled-system/jsx";

export const ReactionIcon = styled(SmileIcon);
export const ReactionAddIcon = styled(SmilePlusIcon);
