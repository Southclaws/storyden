import { Heart } from "lucide-react";

import { styled } from "@/styled-system/jsx";

export const LikeSavedIcon = styled(Heart, {
  base: { fill: "current" },
});
export const LikeIcon = styled(Heart);
