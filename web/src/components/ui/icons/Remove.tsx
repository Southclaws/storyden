import { MinusCircle, MinusIcon } from "lucide-react";

import { styled } from "@/styled-system/jsx";

export const RemoveIcon = styled(MinusIcon);

export const RemoveCircleIcon = styled(MinusCircle);
