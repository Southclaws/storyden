/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * storyden
 * Storyden social API for building community driven platforms.
 * The Storyden API does not adhere to semantic versioning but instead applies a rolling strategy with deprecations and minimal breaking changes. This has been done mainly for a simpler development process and it may be changed to a more fixed versioning strategy in the future. Ultimately, the primary way Storyden tracks versions is dates, there are no set release tags currently.
 *
 * OpenAPI spec version: v1.26.15-post
 */
import type {
  DatagraphMatchesOKResponse,
  DatagraphMatchesParams,
  DatagraphSearchOKResponse,
  DatagraphSearchParams,
  InternalServerErrorResponse,
  NotFoundResponse,
  UnauthorisedResponse,
} from "../openapi-schema";
import { fetcher } from "../server";

export type HTTPStatusCode1xx = 100 | 101 | 102 | 103;
export type HTTPStatusCode2xx = 200 | 201 | 202 | 203 | 204 | 205 | 206 | 207;
export type HTTPStatusCode3xx = 300 | 301 | 302 | 303 | 304 | 305 | 307 | 308;
export type HTTPStatusCode4xx =
  | 400
  | 401
  | 402
  | 403
  | 404
  | 405
  | 406
  | 407
  | 408
  | 409
  | 410
  | 411
  | 412
  | 413
  | 414
  | 415
  | 416
  | 417
  | 418
  | 419
  | 420
  | 421
  | 422
  | 423
  | 424
  | 426
  | 428
  | 429
  | 431
  | 451;
export type HTTPStatusCode5xx = 500 | 501 | 502 | 503 | 504 | 505 | 507 | 511;
export type HTTPStatusCodes =
  | HTTPStatusCode1xx
  | HTTPStatusCode2xx
  | HTTPStatusCode3xx
  | HTTPStatusCode4xx
  | HTTPStatusCode5xx;

export type datagraphSearchResponse200 = {
  data: DatagraphSearchOKResponse;
  status: 200;
};

export type datagraphSearchResponse401 = {
  data: UnauthorisedResponse;
  status: 401;
};

export type datagraphSearchResponse404 = {
  data: NotFoundResponse;
  status: 404;
};

export type datagraphSearchResponseDefault = {
  data: InternalServerErrorResponse;
  status: Exclude<HTTPStatusCodes, 200 | 401 | 404>;
};

export type datagraphSearchResponseSuccess = datagraphSearchResponse200 & {
  headers: Headers;
};
export type datagraphSearchResponseError = (
  | datagraphSearchResponse401
  | datagraphSearchResponse404
  | datagraphSearchResponseDefault
) & {
  headers: Headers;
};

export const getDatagraphSearchUrl = (params?: DatagraphSearchParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    const explodeParameters = ["kind", "authors", "categories", "tags"];

    if (Array.isArray(value) && explodeParameters.includes(key)) {
      value.forEach((v) => {
        normalizedParams.append(key, v === null ? "null" : String(v));
      });
      return;
    }

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/datagraph?${stringifiedParams}`
    : `/datagraph`;
};

/**
 * Query and search content.
 */
export const datagraphSearch = async (
  params?: DatagraphSearchParams,
  options?: Parameters<typeof fetcher>[1],
): Promise<datagraphSearchResponseSuccess> => {
  return fetcher<datagraphSearchResponseSuccess>(
    getDatagraphSearchUrl(params),
    {
      ...options,
      method: "GET",
    },
  );
};

export type datagraphMatchesResponse200 = {
  data: DatagraphMatchesOKResponse;
  status: 200;
};

export type datagraphMatchesResponse401 = {
  data: UnauthorisedResponse;
  status: 401;
};

export type datagraphMatchesResponse404 = {
  data: NotFoundResponse;
  status: 404;
};

export type datagraphMatchesResponseDefault = {
  data: InternalServerErrorResponse;
  status: Exclude<HTTPStatusCodes, 200 | 401 | 404>;
};

export type datagraphMatchesResponseSuccess = datagraphMatchesResponse200 & {
  headers: Headers;
};
export type datagraphMatchesResponseError = (
  | datagraphMatchesResponse401
  | datagraphMatchesResponse404
  | datagraphMatchesResponseDefault
) & {
  headers: Headers;
};

export const getDatagraphMatchesUrl = (params: DatagraphMatchesParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    const explodeParameters = ["kind"];

    if (Array.isArray(value) && explodeParameters.includes(key)) {
      value.forEach((v) => {
        normalizedParams.append(key, v === null ? "null" : String(v));
      });
      return;
    }

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/datagraph/matches?${stringifiedParams}`
    : `/datagraph/matches`;
};

/**
 * Query the datagraph optimised for typeahead scenarios. This endpoint is
 * only active when a `SEARCH_PROVIDER` that supports fast access is used.
 * This includes providers such as Bleve and Redis.
 *
 * This endpoint will return a minified set of results directly from the
 * configured search index, without hitting the database. This makes it
 * suitable for performance sensitive use-cases such as type-ahead search,
 * @ mentioning threads/pages, CTRL+K style menus, and more.
 *
 * Results will include a `kind` field and short content, but will not
 * contain graph edges (such as authorship, links, etc.) due to constraints
 * of the underlying search index and to keep payload sizes smaller.
 */
export const datagraphMatches = async (
  params: DatagraphMatchesParams,
  options?: Parameters<typeof fetcher>[1],
): Promise<datagraphMatchesResponseSuccess> => {
  return fetcher<datagraphMatchesResponseSuccess>(
    getDatagraphMatchesUrl(params),
    {
      ...options,
      method: "GET",
    },
  );
};
