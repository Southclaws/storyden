/**
 * Generated by orval v7.2.0 🍺
 * Do not edit manually.
 * storyden
 * Storyden social API for building community driven platforms.
The Storyden API does not adhere to semantic versioning but instead applies a rolling strategy with deprecations and minimal breaking changes. This has been done mainly for a simpler development process and it may be changed to a more fixed versioning strategy in the future. Ultimately, the primary way Storyden tracks versions is dates, there are no set release tags currently.

 * OpenAPI spec version: v1.26.12-post
 */
import type { Identifier } from "./identifier";

export interface RobotMCPServerInitialProps {
  /** Write-only Bearer token for the MCP server. */
  bearer_token?: string;
  description?: string;
  enabled?: boolean;
  endpoint_url: string;
  name: string;
  /** Optional remote OAuth connection that supplies the Bearer token for
this MCP server after authorization completes.
 */
  oauth_remote_connection_id?: Identifier;
  /** Optional immutable namespace. If omitted, Storyden derives one from the name. */
  slug?: string;
}
