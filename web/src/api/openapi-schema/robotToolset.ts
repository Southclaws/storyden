/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * storyden
 * Storyden social API for building community driven platforms.
 * The Storyden API does not adhere to semantic versioning but instead applies a rolling strategy with deprecations and minimal breaking changes. This has been done mainly for a simpler development process and it may be changed to a more fixed versioning strategy in the future. Ultimately, the primary way Storyden tracks versions is dates, there are no set release tags currently.
 *
 * OpenAPI spec version: v1.26.15-post
 */
import type { Metadata } from "./metadata";
import type { ProfileReference } from "./profileReference";
import type { RobotToolNameList } from "./robotToolNameList";
import type { RobotToolsetSource } from "./robotToolsetSource";

export interface RobotToolset {
  id: string;
  name: string;
  description: string;
  /** Specialist instructions injected whenever the Toolset is active. */
  instruction: string;
  tools: RobotToolNameList;
  source: RobotToolsetSource;
  /** Optional provider-specific source identifier. */
  source_ref?: string;
  editable: boolean;
  /** @minimum 0 */
  usage_count: number;
  /** Whether this Toolset is only available to Robot sessions with an active workspace. */
  requires_workspace: boolean;
  author?: ProfileReference;
  meta?: Metadata;
}
