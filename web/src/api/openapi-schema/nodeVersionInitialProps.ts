/**
 * Generated by orval v7.2.0 🍺
 * Do not edit manually.
 * storyden
 * Storyden social API for building community driven platforms.
The Storyden API does not adhere to semantic versioning but instead applies a rolling strategy with deprecations and minimal breaking changes. This has been done mainly for a simpler development process and it may be changed to a more fixed versioning strategy in the future. Ultimately, the primary way Storyden tracks versions is dates, there are no set release tags currently.

 * OpenAPI spec version: v1.26.11-post
 */
import type { Metadata } from "./metadata";
import type { NodeName } from "./nodeName";
import type { NodeVersionInitialPropsContent } from "./nodeVersionInitialPropsContent";
import type { NodeVersionInitialPropsDescription } from "./nodeVersionInitialPropsDescription";
import type { PropertyMutationList } from "./propertyMutationList";
import type { Slug } from "./slug";

/**
 * Initial overlay for a draft checkpoint. Omitted fields keep the target
node's current value in the new draft snapshot. Nullable fields can be
set to null to clear them. Properties are replace-all and are not merged
with existing values when the version is applied.

 */
export interface NodeVersionInitialProps {
  /** @nullable */
  content?: NodeVersionInitialPropsContent;
  /** @nullable */
  description?: NodeVersionInitialPropsDescription;
  meta?: Metadata;
  name?: NodeName;
  properties?: PropertyMutationList;
  slug?: Slug;
}
