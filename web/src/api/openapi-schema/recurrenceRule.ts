/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * storyden
 * Storyden social API for building community driven platforms.
 * The Storyden API does not adhere to semantic versioning but instead applies a rolling strategy with deprecations and minimal breaking changes. This has been done mainly for a simpler development process and it may be changed to a more fixed versioning strategy in the future. Ultimately, the primary way Storyden tracks versions is dates, there are no set release tags currently.
 *
 * OpenAPI spec version: v1.26.15-post
 */
import type { RecurrenceFrequency } from "./recurrenceFrequency";
import type { RecurrenceMonthDayList } from "./recurrenceMonthDayList";
import type { RecurrenceMonthList } from "./recurrenceMonthList";
import type { RecurrenceWeekdayList } from "./recurrenceWeekdayList";

/**
 * A normalized AST for the supported RFC 5545 recurrence rule subset.
 * The start value supplies date and time parts that the rule omits.
 * Selectors do not need to match the start date. A count limits the
 * occurrences generated on or after the start anchor. A negative month
 * day counts back from the end of the month, so -1 is the last day.
 * Weeks start on Monday. Weekly rules support by_weekday. Monthly rules
 * support by_month and by_month_day. Yearly rules support by_month and
 * by_month_day. Hourly and daily rules do not support selectors.
 */
export interface RecurrenceRule {
  frequency: RecurrenceFrequency;
  /** @minimum 1 */
  interval: number;
  by_weekday?: RecurrenceWeekdayList;
  by_month?: RecurrenceMonthList;
  by_month_day?: RecurrenceMonthDayList;
  /**
   * @minimum 1
   * @maximum 100000
   */
  count?: number;
}
