/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * storyden
 * Storyden social API for building community driven platforms.
 * The Storyden API does not adhere to semantic versioning but instead applies a rolling strategy with deprecations and minimal breaking changes. This has been done mainly for a simpler development process and it may be changed to a more fixed versioning strategy in the future. Ultimately, the primary way Storyden tracks versions is dates, there are no set release tags currently.
 *
 * OpenAPI spec version: v1.26.15-post
 */
import type { CommonProperties } from "./commonProperties";
import type { Identifier } from "./identifier";
import type { RobotMCPToolList } from "./robotMCPToolList";

export type RobotMCPServer = CommonProperties & {
  name: string;
  /** Immutable namespace used in MCP tool IDs. */
  slug: string;
  description: string;
  endpoint_url: string;
  oauth_remote_connection_id?: Identifier;
  enabled: boolean;
  /** Stable read-only Toolset ID that assigns all enabled tools from this MCP server to a Robot. */
  toolset_id: string;
  /**
   * Whether this server has a stored Bearer token. The token value
   * is never returned.
   */
  has_bearer_token: boolean;
  /**
   * Whether this server is linked to a remote OAuth connection with
   * a stored access token.
   */
  has_oauth_token: boolean;
  last_refreshed_at?: string;
  last_error?: string;
  tools: RobotMCPToolList;
};
