/**
 * Generated by orval v7.2.0 🍺
 * Do not edit manually.
 * storyden
 * Storyden social API for building community driven platforms.
The Storyden API does not adhere to semantic versioning but instead applies a rolling strategy with deprecations and minimal breaking changes. This has been done mainly for a simpler development process and it may be changed to a more fixed versioning strategy in the future. Ultimately, the primary way Storyden tracks versions is dates, there are no set release tags currently.

 * OpenAPI spec version: v1.26.6-post
 */
import type { ClientIPServiceSettingsClientIpMode } from "./clientIPServiceSettingsClientIpMode";

export interface ClientIPServiceSettings {
  /** Header name used when `client_ip_mode` is `single_header`.
Common values include `CF-Connecting-IP`, `Fly-Client-IP`, or `X-Real-IP`.
 */
  client_ip_header?: string;
  /** Strategy used to derive the client IP address stored in request info context.

- `remote_addr`: use only the request socket remote address.
- `single_header`: trust a single configured header.
- `xff_trusted_proxies`: trust `X-Forwarded-For` only when the
  request comes from a trusted proxy, selecting the first non-proxy
  IP from the right of the list in `X-Forwarded-For`.
 */
  client_ip_mode?: ClientIPServiceSettingsClientIpMode;
  /** Trusted proxy CIDR ranges. `X-Forwarded-For` is only used if
`RemoteAddr` is within these ranges; the first non-proxy IP from the
right is selected as the client.
 */
  trusted_proxy_cidrs?: string[];
}
