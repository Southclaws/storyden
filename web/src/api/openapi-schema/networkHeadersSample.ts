/**
 * Generated by orval v7.2.0 🍺
 * Do not edit manually.
 * storyden
 * Storyden social API for building community driven platforms.
The Storyden API does not adhere to semantic versioning but instead applies a rolling strategy with deprecations and minimal breaking changes. This has been done mainly for a simpler development process and it may be changed to a more fixed versioning strategy in the future. Ultimately, the primary way Storyden tracks versions is dates, there are no set release tags currently.

 * OpenAPI spec version: v1.26.7
 */
import type { NetworkHeadersSampleHeaders } from "./networkHeadersSampleHeaders";
import type { NetworkHeadersSampleHeadersSsr } from "./networkHeadersSampleHeadersSsr";

/**
 * A sample of network headers received by the backend from the client or
proxy. This is used for configuring the client IP mode.

 */
export interface NetworkHeadersSample {
  /** A sample of the headers directly received by the backend from the
client or proxy if any.
 */
  headers?: NetworkHeadersSampleHeaders;
  /** If the frontend being used supports server-side rendering this field
will be populated on an SSR request showing the headers that the
frontend program passed to the backend via Storyden specific header.
 */
  headers_ssr?: NetworkHeadersSampleHeadersSsr;
  /** The raw client address as seen by the backend, without any
processing or trust-based overrides. This is typically the remote
address of a reverse proxy. Use this to configure trusted proxies.
 */
  raw_client_address?: string;
}
