/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * storyden
 * Storyden social API for building community driven platforms.
 * The Storyden API does not adhere to semantic versioning but instead applies a rolling strategy with deprecations and minimal breaking changes. This has been done mainly for a simpler development process and it may be changed to a more fixed versioning strategy in the future. Ultimately, the primary way Storyden tracks versions is dates, there are no set release tags currently.
 *
 * OpenAPI spec version: v1.26.15-post
 */
import useSwr from "swr";
import type { Arguments, Key, SWRConfiguration } from "swr";
import useSWRMutation from "swr/mutation";
import type { SWRMutationConfiguration } from "swr/mutation";

import { fetcher } from "../client";
import type {
  InternalServerErrorResponse,
  NotFoundResponse,
  PostLocationGetOKResponse,
  PostLocationGetParams,
  PostReactAddBody,
  PostReactAddOKResponse,
  PostUpdateBody,
  PostUpdateOKResponse,
  UnauthorisedResponse,
} from "../openapi-schema";

type SecondParameter<T extends (...args: never) => unknown> = Parameters<T>[1];

export const getPostUpdateUrl = (postId: string) => {
  return `/posts/${postId}`;
};

/**
 * Publish changes to a single post.
 */
export const postUpdate = async (
  postId: string,
  postUpdateBody?: PostUpdateBody,
  options?: Parameters<typeof fetcher>[1],
): Promise<PostUpdateOKResponse> => {
  return fetcher<PostUpdateOKResponse>(getPostUpdateUrl(postId), {
    ...options,
    method: "PATCH",
    headers: { "Content-Type": "application/json", ...options?.headers },
    body: JSON.stringify(postUpdateBody),
  });
};

export const getPostUpdateMutationFetcher = (
  postId: string,
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, { arg }: { arg: PostUpdateBody | undefined }) => {
    return postUpdate(postId, arg, options);
  };
};
export const getPostUpdateMutationKey = (postId: string) =>
  [`/posts/${postId}`] as const;

export type PostUpdateMutationResult = NonNullable<
  Awaited<ReturnType<typeof postUpdate>>
>;

export const usePostUpdate = <
  TError =
    UnauthorisedResponse | NotFoundResponse | InternalServerErrorResponse,
>(
  postId: string,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof postUpdate>>,
      TError,
      Key,
      PostUpdateBody | undefined,
      Awaited<ReturnType<typeof postUpdate>>
    > & { swrKey?: string };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey = swrOptions?.swrKey ?? getPostUpdateMutationKey(postId);
  const swrFn = getPostUpdateMutationFetcher(postId, requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getPostDeleteUrl = (postId: string) => {
  return `/posts/${postId}`;
};

/**
 * Archive a post using soft-delete.
 */
export const postDelete = async (
  postId: string,
  options?: Parameters<typeof fetcher>[1],
): Promise<void> => {
  return fetcher<void>(getPostDeleteUrl(postId), {
    ...options,
    method: "DELETE",
  });
};

export const getPostDeleteMutationFetcher = (
  postId: string,
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, __: { arg: Arguments }) => {
    return postDelete(postId, options);
  };
};
export const getPostDeleteMutationKey = (postId: string) =>
  [`/posts/${postId}`] as const;

export type PostDeleteMutationResult = NonNullable<
  Awaited<ReturnType<typeof postDelete>>
>;

export const usePostDelete = <
  TError =
    UnauthorisedResponse | NotFoundResponse | InternalServerErrorResponse,
>(
  postId: string,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof postDelete>>,
      TError,
      Key,
      Arguments,
      Awaited<ReturnType<typeof postDelete>>
    > & { swrKey?: string };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey = swrOptions?.swrKey ?? getPostDeleteMutationKey(postId);
  const swrFn = getPostDeleteMutationFetcher(postId, requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getPostReactAddUrl = (postId: string) => {
  return `/posts/${postId}/reacts`;
};

/**
 * Add a reaction to a post.
 */
export const postReactAdd = async (
  postId: string,
  postReactAddBody?: PostReactAddBody,
  options?: Parameters<typeof fetcher>[1],
): Promise<PostReactAddOKResponse> => {
  return fetcher<PostReactAddOKResponse>(getPostReactAddUrl(postId), {
    ...options,
    method: "PUT",
    headers: { "Content-Type": "application/json", ...options?.headers },
    body: JSON.stringify(postReactAddBody),
  });
};

export const getPostReactAddMutationFetcher = (
  postId: string,
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, { arg }: { arg: PostReactAddBody | undefined }) => {
    return postReactAdd(postId, arg, options);
  };
};
export const getPostReactAddMutationKey = (postId: string) =>
  [`/posts/${postId}/reacts`] as const;

export type PostReactAddMutationResult = NonNullable<
  Awaited<ReturnType<typeof postReactAdd>>
>;

export const usePostReactAdd = <
  TError =
    UnauthorisedResponse | NotFoundResponse | InternalServerErrorResponse,
>(
  postId: string,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof postReactAdd>>,
      TError,
      Key,
      PostReactAddBody | undefined,
      Awaited<ReturnType<typeof postReactAdd>>
    > & { swrKey?: string };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey = swrOptions?.swrKey ?? getPostReactAddMutationKey(postId);
  const swrFn = getPostReactAddMutationFetcher(postId, requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getPostReactRemoveUrl = (postId: string, reactId: string) => {
  return `/posts/${postId}/reacts/${reactId}`;
};

/**
 * Remove a reaction from a post.
 */
export const postReactRemove = async (
  postId: string,
  reactId: string,
  options?: Parameters<typeof fetcher>[1],
): Promise<void> => {
  return fetcher<void>(getPostReactRemoveUrl(postId, reactId), {
    ...options,
    method: "DELETE",
  });
};

export const getPostReactRemoveMutationFetcher = (
  postId: string,
  reactId: string,
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, __: { arg: Arguments }) => {
    return postReactRemove(postId, reactId, options);
  };
};
export const getPostReactRemoveMutationKey = (
  postId: string,
  reactId: string,
) => [`/posts/${postId}/reacts/${reactId}`] as const;

export type PostReactRemoveMutationResult = NonNullable<
  Awaited<ReturnType<typeof postReactRemove>>
>;

export const usePostReactRemove = <
  TError =
    UnauthorisedResponse | NotFoundResponse | InternalServerErrorResponse,
>(
  postId: string,
  reactId: string,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof postReactRemove>>,
      TError,
      Key,
      Arguments,
      Awaited<ReturnType<typeof postReactRemove>>
    > & { swrKey?: string };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey =
    swrOptions?.swrKey ?? getPostReactRemoveMutationKey(postId, reactId);
  const swrFn = getPostReactRemoveMutationFetcher(
    postId,
    reactId,
    requestOptions,
  );

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getPostLocationGetUrl = (params: PostLocationGetParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/posts/location?${stringifiedParams}`
    : `/posts/location`;
};

/**
 * Locate a post just from its ID. This will tell you what kind of post it
 * is and where to find it. Where "a post is" is simple for threads, just
 * the slug. For replies, it will give you the thread slug and the position
 * within the thread: the index, the page and the position on the page.
 */
export const postLocationGet = async (
  params: PostLocationGetParams,
  options?: Parameters<typeof fetcher>[1],
): Promise<PostLocationGetOKResponse> => {
  return fetcher<PostLocationGetOKResponse>(getPostLocationGetUrl(params), {
    ...options,
    method: "GET",
  });
};

export const getPostLocationGetKey = (params: PostLocationGetParams) =>
  [`/posts/location`, ...(params ? [params] : [])] as const;

export type PostLocationGetQueryResult = NonNullable<
  Awaited<ReturnType<typeof postLocationGet>>
>;

export const usePostLocationGet = <
  TError =
    UnauthorisedResponse | NotFoundResponse | InternalServerErrorResponse,
>(
  params: PostLocationGetParams,
  options?: {
    swr?: SWRConfiguration<
      Awaited<ReturnType<typeof postLocationGet>>,
      TError
    > & { swrKey?: Key; enabled?: boolean };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const isEnabled = swrOptions?.enabled !== false;
  const swrKey =
    swrOptions?.swrKey ??
    (() => (isEnabled ? getPostLocationGetKey(params) : null));
  const swrFn = () => postLocationGet(params, requestOptions);

  const query = useSwr<Awaited<ReturnType<typeof swrFn>>, TError>(
    swrKey,
    swrFn,
    swrOptions,
  );

  return {
    swrKey,
    ...query,
  };
};
