/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * storyden
 * Storyden social API for building community driven platforms.
 * The Storyden API does not adhere to semantic versioning but instead applies a rolling strategy with deprecations and minimal breaking changes. This has been done mainly for a simpler development process and it may be changed to a more fixed versioning strategy in the future. Ultimately, the primary way Storyden tracks versions is dates, there are no set release tags currently.
 *
 * OpenAPI spec version: v1.26.15-post
 */
import useSwr from "swr";
import type { Arguments, Key, SWRConfiguration } from "swr";
import useSWRMutation from "swr/mutation";
import type { SWRMutationConfiguration } from "swr/mutation";

import { fetcher } from "../client";
import type {
  BadRequestResponse,
  ConflictResponse,
  InternalServerErrorResponse,
  NodeAddChildOKResponse,
  NodeCreateBody,
  NodeCreateOKResponse,
  NodeDeleteOKResponse,
  NodeDeleteParams,
  NodeDraftListOKResponse,
  NodeDraftListParams,
  NodeGenerateContentBody,
  NodeGenerateContentOKResponse,
  NodeGenerateTagsBody,
  NodeGenerateTagsOKResponse,
  NodeGenerateTitleBody,
  NodeGenerateTitleOKResponse,
  NodeGetOKResponse,
  NodeGetParams,
  NodeListChildrenParams,
  NodeListOKResponse,
  NodeListParams,
  NodeRemoveChildOKResponse,
  NodeUpdateBody,
  NodeUpdateOKResponse,
  NodeUpdatePositionBody,
  NodeUpdatePropertiesBody,
  NodeUpdatePropertiesOKResponse,
  NodeUpdatePropertySchemaBody,
  NodeUpdatePropertySchemaOKResponse,
  NodeVersionCreateBody,
  NodeVersionCreateOKResponse,
  NodeVersionDeleteOKResponse,
  NodeVersionGetOKResponse,
  NodeVersionListOKResponse,
  NodeVersionListParams,
  NodeVersionUpdateBody,
  NodeVersionUpdateOKResponse,
  NodeVersionUpdateStatusBody,
  NotFoundResponse,
  NotImplementedResponse,
  NotModifiedResponse,
  UnauthorisedResponse,
  VisibilityUpdateBody,
} from "../openapi-schema";

type SecondParameter<T extends (...args: never) => unknown> = Parameters<T>[1];

export const getNodeCreateUrl = () => {
  return `/nodes`;
};

/**
 * Create a node for curating structured knowledge together.
 */
export const nodeCreate = async (
  nodeCreateBody?: NodeCreateBody,
  options?: Parameters<typeof fetcher>[1],
): Promise<NodeCreateOKResponse> => {
  return fetcher<NodeCreateOKResponse>(getNodeCreateUrl(), {
    ...options,
    method: "POST",
    headers: { "Content-Type": "application/json", ...options?.headers },
    body: JSON.stringify(nodeCreateBody),
  });
};

export const getNodeCreateMutationFetcher = (
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, { arg }: { arg: NodeCreateBody | undefined }) => {
    return nodeCreate(arg, options);
  };
};
export const getNodeCreateMutationKey = () => [`/nodes`] as const;

export type NodeCreateMutationResult = NonNullable<
  Awaited<ReturnType<typeof nodeCreate>>
>;

export const useNodeCreate = <
  TError = UnauthorisedResponse | InternalServerErrorResponse,
>(options?: {
  swr?: SWRMutationConfiguration<
    Awaited<ReturnType<typeof nodeCreate>>,
    TError,
    Key,
    NodeCreateBody | undefined,
    Awaited<ReturnType<typeof nodeCreate>>
  > & { swrKey?: string };
  request?: SecondParameter<typeof fetcher>;
}) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey = swrOptions?.swrKey ?? getNodeCreateMutationKey();
  const swrFn = getNodeCreateMutationFetcher(requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getNodeListUrl = (params?: NodeListParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    const explodeParameters = ["visibility"];

    if (Array.isArray(value) && explodeParameters.includes(key)) {
      value.forEach((v) => {
        normalizedParams.append(key, v === null ? "null" : String(v));
      });
      return;
    }

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/nodes?${stringifiedParams}`
    : `/nodes`;
};

/**
 * List nodes using the given filters. Can be used to get a full tree.
 */
export const nodeList = async (
  params?: NodeListParams,
  options?: Parameters<typeof fetcher>[1],
): Promise<NodeListOKResponse> => {
  return fetcher<NodeListOKResponse>(getNodeListUrl(params), {
    ...options,
    method: "GET",
  });
};

export const getNodeListKey = (params?: NodeListParams) =>
  [`/nodes`, ...(params ? [params] : [])] as const;

export type NodeListQueryResult = NonNullable<
  Awaited<ReturnType<typeof nodeList>>
>;

export const useNodeList = <
  TError = NotFoundResponse | InternalServerErrorResponse,
>(
  params?: NodeListParams,
  options?: {
    swr?: SWRConfiguration<Awaited<ReturnType<typeof nodeList>>, TError> & {
      swrKey?: Key;
      enabled?: boolean;
    };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const isEnabled = swrOptions?.enabled !== false;
  const swrKey =
    swrOptions?.swrKey ?? (() => (isEnabled ? getNodeListKey(params) : null));
  const swrFn = () => nodeList(params, requestOptions);

  const query = useSwr<Awaited<ReturnType<typeof swrFn>>, TError>(
    swrKey,
    swrFn,
    swrOptions,
  );

  return {
    swrKey,
    ...query,
  };
};
export const getNodeDraftListUrl = (params?: NodeDraftListParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/nodes/drafts?${stringifiedParams}`
    : `/nodes/drafts`;
};

/**
 * List all draft versions across all nodes visible to the caller.
 *
 * This endpoint is designed for moderation and queue screens where you need
 * to see all pending draft proposals in one request. Each draft includes a
 * reference to its target node for context.
 *
 * Drafts are visible based on the caller's permissions:
 * - Draft authors can see their own drafts
 * - Members with `MANAGE_LIBRARY` can see all drafts
 * - Unauthenticated requests receive 401 Unauthorized
 *
 * Results are ordered by `updated_at` descending so recently updated drafts
 * appear first.
 */
export const nodeDraftList = async (
  params?: NodeDraftListParams,
  options?: Parameters<typeof fetcher>[1],
): Promise<NodeDraftListOKResponse> => {
  return fetcher<NodeDraftListOKResponse>(getNodeDraftListUrl(params), {
    ...options,
    method: "GET",
  });
};

export const getNodeDraftListKey = (params?: NodeDraftListParams) =>
  [`/nodes/drafts`, ...(params ? [params] : [])] as const;

export type NodeDraftListQueryResult = NonNullable<
  Awaited<ReturnType<typeof nodeDraftList>>
>;

export const useNodeDraftList = <
  TError = UnauthorisedResponse | InternalServerErrorResponse,
>(
  params?: NodeDraftListParams,
  options?: {
    swr?: SWRConfiguration<
      Awaited<ReturnType<typeof nodeDraftList>>,
      TError
    > & { swrKey?: Key; enabled?: boolean };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const isEnabled = swrOptions?.enabled !== false;
  const swrKey =
    swrOptions?.swrKey ??
    (() => (isEnabled ? getNodeDraftListKey(params) : null));
  const swrFn = () => nodeDraftList(params, requestOptions);

  const query = useSwr<Awaited<ReturnType<typeof swrFn>>, TError>(
    swrKey,
    swrFn,
    swrOptions,
  );

  return {
    swrKey,
    ...query,
  };
};
export const getNodeGetUrl = (nodeSlug: string, params?: NodeGetParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/nodes/${nodeSlug}?${stringifiedParams}`
    : `/nodes/${nodeSlug}`;
};

/**
 * Get a node by its URL slug.
 */
export const nodeGet = async (
  nodeSlug: string,
  params?: NodeGetParams,
  options?: Parameters<typeof fetcher>[1],
): Promise<NodeGetOKResponse> => {
  return fetcher<NodeGetOKResponse>(getNodeGetUrl(nodeSlug, params), {
    ...options,
    method: "GET",
  });
};

export const getNodeGetKey = (nodeSlug: string, params?: NodeGetParams) =>
  [`/nodes/${nodeSlug}`, ...(params ? [params] : [])] as const;

export type NodeGetQueryResult = NonNullable<
  Awaited<ReturnType<typeof nodeGet>>
>;

export const useNodeGet = <
  TError =
    | NotModifiedResponse
    | UnauthorisedResponse
    | NotFoundResponse
    | InternalServerErrorResponse,
>(
  nodeSlug: string,
  params?: NodeGetParams,
  options?: {
    swr?: SWRConfiguration<Awaited<ReturnType<typeof nodeGet>>, TError> & {
      swrKey?: Key;
      enabled?: boolean;
    };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const isEnabled =
    swrOptions?.enabled !== false &&
    nodeSlug !== null &&
    nodeSlug !== undefined;
  const swrKey =
    swrOptions?.swrKey ??
    (() => (isEnabled ? getNodeGetKey(nodeSlug, params) : null));
  const swrFn = () => nodeGet(nodeSlug, params, requestOptions);

  const query = useSwr<Awaited<ReturnType<typeof swrFn>>, TError>(
    swrKey,
    swrFn,
    swrOptions,
  );

  return {
    swrKey,
    ...query,
  };
};
export const getNodeUpdateUrl = (nodeSlug: string) => {
  return `/nodes/${nodeSlug}`;
};

/**
 * Update a node directly.
 *
 * Direct updates are intended for fast edits by members who can manage
 * the target node. If a node has a working draft version, direct updates
 * to versioned page fields are rejected until the draft is applied or
 * deleted. When a direct update changes versioned page fields and no
 * draft exists, the node's `current_version_id` pointer is cleared
 * because the live node no longer exactly represents an applied
 * checkpoint.
 */
export const nodeUpdate = async (
  nodeSlug: string,
  nodeUpdateBody?: NodeUpdateBody,
  options?: Parameters<typeof fetcher>[1],
): Promise<NodeUpdateOKResponse> => {
  return fetcher<NodeUpdateOKResponse>(getNodeUpdateUrl(nodeSlug), {
    ...options,
    method: "PATCH",
    headers: { "Content-Type": "application/json", ...options?.headers },
    body: JSON.stringify(nodeUpdateBody),
  });
};

export const getNodeUpdateMutationFetcher = (
  nodeSlug: string,
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, { arg }: { arg: NodeUpdateBody | undefined }) => {
    return nodeUpdate(nodeSlug, arg, options);
  };
};
export const getNodeUpdateMutationKey = (nodeSlug: string) =>
  [`/nodes/${nodeSlug}`] as const;

export type NodeUpdateMutationResult = NonNullable<
  Awaited<ReturnType<typeof nodeUpdate>>
>;

export const useNodeUpdate = <
  TError =
    UnauthorisedResponse | NotFoundResponse | InternalServerErrorResponse,
>(
  nodeSlug: string,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof nodeUpdate>>,
      TError,
      Key,
      NodeUpdateBody | undefined,
      Awaited<ReturnType<typeof nodeUpdate>>
    > & { swrKey?: string };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey = swrOptions?.swrKey ?? getNodeUpdateMutationKey(nodeSlug);
  const swrFn = getNodeUpdateMutationFetcher(nodeSlug, requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getNodeDeleteUrl = (
  nodeSlug: string,
  params?: NodeDeleteParams,
) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/nodes/${nodeSlug}?${stringifiedParams}`
    : `/nodes/${nodeSlug}`;
};

/**
 * Delete a node and move all children to its parent or root.
 */
export const nodeDelete = async (
  nodeSlug: string,
  params?: NodeDeleteParams,
  options?: Parameters<typeof fetcher>[1],
): Promise<NodeDeleteOKResponse> => {
  return fetcher<NodeDeleteOKResponse>(getNodeDeleteUrl(nodeSlug, params), {
    ...options,
    method: "DELETE",
  });
};

export const getNodeDeleteMutationFetcher = (
  nodeSlug: string,
  params?: NodeDeleteParams,
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, __: { arg: Arguments }) => {
    return nodeDelete(nodeSlug, params, options);
  };
};
export const getNodeDeleteMutationKey = (
  nodeSlug: string,
  params?: NodeDeleteParams,
) => [`/nodes/${nodeSlug}`, ...(params ? [params] : [])] as const;

export type NodeDeleteMutationResult = NonNullable<
  Awaited<ReturnType<typeof nodeDelete>>
>;

export const useNodeDelete = <
  TError =
    UnauthorisedResponse | NotFoundResponse | InternalServerErrorResponse,
>(
  nodeSlug: string,
  params?: NodeDeleteParams,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof nodeDelete>>,
      TError,
      Key,
      Arguments,
      Awaited<ReturnType<typeof nodeDelete>>
    > & { swrKey?: string };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey =
    swrOptions?.swrKey ?? getNodeDeleteMutationKey(nodeSlug, params);
  const swrFn = getNodeDeleteMutationFetcher(nodeSlug, params, requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getNodeGenerateTitleUrl = (nodeSlug: string) => {
  return `/nodes/${nodeSlug}/title`;
};

/**
 * Generate a proposed title for the specified node. Will not actually
 * mutate the specified node, instead will return a proposal based on the
 * output from a language model call.
 */
export const nodeGenerateTitle = async (
  nodeSlug: string,
  nodeGenerateTitleBody?: NodeGenerateTitleBody,
  options?: Parameters<typeof fetcher>[1],
): Promise<NodeGenerateTitleOKResponse> => {
  return fetcher<NodeGenerateTitleOKResponse>(
    getNodeGenerateTitleUrl(nodeSlug),
    {
      ...options,
      method: "POST",
      headers: { "Content-Type": "application/json", ...options?.headers },
      body: JSON.stringify(nodeGenerateTitleBody),
    },
  );
};

export const getNodeGenerateTitleMutationFetcher = (
  nodeSlug: string,
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, { arg }: { arg: NodeGenerateTitleBody | undefined }) => {
    return nodeGenerateTitle(nodeSlug, arg, options);
  };
};
export const getNodeGenerateTitleMutationKey = (nodeSlug: string) =>
  [`/nodes/${nodeSlug}/title`] as const;

export type NodeGenerateTitleMutationResult = NonNullable<
  Awaited<ReturnType<typeof nodeGenerateTitle>>
>;

export const useNodeGenerateTitle = <
  TError =
    | BadRequestResponse
    | UnauthorisedResponse
    | NotFoundResponse
    | NotImplementedResponse
    | InternalServerErrorResponse,
>(
  nodeSlug: string,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof nodeGenerateTitle>>,
      TError,
      Key,
      NodeGenerateTitleBody | undefined,
      Awaited<ReturnType<typeof nodeGenerateTitle>>
    > & { swrKey?: string };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey =
    swrOptions?.swrKey ?? getNodeGenerateTitleMutationKey(nodeSlug);
  const swrFn = getNodeGenerateTitleMutationFetcher(nodeSlug, requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getNodeGenerateTagsUrl = (nodeSlug: string) => {
  return `/nodes/${nodeSlug}/tags`;
};

/**
 * Generate proposed tags for the specified node. Will not actually mutate
 * the specified node, instead will return a proposal based on the output
 * from a language model call.
 */
export const nodeGenerateTags = async (
  nodeSlug: string,
  nodeGenerateTagsBody?: NodeGenerateTagsBody,
  options?: Parameters<typeof fetcher>[1],
): Promise<NodeGenerateTagsOKResponse> => {
  return fetcher<NodeGenerateTagsOKResponse>(getNodeGenerateTagsUrl(nodeSlug), {
    ...options,
    method: "POST",
    headers: { "Content-Type": "application/json", ...options?.headers },
    body: JSON.stringify(nodeGenerateTagsBody),
  });
};

export const getNodeGenerateTagsMutationFetcher = (
  nodeSlug: string,
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, { arg }: { arg: NodeGenerateTagsBody | undefined }) => {
    return nodeGenerateTags(nodeSlug, arg, options);
  };
};
export const getNodeGenerateTagsMutationKey = (nodeSlug: string) =>
  [`/nodes/${nodeSlug}/tags`] as const;

export type NodeGenerateTagsMutationResult = NonNullable<
  Awaited<ReturnType<typeof nodeGenerateTags>>
>;

export const useNodeGenerateTags = <
  TError =
    | BadRequestResponse
    | UnauthorisedResponse
    | NotFoundResponse
    | NotImplementedResponse
    | InternalServerErrorResponse,
>(
  nodeSlug: string,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof nodeGenerateTags>>,
      TError,
      Key,
      NodeGenerateTagsBody | undefined,
      Awaited<ReturnType<typeof nodeGenerateTags>>
    > & { swrKey?: string };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey = swrOptions?.swrKey ?? getNodeGenerateTagsMutationKey(nodeSlug);
  const swrFn = getNodeGenerateTagsMutationFetcher(nodeSlug, requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getNodeGenerateContentUrl = (nodeSlug: string) => {
  return `/nodes/${nodeSlug}/content`;
};

/**
 * Generate proposed content for the specified node. Will not actually
 * mutate the specified node, instead will return a proposal based on the
 * output from a language model call.
 */
export const nodeGenerateContent = async (
  nodeSlug: string,
  nodeGenerateContentBody?: NodeGenerateContentBody,
  options?: Parameters<typeof fetcher>[1],
): Promise<NodeGenerateContentOKResponse> => {
  return fetcher<NodeGenerateContentOKResponse>(
    getNodeGenerateContentUrl(nodeSlug),
    {
      ...options,
      method: "POST",
      headers: { "Content-Type": "application/json", ...options?.headers },
      body: JSON.stringify(nodeGenerateContentBody),
    },
  );
};

export const getNodeGenerateContentMutationFetcher = (
  nodeSlug: string,
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, { arg }: { arg: NodeGenerateContentBody | undefined }) => {
    return nodeGenerateContent(nodeSlug, arg, options);
  };
};
export const getNodeGenerateContentMutationKey = (nodeSlug: string) =>
  [`/nodes/${nodeSlug}/content`] as const;

export type NodeGenerateContentMutationResult = NonNullable<
  Awaited<ReturnType<typeof nodeGenerateContent>>
>;

export const useNodeGenerateContent = <
  TError =
    | BadRequestResponse
    | UnauthorisedResponse
    | NotFoundResponse
    | NotImplementedResponse
    | InternalServerErrorResponse,
>(
  nodeSlug: string,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof nodeGenerateContent>>,
      TError,
      Key,
      NodeGenerateContentBody | undefined,
      Awaited<ReturnType<typeof nodeGenerateContent>>
    > & { swrKey?: string };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey =
    swrOptions?.swrKey ?? getNodeGenerateContentMutationKey(nodeSlug);
  const swrFn = getNodeGenerateContentMutationFetcher(nodeSlug, requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getNodeListChildrenUrl = (
  nodeSlug: string,
  params?: NodeListChildrenParams,
) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    const explodeParameters = ["tags"];

    if (Array.isArray(value) && explodeParameters.includes(key)) {
      value.forEach((v) => {
        normalizedParams.append(key, v === null ? "null" : String(v));
      });
      return;
    }

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/nodes/${nodeSlug}/children?${stringifiedParams}`
    : `/nodes/${nodeSlug}/children`;
};

/**
 * Get all the children of a given node using the provided filters and page
 * parameters. This can be used for rendering the child nodes of the given
 * node as an interactive table where properties can be used as columns.
 */
export const nodeListChildren = async (
  nodeSlug: string,
  params?: NodeListChildrenParams,
  options?: Parameters<typeof fetcher>[1],
): Promise<NodeListOKResponse> => {
  return fetcher<NodeListOKResponse>(getNodeListChildrenUrl(nodeSlug, params), {
    ...options,
    method: "GET",
  });
};

export const getNodeListChildrenKey = (
  nodeSlug: string,
  params?: NodeListChildrenParams,
) => [`/nodes/${nodeSlug}/children`, ...(params ? [params] : [])] as const;

export type NodeListChildrenQueryResult = NonNullable<
  Awaited<ReturnType<typeof nodeListChildren>>
>;

export const useNodeListChildren = <
  TError =
    UnauthorisedResponse | NotFoundResponse | InternalServerErrorResponse,
>(
  nodeSlug: string,
  params?: NodeListChildrenParams,
  options?: {
    swr?: SWRConfiguration<
      Awaited<ReturnType<typeof nodeListChildren>>,
      TError
    > & { swrKey?: Key; enabled?: boolean };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const isEnabled =
    swrOptions?.enabled !== false &&
    nodeSlug !== null &&
    nodeSlug !== undefined;
  const swrKey =
    swrOptions?.swrKey ??
    (() => (isEnabled ? getNodeListChildrenKey(nodeSlug, params) : null));
  const swrFn = () => nodeListChildren(nodeSlug, params, requestOptions);

  const query = useSwr<Awaited<ReturnType<typeof swrFn>>, TError>(
    swrKey,
    swrFn,
    swrOptions,
  );

  return {
    swrKey,
    ...query,
  };
};
export const getNodeUpdateChildrenPropertySchemaUrl = (nodeSlug: string) => {
  return `/nodes/${nodeSlug}/children/property-schema`;
};

/**
 * Updates the property schema of the children of this node. All children
 * of a node use the same schema for properties resulting in a table-like
 * structure and behaviour. See also: NodeUpdatePropertySchema
 */
export const nodeUpdateChildrenPropertySchema = async (
  nodeSlug: string,
  nodeUpdatePropertySchemaBody?: NodeUpdatePropertySchemaBody,
  options?: Parameters<typeof fetcher>[1],
): Promise<NodeUpdatePropertySchemaOKResponse> => {
  return fetcher<NodeUpdatePropertySchemaOKResponse>(
    getNodeUpdateChildrenPropertySchemaUrl(nodeSlug),
    {
      ...options,
      method: "PATCH",
      headers: { "Content-Type": "application/json", ...options?.headers },
      body: JSON.stringify(nodeUpdatePropertySchemaBody),
    },
  );
};

export const getNodeUpdateChildrenPropertySchemaMutationFetcher = (
  nodeSlug: string,
  options?: SecondParameter<typeof fetcher>,
) => {
  return (
    _: Key,
    { arg }: { arg: NodeUpdatePropertySchemaBody | undefined },
  ) => {
    return nodeUpdateChildrenPropertySchema(nodeSlug, arg, options);
  };
};
export const getNodeUpdateChildrenPropertySchemaMutationKey = (
  nodeSlug: string,
) => [`/nodes/${nodeSlug}/children/property-schema`] as const;

export type NodeUpdateChildrenPropertySchemaMutationResult = NonNullable<
  Awaited<ReturnType<typeof nodeUpdateChildrenPropertySchema>>
>;

export const useNodeUpdateChildrenPropertySchema = <
  TError =
    UnauthorisedResponse | NotFoundResponse | InternalServerErrorResponse,
>(
  nodeSlug: string,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof nodeUpdateChildrenPropertySchema>>,
      TError,
      Key,
      NodeUpdatePropertySchemaBody | undefined,
      Awaited<ReturnType<typeof nodeUpdateChildrenPropertySchema>>
    > & { swrKey?: string };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey =
    swrOptions?.swrKey ??
    getNodeUpdateChildrenPropertySchemaMutationKey(nodeSlug);
  const swrFn = getNodeUpdateChildrenPropertySchemaMutationFetcher(
    nodeSlug,
    requestOptions,
  );

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getNodeUpdatePropertySchemaUrl = (nodeSlug: string) => {
  return `/nodes/${nodeSlug}/property-schema`;
};

/**
 * Updates the property schema of this node and its siblings. All children
 * of a node use the same schema for properties resulting in a table-like
 * structure and behaviour. Property schemas are loosely structured and can
 * automatically cast their values sometimes. A failed cast will not change
 * data and instead just yield an empty value when reading however changing
 * the schema back to the original type (or a type compatible with what the
 * type was before changing) will retain the original data upon next read.
 * This permits clients to undo changes to the schema easily while allowing
 * quick schema changes without the need to remove or update values before.
 */
export const nodeUpdatePropertySchema = async (
  nodeSlug: string,
  nodeUpdatePropertySchemaBody?: NodeUpdatePropertySchemaBody,
  options?: Parameters<typeof fetcher>[1],
): Promise<NodeUpdatePropertySchemaOKResponse> => {
  return fetcher<NodeUpdatePropertySchemaOKResponse>(
    getNodeUpdatePropertySchemaUrl(nodeSlug),
    {
      ...options,
      method: "PATCH",
      headers: { "Content-Type": "application/json", ...options?.headers },
      body: JSON.stringify(nodeUpdatePropertySchemaBody),
    },
  );
};

export const getNodeUpdatePropertySchemaMutationFetcher = (
  nodeSlug: string,
  options?: SecondParameter<typeof fetcher>,
) => {
  return (
    _: Key,
    { arg }: { arg: NodeUpdatePropertySchemaBody | undefined },
  ) => {
    return nodeUpdatePropertySchema(nodeSlug, arg, options);
  };
};
export const getNodeUpdatePropertySchemaMutationKey = (nodeSlug: string) =>
  [`/nodes/${nodeSlug}/property-schema`] as const;

export type NodeUpdatePropertySchemaMutationResult = NonNullable<
  Awaited<ReturnType<typeof nodeUpdatePropertySchema>>
>;

export const useNodeUpdatePropertySchema = <
  TError =
    UnauthorisedResponse | NotFoundResponse | InternalServerErrorResponse,
>(
  nodeSlug: string,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof nodeUpdatePropertySchema>>,
      TError,
      Key,
      NodeUpdatePropertySchemaBody | undefined,
      Awaited<ReturnType<typeof nodeUpdatePropertySchema>>
    > & { swrKey?: string };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey =
    swrOptions?.swrKey ?? getNodeUpdatePropertySchemaMutationKey(nodeSlug);
  const swrFn = getNodeUpdatePropertySchemaMutationFetcher(
    nodeSlug,
    requestOptions,
  );

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getNodeUpdatePropertiesUrl = (nodeSlug: string) => {
  return `/nodes/${nodeSlug}/properties`;
};

/**
 * Update the properties of a node. New schema fields will result in the
 * schema of the node being updated before values are assigned. This will
 * also propagate to all sibling nodes as they all share the same schema.
 */
export const nodeUpdateProperties = async (
  nodeSlug: string,
  nodeUpdatePropertiesBody?: NodeUpdatePropertiesBody,
  options?: Parameters<typeof fetcher>[1],
): Promise<NodeUpdatePropertiesOKResponse> => {
  return fetcher<NodeUpdatePropertiesOKResponse>(
    getNodeUpdatePropertiesUrl(nodeSlug),
    {
      ...options,
      method: "PATCH",
      headers: { "Content-Type": "application/json", ...options?.headers },
      body: JSON.stringify(nodeUpdatePropertiesBody),
    },
  );
};

export const getNodeUpdatePropertiesMutationFetcher = (
  nodeSlug: string,
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, { arg }: { arg: NodeUpdatePropertiesBody | undefined }) => {
    return nodeUpdateProperties(nodeSlug, arg, options);
  };
};
export const getNodeUpdatePropertiesMutationKey = (nodeSlug: string) =>
  [`/nodes/${nodeSlug}/properties`] as const;

export type NodeUpdatePropertiesMutationResult = NonNullable<
  Awaited<ReturnType<typeof nodeUpdateProperties>>
>;

export const useNodeUpdateProperties = <
  TError =
    | BadRequestResponse
    | UnauthorisedResponse
    | NotFoundResponse
    | InternalServerErrorResponse,
>(
  nodeSlug: string,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof nodeUpdateProperties>>,
      TError,
      Key,
      NodeUpdatePropertiesBody | undefined,
      Awaited<ReturnType<typeof nodeUpdateProperties>>
    > & { swrKey?: string };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey =
    swrOptions?.swrKey ?? getNodeUpdatePropertiesMutationKey(nodeSlug);
  const swrFn = getNodeUpdatePropertiesMutationFetcher(
    nodeSlug,
    requestOptions,
  );

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getNodeUpdateVisibilityUrl = (nodeSlug: string) => {
  return `/nodes/${nodeSlug}/visibility`;
};

/**
 * Update the visibility of a node. When changed, this may trigger other
 * operations such as notifications/newsletters. Changing the visibility of
 * anything to "published" is often accompanied by some other side effects.
 */
export const nodeUpdateVisibility = async (
  nodeSlug: string,
  visibilityUpdateBody?: VisibilityUpdateBody,
  options?: Parameters<typeof fetcher>[1],
): Promise<NodeUpdateOKResponse> => {
  return fetcher<NodeUpdateOKResponse>(getNodeUpdateVisibilityUrl(nodeSlug), {
    ...options,
    method: "PATCH",
    headers: { "Content-Type": "application/json", ...options?.headers },
    body: JSON.stringify(visibilityUpdateBody),
  });
};

export const getNodeUpdateVisibilityMutationFetcher = (
  nodeSlug: string,
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, { arg }: { arg: VisibilityUpdateBody | undefined }) => {
    return nodeUpdateVisibility(nodeSlug, arg, options);
  };
};
export const getNodeUpdateVisibilityMutationKey = (nodeSlug: string) =>
  [`/nodes/${nodeSlug}/visibility`] as const;

export type NodeUpdateVisibilityMutationResult = NonNullable<
  Awaited<ReturnType<typeof nodeUpdateVisibility>>
>;

export const useNodeUpdateVisibility = <
  TError =
    UnauthorisedResponse | NotFoundResponse | InternalServerErrorResponse,
>(
  nodeSlug: string,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof nodeUpdateVisibility>>,
      TError,
      Key,
      VisibilityUpdateBody | undefined,
      Awaited<ReturnType<typeof nodeUpdateVisibility>>
    > & { swrKey?: string };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey =
    swrOptions?.swrKey ?? getNodeUpdateVisibilityMutationKey(nodeSlug);
  const swrFn = getNodeUpdateVisibilityMutationFetcher(
    nodeSlug,
    requestOptions,
  );

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getNodeVersionListUrl = (
  nodeSlug: string,
  params?: NodeVersionListParams,
) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/nodes/${nodeSlug}/versions?${stringifiedParams}`
    : `/nodes/${nodeSlug}/versions`;
};

/**
 * List edit versions for a node.
 *
 * Versions have two states: draft and applied. A version is a draft when
 * it is pre-published. There can only be a single draft of a node.
 *
 * Applied versions are immutable historical snapshots of the page fields
 * that were copied into the node. The single draft version, when present,
 * is the working snapshot ahead of the live node and is visible only to
 * its author and members with `MANAGE_LIBRARY`.
 *
 * Results are ordered by `updated_at` descending so draft autosaves and
 * recently applied checkpoints appear before older history.
 */
export const nodeVersionList = async (
  nodeSlug: string,
  params?: NodeVersionListParams,
  options?: Parameters<typeof fetcher>[1],
): Promise<NodeVersionListOKResponse> => {
  return fetcher<NodeVersionListOKResponse>(
    getNodeVersionListUrl(nodeSlug, params),
    {
      ...options,
      method: "GET",
    },
  );
};

export const getNodeVersionListKey = (
  nodeSlug: string,
  params?: NodeVersionListParams,
) => [`/nodes/${nodeSlug}/versions`, ...(params ? [params] : [])] as const;

export type NodeVersionListQueryResult = NonNullable<
  Awaited<ReturnType<typeof nodeVersionList>>
>;

export const useNodeVersionList = <
  TError =
    UnauthorisedResponse | NotFoundResponse | InternalServerErrorResponse,
>(
  nodeSlug: string,
  params?: NodeVersionListParams,
  options?: {
    swr?: SWRConfiguration<
      Awaited<ReturnType<typeof nodeVersionList>>,
      TError
    > & { swrKey?: Key; enabled?: boolean };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const isEnabled =
    swrOptions?.enabled !== false &&
    nodeSlug !== null &&
    nodeSlug !== undefined;
  const swrKey =
    swrOptions?.swrKey ??
    (() => (isEnabled ? getNodeVersionListKey(nodeSlug, params) : null));
  const swrFn = () => nodeVersionList(nodeSlug, params, requestOptions);

  const query = useSwr<Awaited<ReturnType<typeof swrFn>>, TError>(
    swrKey,
    swrFn,
    swrOptions,
  );

  return {
    swrKey,
    ...query,
  };
};
export const getNodeVersionCreateUrl = (nodeSlug: string) => {
  return `/nodes/${nodeSlug}/versions`;
};

/**
 * Create the single mutable draft checkpoint for a node.
 *
 * This operation requires either `SUBMIT_LIBRARY_NODE_CHANGES` or
 * `MANAGE_LIBRARY` permission. The draft starts as a full snapshot of the
 * node's current versioned page fields. Fields supplied in the request
 * overlay that snapshot, omitted fields keep the snapshotted value, and
 * explicit null values clear nullable fields.
 *
 * A node can have only one draft. If a draft already exists for the node,
 * this operation returns a conflict. Drafts do not mutate the target node
 * until the draft is applied through the version status endpoint by a
 * member with `MANAGE_LIBRARY`.
 */
export const nodeVersionCreate = async (
  nodeSlug: string,
  nodeVersionCreateBody?: NodeVersionCreateBody,
  options?: Parameters<typeof fetcher>[1],
): Promise<NodeVersionCreateOKResponse> => {
  return fetcher<NodeVersionCreateOKResponse>(
    getNodeVersionCreateUrl(nodeSlug),
    {
      ...options,
      method: "POST",
      headers: { "Content-Type": "application/json", ...options?.headers },
      body: JSON.stringify(nodeVersionCreateBody),
    },
  );
};

export const getNodeVersionCreateMutationFetcher = (
  nodeSlug: string,
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, { arg }: { arg: NodeVersionCreateBody | undefined }) => {
    return nodeVersionCreate(nodeSlug, arg, options);
  };
};
export const getNodeVersionCreateMutationKey = (nodeSlug: string) =>
  [`/nodes/${nodeSlug}/versions`] as const;

export type NodeVersionCreateMutationResult = NonNullable<
  Awaited<ReturnType<typeof nodeVersionCreate>>
>;

export const useNodeVersionCreate = <
  TError =
    | BadRequestResponse
    | UnauthorisedResponse
    | NotFoundResponse
    | ConflictResponse
    | InternalServerErrorResponse,
>(
  nodeSlug: string,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof nodeVersionCreate>>,
      TError,
      Key,
      NodeVersionCreateBody | undefined,
      Awaited<ReturnType<typeof nodeVersionCreate>>
    > & { swrKey?: string };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey =
    swrOptions?.swrKey ?? getNodeVersionCreateMutationKey(nodeSlug);
  const swrFn = getNodeVersionCreateMutationFetcher(nodeSlug, requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getNodeVersionDraftGetUrl = (nodeSlug: string) => {
  return `/nodes/${nodeSlug}/versions/draft`;
};

/**
 * Get the node's single working draft checkpoint.
 *
 * This is a stable alias for the draft version of a node. It allows
 * clients to read "the draft" without listing versions and inspecting
 * status values. If the node has no draft, or the draft is not visible to
 * the caller, this operation returns not found.
 *
 * The draft is visible only to its author and members with
 * `MANAGE_LIBRARY`.
 */
export const nodeVersionDraftGet = async (
  nodeSlug: string,
  options?: Parameters<typeof fetcher>[1],
): Promise<NodeVersionGetOKResponse> => {
  return fetcher<NodeVersionGetOKResponse>(
    getNodeVersionDraftGetUrl(nodeSlug),
    {
      ...options,
      method: "GET",
    },
  );
};

export const getNodeVersionDraftGetKey = (nodeSlug: string) =>
  [`/nodes/${nodeSlug}/versions/draft`] as const;

export type NodeVersionDraftGetQueryResult = NonNullable<
  Awaited<ReturnType<typeof nodeVersionDraftGet>>
>;

export const useNodeVersionDraftGet = <
  TError =
    UnauthorisedResponse | NotFoundResponse | InternalServerErrorResponse,
>(
  nodeSlug: string,
  options?: {
    swr?: SWRConfiguration<
      Awaited<ReturnType<typeof nodeVersionDraftGet>>,
      TError
    > & { swrKey?: Key; enabled?: boolean };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const isEnabled =
    swrOptions?.enabled !== false &&
    nodeSlug !== null &&
    nodeSlug !== undefined;
  const swrKey =
    swrOptions?.swrKey ??
    (() => (isEnabled ? getNodeVersionDraftGetKey(nodeSlug) : null));
  const swrFn = () => nodeVersionDraftGet(nodeSlug, requestOptions);

  const query = useSwr<Awaited<ReturnType<typeof swrFn>>, TError>(
    swrKey,
    swrFn,
    swrOptions,
  );

  return {
    swrKey,
    ...query,
  };
};
export const getNodeVersionDraftUpdateUrl = (nodeSlug: string) => {
  return `/nodes/${nodeSlug}/versions/draft`;
};

/**
 * Update the node's single working draft checkpoint.
 *
 * This is a stable alias for patching the draft version of a node without
 * first listing versions or knowing the draft version identifier. The
 * node must already have a draft visible to the caller. This operation
 * does not create a draft and does not apply the draft to the target node.
 *
 * The caller must be the draft author or have `MANAGE_LIBRARY`. Fields
 * omitted from the request are left unchanged on the draft snapshot.
 * Explicit null values clear nullable fields. Properties are a complete
 * desired-state list for the target node properties.
 */
export const nodeVersionDraftUpdate = async (
  nodeSlug: string,
  nodeVersionUpdateBody?: NodeVersionUpdateBody,
  options?: Parameters<typeof fetcher>[1],
): Promise<NodeVersionUpdateOKResponse> => {
  return fetcher<NodeVersionUpdateOKResponse>(
    getNodeVersionDraftUpdateUrl(nodeSlug),
    {
      ...options,
      method: "PATCH",
      headers: { "Content-Type": "application/json", ...options?.headers },
      body: JSON.stringify(nodeVersionUpdateBody),
    },
  );
};

export const getNodeVersionDraftUpdateMutationFetcher = (
  nodeSlug: string,
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, { arg }: { arg: NodeVersionUpdateBody | undefined }) => {
    return nodeVersionDraftUpdate(nodeSlug, arg, options);
  };
};
export const getNodeVersionDraftUpdateMutationKey = (nodeSlug: string) =>
  [`/nodes/${nodeSlug}/versions/draft`] as const;

export type NodeVersionDraftUpdateMutationResult = NonNullable<
  Awaited<ReturnType<typeof nodeVersionDraftUpdate>>
>;

export const useNodeVersionDraftUpdate = <
  TError =
    | BadRequestResponse
    | UnauthorisedResponse
    | NotFoundResponse
    | ConflictResponse
    | InternalServerErrorResponse,
>(
  nodeSlug: string,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof nodeVersionDraftUpdate>>,
      TError,
      Key,
      NodeVersionUpdateBody | undefined,
      Awaited<ReturnType<typeof nodeVersionDraftUpdate>>
    > & { swrKey?: string };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey =
    swrOptions?.swrKey ?? getNodeVersionDraftUpdateMutationKey(nodeSlug);
  const swrFn = getNodeVersionDraftUpdateMutationFetcher(
    nodeSlug,
    requestOptions,
  );

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getNodeVersionGetUrl = (nodeSlug: string, versionId: string) => {
  return `/nodes/${nodeSlug}/versions/${versionId}`;
};

/**
 * Get an edit version for a node.
 *
 * The version must belong to the node identified by `node_slug`. Applied
 * versions are immutable historical snapshots and are visible to callers
 * who can read the target node. The draft version is visible only to its
 * author and members with `MANAGE_LIBRARY`.
 */
export const nodeVersionGet = async (
  nodeSlug: string,
  versionId: string,
  options?: Parameters<typeof fetcher>[1],
): Promise<NodeVersionGetOKResponse> => {
  return fetcher<NodeVersionGetOKResponse>(
    getNodeVersionGetUrl(nodeSlug, versionId),
    {
      ...options,
      method: "GET",
    },
  );
};

export const getNodeVersionGetKey = (nodeSlug: string, versionId: string) =>
  [`/nodes/${nodeSlug}/versions/${versionId}`] as const;

export type NodeVersionGetQueryResult = NonNullable<
  Awaited<ReturnType<typeof nodeVersionGet>>
>;

export const useNodeVersionGet = <
  TError =
    UnauthorisedResponse | NotFoundResponse | InternalServerErrorResponse,
>(
  nodeSlug: string,
  versionId: string,
  options?: {
    swr?: SWRConfiguration<
      Awaited<ReturnType<typeof nodeVersionGet>>,
      TError
    > & { swrKey?: Key; enabled?: boolean };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const isEnabled =
    swrOptions?.enabled !== false &&
    nodeSlug !== null &&
    nodeSlug !== undefined &&
    versionId !== null &&
    versionId !== undefined;
  const swrKey =
    swrOptions?.swrKey ??
    (() => (isEnabled ? getNodeVersionGetKey(nodeSlug, versionId) : null));
  const swrFn = () => nodeVersionGet(nodeSlug, versionId, requestOptions);

  const query = useSwr<Awaited<ReturnType<typeof swrFn>>, TError>(
    swrKey,
    swrFn,
    swrOptions,
  );

  return {
    swrKey,
    ...query,
  };
};
export const getNodeVersionUpdateUrl = (
  nodeSlug: string,
  versionId: string,
) => {
  return `/nodes/${nodeSlug}/versions/${versionId}`;
};

/**
 * Update the node's single draft checkpoint.
 *
 * This operation is for draft autosave and editing only. It does not
 * change version status and cannot apply a version to the target node.
 * The version must still have draft status and the caller must be the
 * draft author or have `MANAGE_LIBRARY`.
 *
 * Fields omitted from the request are left unchanged on the draft
 * snapshot. Explicit null values clear nullable fields. Properties are a
 * complete desired-state list for the target node properties: when the
 * version is applied, the list replaces the node's existing property set
 * rather than merging with it.
 */
export const nodeVersionUpdate = async (
  nodeSlug: string,
  versionId: string,
  nodeVersionUpdateBody?: NodeVersionUpdateBody,
  options?: Parameters<typeof fetcher>[1],
): Promise<NodeVersionUpdateOKResponse> => {
  return fetcher<NodeVersionUpdateOKResponse>(
    getNodeVersionUpdateUrl(nodeSlug, versionId),
    {
      ...options,
      method: "PATCH",
      headers: { "Content-Type": "application/json", ...options?.headers },
      body: JSON.stringify(nodeVersionUpdateBody),
    },
  );
};

export const getNodeVersionUpdateMutationFetcher = (
  nodeSlug: string,
  versionId: string,
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, { arg }: { arg: NodeVersionUpdateBody | undefined }) => {
    return nodeVersionUpdate(nodeSlug, versionId, arg, options);
  };
};
export const getNodeVersionUpdateMutationKey = (
  nodeSlug: string,
  versionId: string,
) => [`/nodes/${nodeSlug}/versions/${versionId}`] as const;

export type NodeVersionUpdateMutationResult = NonNullable<
  Awaited<ReturnType<typeof nodeVersionUpdate>>
>;

export const useNodeVersionUpdate = <
  TError =
    | BadRequestResponse
    | UnauthorisedResponse
    | NotFoundResponse
    | ConflictResponse
    | InternalServerErrorResponse,
>(
  nodeSlug: string,
  versionId: string,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof nodeVersionUpdate>>,
      TError,
      Key,
      NodeVersionUpdateBody | undefined,
      Awaited<ReturnType<typeof nodeVersionUpdate>>
    > & { swrKey?: string };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey =
    swrOptions?.swrKey ?? getNodeVersionUpdateMutationKey(nodeSlug, versionId);
  const swrFn = getNodeVersionUpdateMutationFetcher(
    nodeSlug,
    versionId,
    requestOptions,
  );

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getNodeVersionDeleteUrl = (
  nodeSlug: string,
  versionId: string,
) => {
  return `/nodes/${nodeSlug}/versions/${versionId}`;
};

/**
 * Delete the working draft checkpoint.
 *
 * A draft author can discard their own draft. Members with `MANAGE_LIBRARY`
 * permission can discard any draft for the node. The draft row is removed
 * from history. Applied versions are immutable history entries and cannot
 * be deleted through this endpoint.
 */
export const nodeVersionDelete = async (
  nodeSlug: string,
  versionId: string,
  options?: Parameters<typeof fetcher>[1],
): Promise<NodeVersionDeleteOKResponse> => {
  return fetcher<NodeVersionDeleteOKResponse>(
    getNodeVersionDeleteUrl(nodeSlug, versionId),
    {
      ...options,
      method: "DELETE",
    },
  );
};

export const getNodeVersionDeleteMutationFetcher = (
  nodeSlug: string,
  versionId: string,
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, __: { arg: Arguments }) => {
    return nodeVersionDelete(nodeSlug, versionId, options);
  };
};
export const getNodeVersionDeleteMutationKey = (
  nodeSlug: string,
  versionId: string,
) => [`/nodes/${nodeSlug}/versions/${versionId}`] as const;

export type NodeVersionDeleteMutationResult = NonNullable<
  Awaited<ReturnType<typeof nodeVersionDelete>>
>;

export const useNodeVersionDelete = <
  TError =
    UnauthorisedResponse | NotFoundResponse | InternalServerErrorResponse,
>(
  nodeSlug: string,
  versionId: string,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof nodeVersionDelete>>,
      TError,
      Key,
      Arguments,
      Awaited<ReturnType<typeof nodeVersionDelete>>
    > & { swrKey?: string };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey =
    swrOptions?.swrKey ?? getNodeVersionDeleteMutationKey(nodeSlug, versionId);
  const swrFn = getNodeVersionDeleteMutationFetcher(
    nodeSlug,
    versionId,
    requestOptions,
  );

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getNodeVersionUpdateStatusUrl = (
  nodeSlug: string,
  versionId: string,
) => {
  return `/nodes/${nodeSlug}/versions/${versionId}/status`;
};

/**
 * Update the lifecycle status of a checkpoint.
 *
 * This endpoint is separate from the content patch endpoint because status
 * changes have side effects. For the initial checkpoint workflow, the
 * only supported transition is draft to applied. Applying a version is
 * restricted to members with `MANAGE_LIBRARY`.
 *
 * Applying a draft copies the full draft snapshot into the target node,
 * applies properties as a complete desired-state list, marks the version
 * immutable, and updates the node's `current_version_id` pointer to the
 * applied version. This is a linear operation; applying a draft does not
 * merge against other draft or historical versions.
 *
 * Clients must use this endpoint for lifecycle transitions and must not
 * combine status changes with regular draft content updates.
 */
export const nodeVersionUpdateStatus = async (
  nodeSlug: string,
  versionId: string,
  nodeVersionUpdateStatusBody?: NodeVersionUpdateStatusBody,
  options?: Parameters<typeof fetcher>[1],
): Promise<NodeVersionUpdateOKResponse> => {
  return fetcher<NodeVersionUpdateOKResponse>(
    getNodeVersionUpdateStatusUrl(nodeSlug, versionId),
    {
      ...options,
      method: "PATCH",
      headers: { "Content-Type": "application/json", ...options?.headers },
      body: JSON.stringify(nodeVersionUpdateStatusBody),
    },
  );
};

export const getNodeVersionUpdateStatusMutationFetcher = (
  nodeSlug: string,
  versionId: string,
  options?: SecondParameter<typeof fetcher>,
) => {
  return (
    _: Key,
    { arg }: { arg: NodeVersionUpdateStatusBody | undefined },
  ) => {
    return nodeVersionUpdateStatus(nodeSlug, versionId, arg, options);
  };
};
export const getNodeVersionUpdateStatusMutationKey = (
  nodeSlug: string,
  versionId: string,
) => [`/nodes/${nodeSlug}/versions/${versionId}/status`] as const;

export type NodeVersionUpdateStatusMutationResult = NonNullable<
  Awaited<ReturnType<typeof nodeVersionUpdateStatus>>
>;

export const useNodeVersionUpdateStatus = <
  TError =
    | BadRequestResponse
    | UnauthorisedResponse
    | NotFoundResponse
    | InternalServerErrorResponse,
>(
  nodeSlug: string,
  versionId: string,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof nodeVersionUpdateStatus>>,
      TError,
      Key,
      NodeVersionUpdateStatusBody | undefined,
      Awaited<ReturnType<typeof nodeVersionUpdateStatus>>
    > & { swrKey?: string };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey =
    swrOptions?.swrKey ??
    getNodeVersionUpdateStatusMutationKey(nodeSlug, versionId);
  const swrFn = getNodeVersionUpdateStatusMutationFetcher(
    nodeSlug,
    versionId,
    requestOptions,
  );

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getNodeAddAssetUrl = (nodeSlug: string, assetId: string) => {
  return `/nodes/${nodeSlug}/assets/${assetId}`;
};

/**
 * Add an asset to a node.
 */
export const nodeAddAsset = async (
  nodeSlug: string,
  assetId: string,
  options?: Parameters<typeof fetcher>[1],
): Promise<NodeUpdateOKResponse> => {
  return fetcher<NodeUpdateOKResponse>(getNodeAddAssetUrl(nodeSlug, assetId), {
    ...options,
    method: "PUT",
  });
};

export const getNodeAddAssetMutationFetcher = (
  nodeSlug: string,
  assetId: string,
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, __: { arg: Arguments }) => {
    return nodeAddAsset(nodeSlug, assetId, options);
  };
};
export const getNodeAddAssetMutationKey = (nodeSlug: string, assetId: string) =>
  [`/nodes/${nodeSlug}/assets/${assetId}`] as const;

export type NodeAddAssetMutationResult = NonNullable<
  Awaited<ReturnType<typeof nodeAddAsset>>
>;

export const useNodeAddAsset = <
  TError =
    UnauthorisedResponse | NotFoundResponse | InternalServerErrorResponse,
>(
  nodeSlug: string,
  assetId: string,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof nodeAddAsset>>,
      TError,
      Key,
      Arguments,
      Awaited<ReturnType<typeof nodeAddAsset>>
    > & { swrKey?: string };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey =
    swrOptions?.swrKey ?? getNodeAddAssetMutationKey(nodeSlug, assetId);
  const swrFn = getNodeAddAssetMutationFetcher(
    nodeSlug,
    assetId,
    requestOptions,
  );

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getNodeRemoveAssetUrl = (nodeSlug: string, assetId: string) => {
  return `/nodes/${nodeSlug}/assets/${assetId}`;
};

/**
 * Remove an asset from a node.
 */
export const nodeRemoveAsset = async (
  nodeSlug: string,
  assetId: string,
  options?: Parameters<typeof fetcher>[1],
): Promise<NodeUpdateOKResponse> => {
  return fetcher<NodeUpdateOKResponse>(
    getNodeRemoveAssetUrl(nodeSlug, assetId),
    {
      ...options,
      method: "DELETE",
    },
  );
};

export const getNodeRemoveAssetMutationFetcher = (
  nodeSlug: string,
  assetId: string,
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, __: { arg: Arguments }) => {
    return nodeRemoveAsset(nodeSlug, assetId, options);
  };
};
export const getNodeRemoveAssetMutationKey = (
  nodeSlug: string,
  assetId: string,
) => [`/nodes/${nodeSlug}/assets/${assetId}`] as const;

export type NodeRemoveAssetMutationResult = NonNullable<
  Awaited<ReturnType<typeof nodeRemoveAsset>>
>;

export const useNodeRemoveAsset = <
  TError =
    UnauthorisedResponse | NotFoundResponse | InternalServerErrorResponse,
>(
  nodeSlug: string,
  assetId: string,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof nodeRemoveAsset>>,
      TError,
      Key,
      Arguments,
      Awaited<ReturnType<typeof nodeRemoveAsset>>
    > & { swrKey?: string };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey =
    swrOptions?.swrKey ?? getNodeRemoveAssetMutationKey(nodeSlug, assetId);
  const swrFn = getNodeRemoveAssetMutationFetcher(
    nodeSlug,
    assetId,
    requestOptions,
  );

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getNodeAddNodeUrl = (nodeSlug: string, nodeSlugChild: string) => {
  return `/nodes/${nodeSlug}/nodes/${nodeSlugChild}`;
};

/**
 * Set a node's parent to the specified node
 */
export const nodeAddNode = async (
  nodeSlug: string,
  nodeSlugChild: string,
  options?: Parameters<typeof fetcher>[1],
): Promise<NodeAddChildOKResponse> => {
  return fetcher<NodeAddChildOKResponse>(
    getNodeAddNodeUrl(nodeSlug, nodeSlugChild),
    {
      ...options,
      method: "PUT",
    },
  );
};

export const getNodeAddNodeMutationFetcher = (
  nodeSlug: string,
  nodeSlugChild: string,
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, __: { arg: Arguments }) => {
    return nodeAddNode(nodeSlug, nodeSlugChild, options);
  };
};
export const getNodeAddNodeMutationKey = (
  nodeSlug: string,
  nodeSlugChild: string,
) => [`/nodes/${nodeSlug}/nodes/${nodeSlugChild}`] as const;

export type NodeAddNodeMutationResult = NonNullable<
  Awaited<ReturnType<typeof nodeAddNode>>
>;

export const useNodeAddNode = <
  TError =
    UnauthorisedResponse | NotFoundResponse | InternalServerErrorResponse,
>(
  nodeSlug: string,
  nodeSlugChild: string,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof nodeAddNode>>,
      TError,
      Key,
      Arguments,
      Awaited<ReturnType<typeof nodeAddNode>>
    > & { swrKey?: string };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey =
    swrOptions?.swrKey ?? getNodeAddNodeMutationKey(nodeSlug, nodeSlugChild);
  const swrFn = getNodeAddNodeMutationFetcher(
    nodeSlug,
    nodeSlugChild,
    requestOptions,
  );

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getNodeRemoveNodeUrl = (
  nodeSlug: string,
  nodeSlugChild: string,
) => {
  return `/nodes/${nodeSlug}/nodes/${nodeSlugChild}`;
};

/**
 * Remove a node from its parent node and back to the top level.
 */
export const nodeRemoveNode = async (
  nodeSlug: string,
  nodeSlugChild: string,
  options?: Parameters<typeof fetcher>[1],
): Promise<NodeRemoveChildOKResponse> => {
  return fetcher<NodeRemoveChildOKResponse>(
    getNodeRemoveNodeUrl(nodeSlug, nodeSlugChild),
    {
      ...options,
      method: "DELETE",
    },
  );
};

export const getNodeRemoveNodeMutationFetcher = (
  nodeSlug: string,
  nodeSlugChild: string,
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, __: { arg: Arguments }) => {
    return nodeRemoveNode(nodeSlug, nodeSlugChild, options);
  };
};
export const getNodeRemoveNodeMutationKey = (
  nodeSlug: string,
  nodeSlugChild: string,
) => [`/nodes/${nodeSlug}/nodes/${nodeSlugChild}`] as const;

export type NodeRemoveNodeMutationResult = NonNullable<
  Awaited<ReturnType<typeof nodeRemoveNode>>
>;

export const useNodeRemoveNode = <
  TError =
    UnauthorisedResponse | NotFoundResponse | InternalServerErrorResponse,
>(
  nodeSlug: string,
  nodeSlugChild: string,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof nodeRemoveNode>>,
      TError,
      Key,
      Arguments,
      Awaited<ReturnType<typeof nodeRemoveNode>>
    > & { swrKey?: string };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey =
    swrOptions?.swrKey ?? getNodeRemoveNodeMutationKey(nodeSlug, nodeSlugChild);
  const swrFn = getNodeRemoveNodeMutationFetcher(
    nodeSlug,
    nodeSlugChild,
    requestOptions,
  );

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getNodeUpdatePositionUrl = (nodeSlug: string) => {
  return `/nodes/${nodeSlug}/position`;
};

/**
 * Update the node's position in the tree, which optionally allows for
 * changing the node's parent either to another node or to `null` which
 * severs the parent and moves the node to the root. This endpoint also
 * allows for moving the node's sort position within either its current
 * parent, or when moving it to a new parent. Use this operation for a
 * draggable tree interface or a table interface.
 */
export const nodeUpdatePosition = async (
  nodeSlug: string,
  nodeUpdatePositionBody?: NodeUpdatePositionBody,
  options?: Parameters<typeof fetcher>[1],
): Promise<NodeUpdateOKResponse> => {
  return fetcher<NodeUpdateOKResponse>(getNodeUpdatePositionUrl(nodeSlug), {
    ...options,
    method: "PATCH",
    headers: { "Content-Type": "application/json", ...options?.headers },
    body: JSON.stringify(nodeUpdatePositionBody),
  });
};

export const getNodeUpdatePositionMutationFetcher = (
  nodeSlug: string,
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, { arg }: { arg: NodeUpdatePositionBody | undefined }) => {
    return nodeUpdatePosition(nodeSlug, arg, options);
  };
};
export const getNodeUpdatePositionMutationKey = (nodeSlug: string) =>
  [`/nodes/${nodeSlug}/position`] as const;

export type NodeUpdatePositionMutationResult = NonNullable<
  Awaited<ReturnType<typeof nodeUpdatePosition>>
>;

export const useNodeUpdatePosition = <
  TError =
    | BadRequestResponse
    | UnauthorisedResponse
    | NotFoundResponse
    | InternalServerErrorResponse,
>(
  nodeSlug: string,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof nodeUpdatePosition>>,
      TError,
      Key,
      NodeUpdatePositionBody | undefined,
      Awaited<ReturnType<typeof nodeUpdatePosition>>
    > & { swrKey?: string };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey =
    swrOptions?.swrKey ?? getNodeUpdatePositionMutationKey(nodeSlug);
  const swrFn = getNodeUpdatePositionMutationFetcher(nodeSlug, requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
