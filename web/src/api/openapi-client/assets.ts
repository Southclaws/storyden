/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * storyden
 * Storyden social API for building community driven platforms.
 * The Storyden API does not adhere to semantic versioning but instead applies a rolling strategy with deprecations and minimal breaking changes. This has been done mainly for a simpler development process and it may be changed to a more fixed versioning strategy in the future. Ultimately, the primary way Storyden tracks versions is dates, there are no set release tags currently.
 *
 * OpenAPI spec version: v1.26.15-post
 */
import useSwr from "swr";
import type { Key, SWRConfiguration } from "swr";
import useSWRMutation from "swr/mutation";
import type { SWRMutationConfiguration } from "swr/mutation";

import { fetcher } from "../client";
import type {
  AssetGetOKResponse,
  AssetUploadBody,
  AssetUploadOKResponse,
  AssetUploadParams,
  InternalServerErrorResponse,
  NotFoundResponse,
  UnauthorisedResponse,
} from "../openapi-schema";

type SecondParameter<T extends (...args: never) => unknown> = Parameters<T>[1];

export const getAssetUploadUrl = (params?: AssetUploadParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/assets?${stringifiedParams}`
    : `/assets`;
};

/**
 * Upload and process a media file.
 */
export const assetUpload = async (
  assetUploadBody?: AssetUploadBody,
  params?: AssetUploadParams,
  options?: Parameters<typeof fetcher>[1],
): Promise<AssetUploadOKResponse> => {
  return fetcher<AssetUploadOKResponse>(getAssetUploadUrl(params), {
    ...options,
    method: "POST",
    headers: {
      "Content-Type": "application/octet-stream",
      ...options?.headers,
    },
    body: assetUploadBody,
  });
};

export const getAssetUploadMutationFetcher = (
  params?: AssetUploadParams,
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, { arg }: { arg: AssetUploadBody | undefined }) => {
    return assetUpload(arg, params, options);
  };
};
export const getAssetUploadMutationKey = (params?: AssetUploadParams) =>
  [`/assets`, ...(params ? [params] : [])] as const;

export type AssetUploadMutationResult = NonNullable<
  Awaited<ReturnType<typeof assetUpload>>
>;

export const useAssetUpload = <
  TError = UnauthorisedResponse | InternalServerErrorResponse,
>(
  params?: AssetUploadParams,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof assetUpload>>,
      TError,
      Key,
      AssetUploadBody | undefined,
      Awaited<ReturnType<typeof assetUpload>>
    > & { swrKey?: string };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey = swrOptions?.swrKey ?? getAssetUploadMutationKey(params);
  const swrFn = getAssetUploadMutationFetcher(params, requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getAssetGetUrl = (assetFilename: string) => {
  return `/assets/${assetFilename}`;
};

/**
 * Download an asset by its ID.
 */
export const assetGet = async (
  assetFilename: string,
  options?: Parameters<typeof fetcher>[1],
): Promise<AssetGetOKResponse> => {
  return fetcher<AssetGetOKResponse>(getAssetGetUrl(assetFilename), {
    ...options,
    method: "GET",
  });
};

export const getAssetGetKey = (assetFilename: string) =>
  [`/assets/${assetFilename}`] as const;

export type AssetGetQueryResult = NonNullable<
  Awaited<ReturnType<typeof assetGet>>
>;

export const useAssetGet = <
  TError =
    UnauthorisedResponse | NotFoundResponse | InternalServerErrorResponse,
>(
  assetFilename: string,
  options?: {
    swr?: SWRConfiguration<Awaited<ReturnType<typeof assetGet>>, TError> & {
      swrKey?: Key;
      enabled?: boolean;
    };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const isEnabled =
    swrOptions?.enabled !== false &&
    assetFilename !== null &&
    assetFilename !== undefined;
  const swrKey =
    swrOptions?.swrKey ??
    (() => (isEnabled ? getAssetGetKey(assetFilename) : null));
  const swrFn = () => assetGet(assetFilename, requestOptions);

  const query = useSwr<Awaited<ReturnType<typeof swrFn>>, TError>(
    swrKey,
    swrFn,
    swrOptions,
  );

  return {
    swrKey,
    ...query,
  };
};
