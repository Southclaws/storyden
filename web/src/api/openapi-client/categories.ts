/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * storyden
 * Storyden social API for building community driven platforms.
 * The Storyden API does not adhere to semantic versioning but instead applies a rolling strategy with deprecations and minimal breaking changes. This has been done mainly for a simpler development process and it may be changed to a more fixed versioning strategy in the future. Ultimately, the primary way Storyden tracks versions is dates, there are no set release tags currently.
 *
 * OpenAPI spec version: v1.26.15-post
 */
import useSwr from "swr";
import type { Key, SWRConfiguration } from "swr";
import useSWRMutation from "swr/mutation";
import type { SWRMutationConfiguration } from "swr/mutation";

import { fetcher } from "../client";
import type {
  BadRequestResponse,
  CategoryCreateBody,
  CategoryCreateOKResponse,
  CategoryDeleteBody,
  CategoryDeleteOKResponse,
  CategoryGetOKResponse,
  CategoryListOKResponse,
  CategoryUpdateBody,
  CategoryUpdateOKResponse,
  CategoryUpdatePositionBody,
  InternalServerErrorResponse,
  NotFoundResponse,
  NotModifiedResponse,
  UnauthorisedResponse,
} from "../openapi-schema";

type SecondParameter<T extends (...args: never) => unknown> = Parameters<T>[1];

export const getCategoryCreateUrl = () => {
  return `/categories`;
};

/**
 * Create a category for organising posts.
 */
export const categoryCreate = async (
  categoryCreateBody?: CategoryCreateBody,
  options?: Parameters<typeof fetcher>[1],
): Promise<CategoryCreateOKResponse> => {
  return fetcher<CategoryCreateOKResponse>(getCategoryCreateUrl(), {
    ...options,
    method: "POST",
    headers: { "Content-Type": "application/json", ...options?.headers },
    body: JSON.stringify(categoryCreateBody),
  });
};

export const getCategoryCreateMutationFetcher = (
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, { arg }: { arg: CategoryCreateBody | undefined }) => {
    return categoryCreate(arg, options);
  };
};
export const getCategoryCreateMutationKey = () => [`/categories`] as const;

export type CategoryCreateMutationResult = NonNullable<
  Awaited<ReturnType<typeof categoryCreate>>
>;

export const useCategoryCreate = <
  TError =
    BadRequestResponse | UnauthorisedResponse | InternalServerErrorResponse,
>(options?: {
  swr?: SWRMutationConfiguration<
    Awaited<ReturnType<typeof categoryCreate>>,
    TError,
    Key,
    CategoryCreateBody | undefined,
    Awaited<ReturnType<typeof categoryCreate>>
  > & { swrKey?: string };
  request?: SecondParameter<typeof fetcher>;
}) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey = swrOptions?.swrKey ?? getCategoryCreateMutationKey();
  const swrFn = getCategoryCreateMutationFetcher(requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getCategoryListUrl = () => {
  return `/categories`;
};

/**
 * Get a list of all categories on the site.
 */
export const categoryList = async (
  options?: Parameters<typeof fetcher>[1],
): Promise<CategoryListOKResponse> => {
  return fetcher<CategoryListOKResponse>(getCategoryListUrl(), {
    ...options,
    method: "GET",
  });
};

export const getCategoryListKey = () => [`/categories`] as const;

export type CategoryListQueryResult = NonNullable<
  Awaited<ReturnType<typeof categoryList>>
>;

export const useCategoryList = <
  TError = InternalServerErrorResponse,
>(options?: {
  swr?: SWRConfiguration<Awaited<ReturnType<typeof categoryList>>, TError> & {
    swrKey?: Key;
    enabled?: boolean;
  };
  request?: SecondParameter<typeof fetcher>;
}) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const isEnabled = swrOptions?.enabled !== false;
  const swrKey =
    swrOptions?.swrKey ?? (() => (isEnabled ? getCategoryListKey() : null));
  const swrFn = () => categoryList(requestOptions);

  const query = useSwr<Awaited<ReturnType<typeof swrFn>>, TError>(
    swrKey,
    swrFn,
    swrOptions,
  );

  return {
    swrKey,
    ...query,
  };
};
export const getCategoryGetUrl = (categorySlug: string) => {
  return `/categories/${categorySlug}`;
};

/**
 * Get information about a category.
 */
export const categoryGet = async (
  categorySlug: string,
  options?: Parameters<typeof fetcher>[1],
): Promise<CategoryGetOKResponse> => {
  return fetcher<CategoryGetOKResponse>(getCategoryGetUrl(categorySlug), {
    ...options,
    method: "GET",
  });
};

export const getCategoryGetKey = (categorySlug: string) =>
  [`/categories/${categorySlug}`] as const;

export type CategoryGetQueryResult = NonNullable<
  Awaited<ReturnType<typeof categoryGet>>
>;

export const useCategoryGet = <
  TError = NotModifiedResponse | NotFoundResponse | InternalServerErrorResponse,
>(
  categorySlug: string,
  options?: {
    swr?: SWRConfiguration<Awaited<ReturnType<typeof categoryGet>>, TError> & {
      swrKey?: Key;
      enabled?: boolean;
    };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const isEnabled =
    swrOptions?.enabled !== false &&
    categorySlug !== null &&
    categorySlug !== undefined;
  const swrKey =
    swrOptions?.swrKey ??
    (() => (isEnabled ? getCategoryGetKey(categorySlug) : null));
  const swrFn = () => categoryGet(categorySlug, requestOptions);

  const query = useSwr<Awaited<ReturnType<typeof swrFn>>, TError>(
    swrKey,
    swrFn,
    swrOptions,
  );

  return {
    swrKey,
    ...query,
  };
};
export const getCategoryUpdateUrl = (categorySlug: string) => {
  return `/categories/${categorySlug}`;
};

/**
 * Update a category's information.
 */
export const categoryUpdate = async (
  categorySlug: string,
  categoryUpdateBody?: CategoryUpdateBody,
  options?: Parameters<typeof fetcher>[1],
): Promise<CategoryUpdateOKResponse> => {
  return fetcher<CategoryUpdateOKResponse>(getCategoryUpdateUrl(categorySlug), {
    ...options,
    method: "PATCH",
    headers: { "Content-Type": "application/json", ...options?.headers },
    body: JSON.stringify(categoryUpdateBody),
  });
};

export const getCategoryUpdateMutationFetcher = (
  categorySlug: string,
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, { arg }: { arg: CategoryUpdateBody | undefined }) => {
    return categoryUpdate(categorySlug, arg, options);
  };
};
export const getCategoryUpdateMutationKey = (categorySlug: string) =>
  [`/categories/${categorySlug}`] as const;

export type CategoryUpdateMutationResult = NonNullable<
  Awaited<ReturnType<typeof categoryUpdate>>
>;

export const useCategoryUpdate = <
  TError =
    BadRequestResponse | UnauthorisedResponse | InternalServerErrorResponse,
>(
  categorySlug: string,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof categoryUpdate>>,
      TError,
      Key,
      CategoryUpdateBody | undefined,
      Awaited<ReturnType<typeof categoryUpdate>>
    > & { swrKey?: string };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey =
    swrOptions?.swrKey ?? getCategoryUpdateMutationKey(categorySlug);
  const swrFn = getCategoryUpdateMutationFetcher(categorySlug, requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getCategoryDeleteUrl = (categorySlug: string) => {
  return `/categories/${categorySlug}`;
};

/**
 * Delete a category. All posts in this category will be moved to the specified target category.
 */
export const categoryDelete = async (
  categorySlug: string,
  categoryDeleteBody?: CategoryDeleteBody,
  options?: Parameters<typeof fetcher>[1],
): Promise<CategoryDeleteOKResponse> => {
  return fetcher<CategoryDeleteOKResponse>(getCategoryDeleteUrl(categorySlug), {
    ...options,
    method: "DELETE",
    headers: { "Content-Type": "application/json", ...options?.headers },
    body: JSON.stringify(categoryDeleteBody),
  });
};

export const getCategoryDeleteMutationFetcher = (
  categorySlug: string,
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, { arg }: { arg: CategoryDeleteBody | undefined }) => {
    return categoryDelete(categorySlug, arg, options);
  };
};
export const getCategoryDeleteMutationKey = (categorySlug: string) =>
  [`/categories/${categorySlug}`] as const;

export type CategoryDeleteMutationResult = NonNullable<
  Awaited<ReturnType<typeof categoryDelete>>
>;

export const useCategoryDelete = <
  TError =
    | BadRequestResponse
    | UnauthorisedResponse
    | NotFoundResponse
    | InternalServerErrorResponse,
>(
  categorySlug: string,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof categoryDelete>>,
      TError,
      Key,
      CategoryDeleteBody | undefined,
      Awaited<ReturnType<typeof categoryDelete>>
    > & { swrKey?: string };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey =
    swrOptions?.swrKey ?? getCategoryDeleteMutationKey(categorySlug);
  const swrFn = getCategoryDeleteMutationFetcher(categorySlug, requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getCategoryUpdatePositionUrl = (categorySlug: string) => {
  return `/categories/${categorySlug}/position`;
};

/**
 * Update the category's position in the tree. You may change the parent
 * using `parent`, and/or reposition the category among its siblings using
 * either `before` or `after`. Use this operation for drag-and-drop
 * interfaces.
 */
export const categoryUpdatePosition = async (
  categorySlug: string,
  categoryUpdatePositionBody?: CategoryUpdatePositionBody,
  options?: Parameters<typeof fetcher>[1],
): Promise<CategoryListOKResponse> => {
  return fetcher<CategoryListOKResponse>(
    getCategoryUpdatePositionUrl(categorySlug),
    {
      ...options,
      method: "PATCH",
      headers: { "Content-Type": "application/json", ...options?.headers },
      body: JSON.stringify(categoryUpdatePositionBody),
    },
  );
};

export const getCategoryUpdatePositionMutationFetcher = (
  categorySlug: string,
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, { arg }: { arg: CategoryUpdatePositionBody | undefined }) => {
    return categoryUpdatePosition(categorySlug, arg, options);
  };
};
export const getCategoryUpdatePositionMutationKey = (categorySlug: string) =>
  [`/categories/${categorySlug}/position`] as const;

export type CategoryUpdatePositionMutationResult = NonNullable<
  Awaited<ReturnType<typeof categoryUpdatePosition>>
>;

export const useCategoryUpdatePosition = <
  TError =
    | BadRequestResponse
    | UnauthorisedResponse
    | NotFoundResponse
    | InternalServerErrorResponse,
>(
  categorySlug: string,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof categoryUpdatePosition>>,
      TError,
      Key,
      CategoryUpdatePositionBody | undefined,
      Awaited<ReturnType<typeof categoryUpdatePosition>>
    > & { swrKey?: string };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey =
    swrOptions?.swrKey ?? getCategoryUpdatePositionMutationKey(categorySlug);
  const swrFn = getCategoryUpdatePositionMutationFetcher(
    categorySlug,
    requestOptions,
  );

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
