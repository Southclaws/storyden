/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * storyden
 * Storyden social API for building community driven platforms.
 * The Storyden API does not adhere to semantic versioning but instead applies a rolling strategy with deprecations and minimal breaking changes. This has been done mainly for a simpler development process and it may be changed to a more fixed versioning strategy in the future. Ultimately, the primary way Storyden tracks versions is dates, there are no set release tags currently.
 *
 * OpenAPI spec version: v1.26.15-post
 */
import useSwr from "swr";
import type { Arguments, Key, SWRConfiguration } from "swr";
import useSWRMutation from "swr/mutation";
import type { SWRMutationConfiguration } from "swr/mutation";

import { fetcher } from "../client";
import type {
  InternalServerErrorResponse,
  NotFoundResponse,
  NotModifiedResponse,
  ProfileFollowersGetOKResponse,
  ProfileFollowersGetParams,
  ProfileFollowingGetOKResponse,
  ProfileFollowingGetParams,
  ProfileGetOKResponse,
  ProfileListOKResponse,
  ProfileListParams,
  UnauthorisedResponse,
} from "../openapi-schema";

type SecondParameter<T extends (...args: never) => unknown> = Parameters<T>[1];

export const getProfileListUrl = (params?: ProfileListParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    const explodeParameters = ["roles", "invited_by"];

    if (Array.isArray(value) && explodeParameters.includes(key)) {
      value.forEach((v) => {
        normalizedParams.append(key, v === null ? "null" : String(v));
      });
      return;
    }

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/profiles?${stringifiedParams}`
    : `/profiles`;
};

/**
 * Query and search profiles.
 */
export const profileList = async (
  params?: ProfileListParams,
  options?: Parameters<typeof fetcher>[1],
): Promise<ProfileListOKResponse> => {
  return fetcher<ProfileListOKResponse>(getProfileListUrl(params), {
    ...options,
    method: "GET",
  });
};

export const getProfileListKey = (params?: ProfileListParams) =>
  [`/profiles`, ...(params ? [params] : [])] as const;

export type ProfileListQueryResult = NonNullable<
  Awaited<ReturnType<typeof profileList>>
>;

export const useProfileList = <
  TError =
    UnauthorisedResponse | NotFoundResponse | InternalServerErrorResponse,
>(
  params?: ProfileListParams,
  options?: {
    swr?: SWRConfiguration<Awaited<ReturnType<typeof profileList>>, TError> & {
      swrKey?: Key;
      enabled?: boolean;
    };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const isEnabled = swrOptions?.enabled !== false;
  const swrKey =
    swrOptions?.swrKey ??
    (() => (isEnabled ? getProfileListKey(params) : null));
  const swrFn = () => profileList(params, requestOptions);

  const query = useSwr<Awaited<ReturnType<typeof swrFn>>, TError>(
    swrKey,
    swrFn,
    swrOptions,
  );

  return {
    swrKey,
    ...query,
  };
};
export const getProfileGetUrl = (accountHandle: string) => {
  return `/profiles/${accountHandle}`;
};

/**
 * Get a public profile by ID.
 */
export const profileGet = async (
  accountHandle: string,
  options?: Parameters<typeof fetcher>[1],
): Promise<ProfileGetOKResponse> => {
  return fetcher<ProfileGetOKResponse>(getProfileGetUrl(accountHandle), {
    ...options,
    method: "GET",
  });
};

export const getProfileGetKey = (accountHandle: string) =>
  [`/profiles/${accountHandle}`] as const;

export type ProfileGetQueryResult = NonNullable<
  Awaited<ReturnType<typeof profileGet>>
>;

export const useProfileGet = <
  TError =
    | NotModifiedResponse
    | UnauthorisedResponse
    | NotFoundResponse
    | InternalServerErrorResponse,
>(
  accountHandle: string,
  options?: {
    swr?: SWRConfiguration<Awaited<ReturnType<typeof profileGet>>, TError> & {
      swrKey?: Key;
      enabled?: boolean;
    };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const isEnabled =
    swrOptions?.enabled !== false &&
    accountHandle !== null &&
    accountHandle !== undefined;
  const swrKey =
    swrOptions?.swrKey ??
    (() => (isEnabled ? getProfileGetKey(accountHandle) : null));
  const swrFn = () => profileGet(accountHandle, requestOptions);

  const query = useSwr<Awaited<ReturnType<typeof swrFn>>, TError>(
    swrKey,
    swrFn,
    swrOptions,
  );

  return {
    swrKey,
    ...query,
  };
};
export const getProfileFollowersGetUrl = (
  accountHandle: string,
  params?: ProfileFollowersGetParams,
) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/profiles/${accountHandle}/followers?${stringifiedParams}`
    : `/profiles/${accountHandle}/followers`;
};

/**
 * Get the followers and following details for a profile.
 */
export const profileFollowersGet = async (
  accountHandle: string,
  params?: ProfileFollowersGetParams,
  options?: Parameters<typeof fetcher>[1],
): Promise<ProfileFollowersGetOKResponse> => {
  return fetcher<ProfileFollowersGetOKResponse>(
    getProfileFollowersGetUrl(accountHandle, params),
    {
      ...options,
      method: "GET",
    },
  );
};

export const getProfileFollowersGetKey = (
  accountHandle: string,
  params?: ProfileFollowersGetParams,
) =>
  [
    `/profiles/${accountHandle}/followers`,
    ...(params ? [params] : []),
  ] as const;

export type ProfileFollowersGetQueryResult = NonNullable<
  Awaited<ReturnType<typeof profileFollowersGet>>
>;

export const useProfileFollowersGet = <
  TError =
    UnauthorisedResponse | NotFoundResponse | InternalServerErrorResponse,
>(
  accountHandle: string,
  params?: ProfileFollowersGetParams,
  options?: {
    swr?: SWRConfiguration<
      Awaited<ReturnType<typeof profileFollowersGet>>,
      TError
    > & { swrKey?: Key; enabled?: boolean };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const isEnabled =
    swrOptions?.enabled !== false &&
    accountHandle !== null &&
    accountHandle !== undefined;
  const swrKey =
    swrOptions?.swrKey ??
    (() =>
      isEnabled ? getProfileFollowersGetKey(accountHandle, params) : null);
  const swrFn = () =>
    profileFollowersGet(accountHandle, params, requestOptions);

  const query = useSwr<Awaited<ReturnType<typeof swrFn>>, TError>(
    swrKey,
    swrFn,
    swrOptions,
  );

  return {
    swrKey,
    ...query,
  };
};
export const getProfileFollowersAddUrl = (accountHandle: string) => {
  return `/profiles/${accountHandle}/followers`;
};

/**
 * Follow the specified profile as the authenticated account.
 */
export const profileFollowersAdd = async (
  accountHandle: string,
  options?: Parameters<typeof fetcher>[1],
): Promise<void> => {
  return fetcher<void>(getProfileFollowersAddUrl(accountHandle), {
    ...options,
    method: "PUT",
  });
};

export const getProfileFollowersAddMutationFetcher = (
  accountHandle: string,
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, __: { arg: Arguments }) => {
    return profileFollowersAdd(accountHandle, options);
  };
};
export const getProfileFollowersAddMutationKey = (accountHandle: string) =>
  [`/profiles/${accountHandle}/followers`] as const;

export type ProfileFollowersAddMutationResult = NonNullable<
  Awaited<ReturnType<typeof profileFollowersAdd>>
>;

export const useProfileFollowersAdd = <
  TError =
    UnauthorisedResponse | NotFoundResponse | InternalServerErrorResponse,
>(
  accountHandle: string,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof profileFollowersAdd>>,
      TError,
      Key,
      Arguments,
      Awaited<ReturnType<typeof profileFollowersAdd>>
    > & { swrKey?: string };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey =
    swrOptions?.swrKey ?? getProfileFollowersAddMutationKey(accountHandle);
  const swrFn = getProfileFollowersAddMutationFetcher(
    accountHandle,
    requestOptions,
  );

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getProfileFollowersRemoveUrl = (accountHandle: string) => {
  return `/profiles/${accountHandle}/followers`;
};

/**
 * Unfollow the specified profile as the authenticated account.
 */
export const profileFollowersRemove = async (
  accountHandle: string,
  options?: Parameters<typeof fetcher>[1],
): Promise<void> => {
  return fetcher<void>(getProfileFollowersRemoveUrl(accountHandle), {
    ...options,
    method: "DELETE",
  });
};

export const getProfileFollowersRemoveMutationFetcher = (
  accountHandle: string,
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, __: { arg: Arguments }) => {
    return profileFollowersRemove(accountHandle, options);
  };
};
export const getProfileFollowersRemoveMutationKey = (accountHandle: string) =>
  [`/profiles/${accountHandle}/followers`] as const;

export type ProfileFollowersRemoveMutationResult = NonNullable<
  Awaited<ReturnType<typeof profileFollowersRemove>>
>;

export const useProfileFollowersRemove = <
  TError =
    UnauthorisedResponse | NotFoundResponse | InternalServerErrorResponse,
>(
  accountHandle: string,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof profileFollowersRemove>>,
      TError,
      Key,
      Arguments,
      Awaited<ReturnType<typeof profileFollowersRemove>>
    > & { swrKey?: string };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey =
    swrOptions?.swrKey ?? getProfileFollowersRemoveMutationKey(accountHandle);
  const swrFn = getProfileFollowersRemoveMutationFetcher(
    accountHandle,
    requestOptions,
  );

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getProfileFollowingGetUrl = (
  accountHandle: string,
  params?: ProfileFollowingGetParams,
) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/profiles/${accountHandle}/following?${stringifiedParams}`
    : `/profiles/${accountHandle}/following`;
};

/**
 * Get the profiles that this account is following.
 */
export const profileFollowingGet = async (
  accountHandle: string,
  params?: ProfileFollowingGetParams,
  options?: Parameters<typeof fetcher>[1],
): Promise<ProfileFollowingGetOKResponse> => {
  return fetcher<ProfileFollowingGetOKResponse>(
    getProfileFollowingGetUrl(accountHandle, params),
    {
      ...options,
      method: "GET",
    },
  );
};

export const getProfileFollowingGetKey = (
  accountHandle: string,
  params?: ProfileFollowingGetParams,
) =>
  [
    `/profiles/${accountHandle}/following`,
    ...(params ? [params] : []),
  ] as const;

export type ProfileFollowingGetQueryResult = NonNullable<
  Awaited<ReturnType<typeof profileFollowingGet>>
>;

export const useProfileFollowingGet = <
  TError =
    | NotModifiedResponse
    | UnauthorisedResponse
    | NotFoundResponse
    | InternalServerErrorResponse,
>(
  accountHandle: string,
  params?: ProfileFollowingGetParams,
  options?: {
    swr?: SWRConfiguration<
      Awaited<ReturnType<typeof profileFollowingGet>>,
      TError
    > & { swrKey?: Key; enabled?: boolean };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const isEnabled =
    swrOptions?.enabled !== false &&
    accountHandle !== null &&
    accountHandle !== undefined;
  const swrKey =
    swrOptions?.swrKey ??
    (() =>
      isEnabled ? getProfileFollowingGetKey(accountHandle, params) : null);
  const swrFn = () =>
    profileFollowingGet(accountHandle, params, requestOptions);

  const query = useSwr<Awaited<ReturnType<typeof swrFn>>, TError>(
    swrKey,
    swrFn,
    swrOptions,
  );

  return {
    swrKey,
    ...query,
  };
};
