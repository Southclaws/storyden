/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * storyden
 * Storyden social API for building community driven platforms.
 * The Storyden API does not adhere to semantic versioning but instead applies a rolling strategy with deprecations and minimal breaking changes. This has been done mainly for a simpler development process and it may be changed to a more fixed versioning strategy in the future. Ultimately, the primary way Storyden tracks versions is dates, there are no set release tags currently.
 *
 * OpenAPI spec version: v1.26.15-post
 */
import useSwr from "swr";
import type { Arguments, Key, SWRConfiguration } from "swr";
import useSWRMutation from "swr/mutation";
import type { SWRMutationConfiguration } from "swr/mutation";

import { fetcher } from "../client";
import type {
  InternalServerErrorResponse,
  NotFoundResponse,
  NotModifiedResponse,
  ThreadCreateBody,
  ThreadCreateOKResponse,
  ThreadGetParams,
  ThreadGetResponse,
  ThreadListOKResponse,
  ThreadListParams,
  ThreadUpdateBody,
  ThreadUpdateOKResponse,
  UnauthorisedResponse,
} from "../openapi-schema";

type SecondParameter<T extends (...args: never) => unknown> = Parameters<T>[1];

export const getThreadCreateUrl = () => {
  return `/threads`;
};

/**
 * Create a new thread within the specified category.
 */
export const threadCreate = async (
  threadCreateBody?: ThreadCreateBody,
  options?: Parameters<typeof fetcher>[1],
): Promise<ThreadCreateOKResponse> => {
  return fetcher<ThreadCreateOKResponse>(getThreadCreateUrl(), {
    ...options,
    method: "POST",
    headers: { "Content-Type": "application/json", ...options?.headers },
    body: JSON.stringify(threadCreateBody),
  });
};

export const getThreadCreateMutationFetcher = (
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, { arg }: { arg: ThreadCreateBody | undefined }) => {
    return threadCreate(arg, options);
  };
};
export const getThreadCreateMutationKey = () => [`/threads`] as const;

export type ThreadCreateMutationResult = NonNullable<
  Awaited<ReturnType<typeof threadCreate>>
>;

export const useThreadCreate = <
  TError =
    UnauthorisedResponse | NotFoundResponse | InternalServerErrorResponse,
>(options?: {
  swr?: SWRMutationConfiguration<
    Awaited<ReturnType<typeof threadCreate>>,
    TError,
    Key,
    ThreadCreateBody | undefined,
    Awaited<ReturnType<typeof threadCreate>>
  > & { swrKey?: string };
  request?: SecondParameter<typeof fetcher>;
}) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey = swrOptions?.swrKey ?? getThreadCreateMutationKey();
  const swrFn = getThreadCreateMutationFetcher(requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getThreadListUrl = (params?: ThreadListParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    const explodeParameters = ["visibility", "tags", "categories"];

    if (Array.isArray(value) && explodeParameters.includes(key)) {
      value.forEach((v) => {
        normalizedParams.append(key, v === null ? "null" : String(v));
      });
      return;
    }

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/threads?${stringifiedParams}`
    : `/threads`;
};

/**
 * Get a list of all threads.
 */
export const threadList = async (
  params?: ThreadListParams,
  options?: Parameters<typeof fetcher>[1],
): Promise<ThreadListOKResponse> => {
  return fetcher<ThreadListOKResponse>(getThreadListUrl(params), {
    ...options,
    method: "GET",
  });
};

export const getThreadListKey = (params?: ThreadListParams) =>
  [`/threads`, ...(params ? [params] : [])] as const;

export type ThreadListQueryResult = NonNullable<
  Awaited<ReturnType<typeof threadList>>
>;

export const useThreadList = <
  TError =
    UnauthorisedResponse | NotFoundResponse | InternalServerErrorResponse,
>(
  params?: ThreadListParams,
  options?: {
    swr?: SWRConfiguration<Awaited<ReturnType<typeof threadList>>, TError> & {
      swrKey?: Key;
      enabled?: boolean;
    };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const isEnabled = swrOptions?.enabled !== false;
  const swrKey =
    swrOptions?.swrKey ?? (() => (isEnabled ? getThreadListKey(params) : null));
  const swrFn = () => threadList(params, requestOptions);

  const query = useSwr<Awaited<ReturnType<typeof swrFn>>, TError>(
    swrKey,
    swrFn,
    swrOptions,
  );

  return {
    swrKey,
    ...query,
  };
};
export const getThreadGetUrl = (
  threadMark: string,
  params?: ThreadGetParams,
) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/threads/${threadMark}?${stringifiedParams}`
    : `/threads/${threadMark}`;
};

/**
 * Get information about a thread such as its title, author, when it was
 * created as well as a list of the posts within the thread.
 * @summary Get information about a thread and the posts within the thread.
 */
export const threadGet = async (
  threadMark: string,
  params?: ThreadGetParams,
  options?: Parameters<typeof fetcher>[1],
): Promise<ThreadGetResponse> => {
  return fetcher<ThreadGetResponse>(getThreadGetUrl(threadMark, params), {
    ...options,
    method: "GET",
  });
};

export const getThreadGetKey = (threadMark: string, params?: ThreadGetParams) =>
  [`/threads/${threadMark}`, ...(params ? [params] : [])] as const;

export type ThreadGetQueryResult = NonNullable<
  Awaited<ReturnType<typeof threadGet>>
>;

/**
 * @summary Get information about a thread and the posts within the thread.
 */
export const useThreadGet = <
  TError =
    | NotModifiedResponse
    | UnauthorisedResponse
    | NotFoundResponse
    | InternalServerErrorResponse,
>(
  threadMark: string,
  params?: ThreadGetParams,
  options?: {
    swr?: SWRConfiguration<Awaited<ReturnType<typeof threadGet>>, TError> & {
      swrKey?: Key;
      enabled?: boolean;
    };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const isEnabled =
    swrOptions?.enabled !== false &&
    threadMark !== null &&
    threadMark !== undefined;
  const swrKey =
    swrOptions?.swrKey ??
    (() => (isEnabled ? getThreadGetKey(threadMark, params) : null));
  const swrFn = () => threadGet(threadMark, params, requestOptions);

  const query = useSwr<Awaited<ReturnType<typeof swrFn>>, TError>(
    swrKey,
    swrFn,
    swrOptions,
  );

  return {
    swrKey,
    ...query,
  };
};
export const getThreadUpdateUrl = (threadMark: string) => {
  return `/threads/${threadMark}`;
};

/**
 * Publish changes to a thread.
 */
export const threadUpdate = async (
  threadMark: string,
  threadUpdateBody?: ThreadUpdateBody,
  options?: Parameters<typeof fetcher>[1],
): Promise<ThreadUpdateOKResponse> => {
  return fetcher<ThreadUpdateOKResponse>(getThreadUpdateUrl(threadMark), {
    ...options,
    method: "PATCH",
    headers: { "Content-Type": "application/json", ...options?.headers },
    body: JSON.stringify(threadUpdateBody),
  });
};

export const getThreadUpdateMutationFetcher = (
  threadMark: string,
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, { arg }: { arg: ThreadUpdateBody | undefined }) => {
    return threadUpdate(threadMark, arg, options);
  };
};
export const getThreadUpdateMutationKey = (threadMark: string) =>
  [`/threads/${threadMark}`] as const;

export type ThreadUpdateMutationResult = NonNullable<
  Awaited<ReturnType<typeof threadUpdate>>
>;

export const useThreadUpdate = <
  TError =
    UnauthorisedResponse | NotFoundResponse | InternalServerErrorResponse,
>(
  threadMark: string,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof threadUpdate>>,
      TError,
      Key,
      ThreadUpdateBody | undefined,
      Awaited<ReturnType<typeof threadUpdate>>
    > & { swrKey?: string };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey = swrOptions?.swrKey ?? getThreadUpdateMutationKey(threadMark);
  const swrFn = getThreadUpdateMutationFetcher(threadMark, requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getThreadDeleteUrl = (threadMark: string) => {
  return `/threads/${threadMark}`;
};

/**
 * Archive a thread using soft-delete.
 */
export const threadDelete = async (
  threadMark: string,
  options?: Parameters<typeof fetcher>[1],
): Promise<void> => {
  return fetcher<void>(getThreadDeleteUrl(threadMark), {
    ...options,
    method: "DELETE",
  });
};

export const getThreadDeleteMutationFetcher = (
  threadMark: string,
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, __: { arg: Arguments }) => {
    return threadDelete(threadMark, options);
  };
};
export const getThreadDeleteMutationKey = (threadMark: string) =>
  [`/threads/${threadMark}`] as const;

export type ThreadDeleteMutationResult = NonNullable<
  Awaited<ReturnType<typeof threadDelete>>
>;

export const useThreadDelete = <
  TError =
    UnauthorisedResponse | NotFoundResponse | InternalServerErrorResponse,
>(
  threadMark: string,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof threadDelete>>,
      TError,
      Key,
      Arguments,
      Awaited<ReturnType<typeof threadDelete>>
    > & { swrKey?: string };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey = swrOptions?.swrKey ?? getThreadDeleteMutationKey(threadMark);
  const swrFn = getThreadDeleteMutationFetcher(threadMark, requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
