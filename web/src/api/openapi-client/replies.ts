/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * storyden
 * Storyden social API for building community driven platforms.
 * The Storyden API does not adhere to semantic versioning but instead applies a rolling strategy with deprecations and minimal breaking changes. This has been done mainly for a simpler development process and it may be changed to a more fixed versioning strategy in the future. Ultimately, the primary way Storyden tracks versions is dates, there are no set release tags currently.
 *
 * OpenAPI spec version: v1.26.15-post
 */
import type { Key } from "swr";
import useSWRMutation from "swr/mutation";
import type { SWRMutationConfiguration } from "swr/mutation";

import { fetcher } from "../client";
import type {
  InternalServerErrorResponse,
  NotFoundResponse,
  ReplyCreateBody,
  ReplyCreateOKResponse,
  UnauthorisedResponse,
} from "../openapi-schema";

type SecondParameter<T extends (...args: never) => unknown> = Parameters<T>[1];

export const getReplyCreateUrl = (threadMark: string) => {
  return `/threads/${threadMark}/replies`;
};

/**
 * Create a new post within a thread.
 */
export const replyCreate = async (
  threadMark: string,
  replyCreateBody?: ReplyCreateBody,
  options?: Parameters<typeof fetcher>[1],
): Promise<ReplyCreateOKResponse> => {
  return fetcher<ReplyCreateOKResponse>(getReplyCreateUrl(threadMark), {
    ...options,
    method: "POST",
    headers: { "Content-Type": "application/json", ...options?.headers },
    body: JSON.stringify(replyCreateBody),
  });
};

export const getReplyCreateMutationFetcher = (
  threadMark: string,
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, { arg }: { arg: ReplyCreateBody | undefined }) => {
    return replyCreate(threadMark, arg, options);
  };
};
export const getReplyCreateMutationKey = (threadMark: string) =>
  [`/threads/${threadMark}/replies`] as const;

export type ReplyCreateMutationResult = NonNullable<
  Awaited<ReturnType<typeof replyCreate>>
>;

export const useReplyCreate = <
  TError =
    UnauthorisedResponse | NotFoundResponse | InternalServerErrorResponse,
>(
  threadMark: string,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof replyCreate>>,
      TError,
      Key,
      ReplyCreateBody | undefined,
      Awaited<ReturnType<typeof replyCreate>>
    > & { swrKey?: string };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey = swrOptions?.swrKey ?? getReplyCreateMutationKey(threadMark);
  const swrFn = getReplyCreateMutationFetcher(threadMark, requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
