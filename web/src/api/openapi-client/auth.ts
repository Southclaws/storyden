/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * storyden
 * Storyden social API for building community driven platforms.
 * The Storyden API does not adhere to semantic versioning but instead applies a rolling strategy with deprecations and minimal breaking changes. This has been done mainly for a simpler development process and it may be changed to a more fixed versioning strategy in the future. Ultimately, the primary way Storyden tracks versions is dates, there are no set release tags currently.
 *
 * OpenAPI spec version: v1.26.15-post
 */
import useSwr from "swr";
import type { Arguments, Key, SWRConfiguration } from "swr";
import useSWRMutation from "swr/mutation";
import type { SWRMutationConfiguration } from "swr/mutation";

import { fetcher } from "../client";
import type {
  AccessKeyCreateBody,
  AccessKeyCreateOKResponse,
  AccessKeyListOKResponse,
  AuthEmailBody,
  AuthEmailPasswordBody,
  AuthEmailPasswordResetBody,
  AuthEmailPasswordSignupParams,
  AuthEmailSignupParams,
  AuthEmailVerifyBody,
  AuthPasswordBody,
  AuthPasswordCreateBody,
  AuthPasswordResetBody,
  AuthPasswordSignupParams,
  AuthPasswordUpdateBody,
  AuthProviderListOKResponse,
  AuthProviderLogoutParams,
  AuthSuccessOKResponse,
  BadRequestResponse,
  ConflictResponse,
  ForbiddenResponse,
  InternalServerErrorResponse,
  NoContentResponse,
  NotFoundResponse,
  OAuthAuthoriseConsentOKResponse,
  OAuthAuthoriseConsentParams,
  OAuthAuthoriseConsentSubmitBody,
  OAuthAuthoriseConsentSubmitOKResponse,
  OAuthAuthoriseFoundResponse,
  OAuthAuthoriseOKResponse,
  OAuthAuthoriseParams,
  OAuthClientIssuedOKResponse,
  OAuthClientListOKResponse,
  OAuthClientOKResponse,
  OAuthClientRegisterBody,
  OAuthClientRegisterErrorResponse,
  OAuthClientRegisterOKResponse,
  OAuthClientSelfCreateBody,
  OAuthClientSelfUpdateBody,
  OAuthDeviceAuthorisationBody,
  OAuthDeviceAuthorisationOKResponse,
  OAuthDeviceConsentOKResponse,
  OAuthDeviceConsentParams,
  OAuthDeviceConsentSubmitBody,
  OAuthDeviceConsentSubmitOKResponse,
  OAuthErrorResponse,
  OAuthJWKSOKResponse,
  OAuthProviderCallbackBody,
  OAuthRefreshTokenListOKResponse,
  OAuthRefreshTokenListParams,
  OAuthRemoteCallbackOKResponse,
  OAuthRemoteCallbackParams,
  OAuthTokenBody,
  OAuthTokenErrorResponse,
  OAuthTokenOKResponse,
  OAuthTokenUnauthorisedResponse,
  OAuthUserInfoOKResponse,
  PhoneRequestCodeBody,
  PhoneRequestCodeParams,
  PhoneSubmitCodeBody,
  UnauthorisedResponse,
  WebAuthnGetAssertionOKResponse,
  WebAuthnMakeAssertionBody,
  WebAuthnMakeCredentialBody,
  WebAuthnMakeCredentialParams,
  WebAuthnRequestCredentialOKResponse,
} from "../openapi-schema";

type SecondParameter<T extends (...args: never) => unknown> = Parameters<T>[1];

export const getAuthProviderListUrl = () => {
  return `/auth`;
};

/**
 * Retrieve a list of authentication providers. Storyden supports a few
 * ways to authenticate, from simple passwords to OAuth and WebAuthn. This
 * endpoint tells a client which auth capabilities are enabled.
 */
export const authProviderList = async (
  options?: Parameters<typeof fetcher>[1],
): Promise<AuthProviderListOKResponse> => {
  return fetcher<AuthProviderListOKResponse>(getAuthProviderListUrl(), {
    ...options,
    method: "GET",
  });
};

export const getAuthProviderListKey = () => [`/auth`] as const;

export type AuthProviderListQueryResult = NonNullable<
  Awaited<ReturnType<typeof authProviderList>>
>;

export const useAuthProviderList = <
  TError = BadRequestResponse | InternalServerErrorResponse,
>(options?: {
  swr?: SWRConfiguration<
    Awaited<ReturnType<typeof authProviderList>>,
    TError
  > & { swrKey?: Key; enabled?: boolean };
  request?: SecondParameter<typeof fetcher>;
}) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const isEnabled = swrOptions?.enabled !== false;
  const swrKey =
    swrOptions?.swrKey ?? (() => (isEnabled ? getAuthProviderListKey() : null));
  const swrFn = () => authProviderList(requestOptions);

  const query = useSwr<Awaited<ReturnType<typeof swrFn>>, TError>(
    swrKey,
    swrFn,
    swrOptions,
  );

  return {
    swrKey,
    ...query,
  };
};
export const getAuthPasswordSignupUrl = (params?: AuthPasswordSignupParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/auth/password/signup?${stringifiedParams}`
    : `/auth/password/signup`;
};

/**
 * Register a new account with a username and password.
 */
export const authPasswordSignup = async (
  authPasswordBody?: AuthPasswordBody,
  params?: AuthPasswordSignupParams,
  options?: Parameters<typeof fetcher>[1],
): Promise<AuthSuccessOKResponse> => {
  return fetcher<AuthSuccessOKResponse>(getAuthPasswordSignupUrl(params), {
    ...options,
    method: "POST",
    headers: { "Content-Type": "application/json", ...options?.headers },
    body: JSON.stringify(authPasswordBody),
  });
};

export const getAuthPasswordSignupMutationFetcher = (
  params?: AuthPasswordSignupParams,
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, { arg }: { arg: AuthPasswordBody | undefined }) => {
    return authPasswordSignup(arg, params, options);
  };
};
export const getAuthPasswordSignupMutationKey = (
  params?: AuthPasswordSignupParams,
) => [`/auth/password/signup`, ...(params ? [params] : [])] as const;

export type AuthPasswordSignupMutationResult = NonNullable<
  Awaited<ReturnType<typeof authPasswordSignup>>
>;

export const useAuthPasswordSignup = <
  TError = BadRequestResponse | ConflictResponse | InternalServerErrorResponse,
>(
  params?: AuthPasswordSignupParams,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof authPasswordSignup>>,
      TError,
      Key,
      AuthPasswordBody | undefined,
      Awaited<ReturnType<typeof authPasswordSignup>>
    > & { swrKey?: string };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey = swrOptions?.swrKey ?? getAuthPasswordSignupMutationKey(params);
  const swrFn = getAuthPasswordSignupMutationFetcher(params, requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getAuthPasswordSigninUrl = () => {
  return `/auth/password/signin`;
};

/**
 * Sign in to an existing account with a username and password.
 */
export const authPasswordSignin = async (
  authPasswordBody?: AuthPasswordBody,
  options?: Parameters<typeof fetcher>[1],
): Promise<AuthSuccessOKResponse> => {
  return fetcher<AuthSuccessOKResponse>(getAuthPasswordSigninUrl(), {
    ...options,
    method: "POST",
    headers: { "Content-Type": "application/json", ...options?.headers },
    body: JSON.stringify(authPasswordBody),
  });
};

export const getAuthPasswordSigninMutationFetcher = (
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, { arg }: { arg: AuthPasswordBody | undefined }) => {
    return authPasswordSignin(arg, options);
  };
};
export const getAuthPasswordSigninMutationKey = () =>
  [`/auth/password/signin`] as const;

export type AuthPasswordSigninMutationResult = NonNullable<
  Awaited<ReturnType<typeof authPasswordSignin>>
>;

export const useAuthPasswordSignin = <
  TError =
    | UnauthorisedResponse
    | ForbiddenResponse
    | NotFoundResponse
    | InternalServerErrorResponse,
>(options?: {
  swr?: SWRMutationConfiguration<
    Awaited<ReturnType<typeof authPasswordSignin>>,
    TError,
    Key,
    AuthPasswordBody | undefined,
    Awaited<ReturnType<typeof authPasswordSignin>>
  > & { swrKey?: string };
  request?: SecondParameter<typeof fetcher>;
}) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey = swrOptions?.swrKey ?? getAuthPasswordSigninMutationKey();
  const swrFn = getAuthPasswordSigninMutationFetcher(requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getAuthPasswordCreateUrl = () => {
  return `/auth/password`;
};

/**
 * Given the requesting account does not have a password authentication,
 * add a password authentication method to it with the given password.
 */
export const authPasswordCreate = async (
  authPasswordCreateBody?: AuthPasswordCreateBody,
  options?: Parameters<typeof fetcher>[1],
): Promise<AuthSuccessOKResponse> => {
  return fetcher<AuthSuccessOKResponse>(getAuthPasswordCreateUrl(), {
    ...options,
    method: "POST",
    headers: { "Content-Type": "application/json", ...options?.headers },
    body: JSON.stringify(authPasswordCreateBody),
  });
};

export const getAuthPasswordCreateMutationFetcher = (
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, { arg }: { arg: AuthPasswordCreateBody | undefined }) => {
    return authPasswordCreate(arg, options);
  };
};
export const getAuthPasswordCreateMutationKey = () =>
  [`/auth/password`] as const;

export type AuthPasswordCreateMutationResult = NonNullable<
  Awaited<ReturnType<typeof authPasswordCreate>>
>;

export const useAuthPasswordCreate = <
  TError =
    UnauthorisedResponse | NotFoundResponse | InternalServerErrorResponse,
>(options?: {
  swr?: SWRMutationConfiguration<
    Awaited<ReturnType<typeof authPasswordCreate>>,
    TError,
    Key,
    AuthPasswordCreateBody | undefined,
    Awaited<ReturnType<typeof authPasswordCreate>>
  > & { swrKey?: string };
  request?: SecondParameter<typeof fetcher>;
}) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey = swrOptions?.swrKey ?? getAuthPasswordCreateMutationKey();
  const swrFn = getAuthPasswordCreateMutationFetcher(requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getAuthPasswordUpdateUrl = () => {
  return `/auth/password`;
};

/**
 * Given the requesting account has a password authentication, update the
 * password on file.
 */
export const authPasswordUpdate = async (
  authPasswordUpdateBody?: AuthPasswordUpdateBody,
  options?: Parameters<typeof fetcher>[1],
): Promise<AuthSuccessOKResponse> => {
  return fetcher<AuthSuccessOKResponse>(getAuthPasswordUpdateUrl(), {
    ...options,
    method: "PATCH",
    headers: { "Content-Type": "application/json", ...options?.headers },
    body: JSON.stringify(authPasswordUpdateBody),
  });
};

export const getAuthPasswordUpdateMutationFetcher = (
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, { arg }: { arg: AuthPasswordUpdateBody | undefined }) => {
    return authPasswordUpdate(arg, options);
  };
};
export const getAuthPasswordUpdateMutationKey = () =>
  [`/auth/password`] as const;

export type AuthPasswordUpdateMutationResult = NonNullable<
  Awaited<ReturnType<typeof authPasswordUpdate>>
>;

export const useAuthPasswordUpdate = <
  TError =
    UnauthorisedResponse | NotFoundResponse | InternalServerErrorResponse,
>(options?: {
  swr?: SWRMutationConfiguration<
    Awaited<ReturnType<typeof authPasswordUpdate>>,
    TError,
    Key,
    AuthPasswordUpdateBody | undefined,
    Awaited<ReturnType<typeof authPasswordUpdate>>
  > & { swrKey?: string };
  request?: SecondParameter<typeof fetcher>;
}) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey = swrOptions?.swrKey ?? getAuthPasswordUpdateMutationKey();
  const swrFn = getAuthPasswordUpdateMutationFetcher(requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getAuthPasswordResetUrl = () => {
  return `/auth/password/reset`;
};

/**
 * Complete a password-reset flow using a token that was provided to the
 * member via a reset request operation such as `AuthEmailPasswordReset`.
 */
export const authPasswordReset = async (
  authPasswordResetBody?: AuthPasswordResetBody,
  options?: Parameters<typeof fetcher>[1],
): Promise<AuthSuccessOKResponse> => {
  return fetcher<AuthSuccessOKResponse>(getAuthPasswordResetUrl(), {
    ...options,
    method: "POST",
    headers: { "Content-Type": "application/json", ...options?.headers },
    body: JSON.stringify(authPasswordResetBody),
  });
};

export const getAuthPasswordResetMutationFetcher = (
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, { arg }: { arg: AuthPasswordResetBody | undefined }) => {
    return authPasswordReset(arg, options);
  };
};
export const getAuthPasswordResetMutationKey = () =>
  [`/auth/password/reset`] as const;

export type AuthPasswordResetMutationResult = NonNullable<
  Awaited<ReturnType<typeof authPasswordReset>>
>;

export const useAuthPasswordReset = <
  TError =
    | UnauthorisedResponse
    | ForbiddenResponse
    | NotFoundResponse
    | InternalServerErrorResponse,
>(options?: {
  swr?: SWRMutationConfiguration<
    Awaited<ReturnType<typeof authPasswordReset>>,
    TError,
    Key,
    AuthPasswordResetBody | undefined,
    Awaited<ReturnType<typeof authPasswordReset>>
  > & { swrKey?: string };
  request?: SecondParameter<typeof fetcher>;
}) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey = swrOptions?.swrKey ?? getAuthPasswordResetMutationKey();
  const swrFn = getAuthPasswordResetMutationFetcher(requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getAuthEmailPasswordSignupUrl = (
  params?: AuthEmailPasswordSignupParams,
) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/auth/email-password/signup?${stringifiedParams}`
    : `/auth/email-password/signup`;
};

/**
 * Register a new account with a email and password.
 */
export const authEmailPasswordSignup = async (
  authEmailPasswordBody?: AuthEmailPasswordBody,
  params?: AuthEmailPasswordSignupParams,
  options?: Parameters<typeof fetcher>[1],
): Promise<AuthSuccessOKResponse> => {
  return fetcher<AuthSuccessOKResponse>(getAuthEmailPasswordSignupUrl(params), {
    ...options,
    method: "POST",
    headers: { "Content-Type": "application/json", ...options?.headers },
    body: JSON.stringify(authEmailPasswordBody),
  });
};

export const getAuthEmailPasswordSignupMutationFetcher = (
  params?: AuthEmailPasswordSignupParams,
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, { arg }: { arg: AuthEmailPasswordBody | undefined }) => {
    return authEmailPasswordSignup(arg, params, options);
  };
};
export const getAuthEmailPasswordSignupMutationKey = (
  params?: AuthEmailPasswordSignupParams,
) => [`/auth/email-password/signup`, ...(params ? [params] : [])] as const;

export type AuthEmailPasswordSignupMutationResult = NonNullable<
  Awaited<ReturnType<typeof authEmailPasswordSignup>>
>;

export const useAuthEmailPasswordSignup = <
  TError = BadRequestResponse | ConflictResponse | InternalServerErrorResponse,
>(
  params?: AuthEmailPasswordSignupParams,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof authEmailPasswordSignup>>,
      TError,
      Key,
      AuthEmailPasswordBody | undefined,
      Awaited<ReturnType<typeof authEmailPasswordSignup>>
    > & { swrKey?: string };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey =
    swrOptions?.swrKey ?? getAuthEmailPasswordSignupMutationKey(params);
  const swrFn = getAuthEmailPasswordSignupMutationFetcher(
    params,
    requestOptions,
  );

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getAuthEmailPasswordSigninUrl = () => {
  return `/auth/email-password/signin`;
};

/**
 * Sign in to an existing account with a email and password.
 */
export const authEmailPasswordSignin = async (
  authEmailPasswordBody?: AuthEmailPasswordBody,
  options?: Parameters<typeof fetcher>[1],
): Promise<AuthSuccessOKResponse> => {
  return fetcher<AuthSuccessOKResponse>(getAuthEmailPasswordSigninUrl(), {
    ...options,
    method: "POST",
    headers: { "Content-Type": "application/json", ...options?.headers },
    body: JSON.stringify(authEmailPasswordBody),
  });
};

export const getAuthEmailPasswordSigninMutationFetcher = (
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, { arg }: { arg: AuthEmailPasswordBody | undefined }) => {
    return authEmailPasswordSignin(arg, options);
  };
};
export const getAuthEmailPasswordSigninMutationKey = () =>
  [`/auth/email-password/signin`] as const;

export type AuthEmailPasswordSigninMutationResult = NonNullable<
  Awaited<ReturnType<typeof authEmailPasswordSignin>>
>;

export const useAuthEmailPasswordSignin = <
  TError =
    | UnauthorisedResponse
    | ForbiddenResponse
    | NotFoundResponse
    | InternalServerErrorResponse,
>(options?: {
  swr?: SWRMutationConfiguration<
    Awaited<ReturnType<typeof authEmailPasswordSignin>>,
    TError,
    Key,
    AuthEmailPasswordBody | undefined,
    Awaited<ReturnType<typeof authEmailPasswordSignin>>
  > & { swrKey?: string };
  request?: SecondParameter<typeof fetcher>;
}) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey = swrOptions?.swrKey ?? getAuthEmailPasswordSigninMutationKey();
  const swrFn = getAuthEmailPasswordSigninMutationFetcher(requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getAuthPasswordResetRequestEmailUrl = () => {
  return `/auth/email-password/reset`;
};

/**
 * Request password reset email to be sent to the specified email address.
 */
export const authPasswordResetRequestEmail = async (
  authEmailPasswordResetBody?: AuthEmailPasswordResetBody,
  options?: Parameters<typeof fetcher>[1],
): Promise<void> => {
  return fetcher<void>(getAuthPasswordResetRequestEmailUrl(), {
    ...options,
    method: "POST",
    headers: { "Content-Type": "application/json", ...options?.headers },
    body: JSON.stringify(authEmailPasswordResetBody),
  });
};

export const getAuthPasswordResetRequestEmailMutationFetcher = (
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, { arg }: { arg: AuthEmailPasswordResetBody | undefined }) => {
    return authPasswordResetRequestEmail(arg, options);
  };
};
export const getAuthPasswordResetRequestEmailMutationKey = () =>
  [`/auth/email-password/reset`] as const;

export type AuthPasswordResetRequestEmailMutationResult = NonNullable<
  Awaited<ReturnType<typeof authPasswordResetRequestEmail>>
>;

export const useAuthPasswordResetRequestEmail = <
  TError =
    UnauthorisedResponse | NotFoundResponse | InternalServerErrorResponse,
>(options?: {
  swr?: SWRMutationConfiguration<
    Awaited<ReturnType<typeof authPasswordResetRequestEmail>>,
    TError,
    Key,
    AuthEmailPasswordResetBody | undefined,
    Awaited<ReturnType<typeof authPasswordResetRequestEmail>>
  > & { swrKey?: string };
  request?: SecondParameter<typeof fetcher>;
}) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey =
    swrOptions?.swrKey ?? getAuthPasswordResetRequestEmailMutationKey();
  const swrFn = getAuthPasswordResetRequestEmailMutationFetcher(requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getAuthEmailSignupUrl = (params?: AuthEmailSignupParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/auth/email/signup?${stringifiedParams}`
    : `/auth/email/signup`;
};

/**
 * Register a new account with an email and optional password. The password
 * requirement is dependent on how the instance is configured for account
 * authentication with email addresses (password vs magic link.)
 *
 * When the email address has not been registered, this endpoint will send
 * a verification email however it will also return a session cookie to
 * facilitate pre-verification usage of the platform. If the email address
 * already exists, no session cookie will be returned in order to prevent
 * arbitrary account control by a malicious actor. In this case, the email
 * will be sent again with the same OTP for the case where the user has
 * cleared their cookies or switched device but hasn't yet verified due to
 * missing the email or a delivery failure. In this sense, the endpoint can
 * act as a "resend verification email" operation as well as registration.
 *
 * In the first case, a 200 response is provided with the session cookie,
 * in the second case, a 422 response is provided without a session cookie.
 *
 * Given that this is an unauthenticated endpoint that triggers an email to
 * be sent to any public address, it MUST be heavily rate limited.
 */
export const authEmailSignup = async (
  authEmailBody?: AuthEmailBody,
  params?: AuthEmailSignupParams,
  options?: Parameters<typeof fetcher>[1],
): Promise<AuthSuccessOKResponse> => {
  return fetcher<AuthSuccessOKResponse>(getAuthEmailSignupUrl(params), {
    ...options,
    method: "POST",
    headers: { "Content-Type": "application/json", ...options?.headers },
    body: JSON.stringify(authEmailBody),
  });
};

export const getAuthEmailSignupMutationFetcher = (
  params?: AuthEmailSignupParams,
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, { arg }: { arg: AuthEmailBody | undefined }) => {
    return authEmailSignup(arg, params, options);
  };
};
export const getAuthEmailSignupMutationKey = (params?: AuthEmailSignupParams) =>
  [`/auth/email/signup`, ...(params ? [params] : [])] as const;

export type AuthEmailSignupMutationResult = NonNullable<
  Awaited<ReturnType<typeof authEmailSignup>>
>;

export const useAuthEmailSignup = <
  TError = BadRequestResponse | void | InternalServerErrorResponse,
>(
  params?: AuthEmailSignupParams,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof authEmailSignup>>,
      TError,
      Key,
      AuthEmailBody | undefined,
      Awaited<ReturnType<typeof authEmailSignup>>
    > & { swrKey?: string };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey = swrOptions?.swrKey ?? getAuthEmailSignupMutationKey(params);
  const swrFn = getAuthEmailSignupMutationFetcher(params, requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getAuthEmailSigninUrl = () => {
  return `/auth/email/signin`;
};

/**
 * Sign in to an existing account with an email and optional password. The
 * behaviour of this endpoint depends on how the instance is configured. If
 * email+password is the preferred method, a cookie is returned on success
 * but if magic links are preferred, the endpoint will start the code flow.
 */
export const authEmailSignin = async (
  authEmailBody?: AuthEmailBody,
  options?: Parameters<typeof fetcher>[1],
): Promise<AuthSuccessOKResponse> => {
  return fetcher<AuthSuccessOKResponse>(getAuthEmailSigninUrl(), {
    ...options,
    method: "POST",
    headers: { "Content-Type": "application/json", ...options?.headers },
    body: JSON.stringify(authEmailBody),
  });
};

export const getAuthEmailSigninMutationFetcher = (
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, { arg }: { arg: AuthEmailBody | undefined }) => {
    return authEmailSignin(arg, options);
  };
};
export const getAuthEmailSigninMutationKey = () =>
  [`/auth/email/signin`] as const;

export type AuthEmailSigninMutationResult = NonNullable<
  Awaited<ReturnType<typeof authEmailSignin>>
>;

export const useAuthEmailSignin = <
  TError =
    | UnauthorisedResponse
    | ForbiddenResponse
    | NotFoundResponse
    | InternalServerErrorResponse,
>(options?: {
  swr?: SWRMutationConfiguration<
    Awaited<ReturnType<typeof authEmailSignin>>,
    TError,
    Key,
    AuthEmailBody | undefined,
    Awaited<ReturnType<typeof authEmailSignin>>
  > & { swrKey?: string };
  request?: SecondParameter<typeof fetcher>;
}) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey = swrOptions?.swrKey ?? getAuthEmailSigninMutationKey();
  const swrFn = getAuthEmailSigninMutationFetcher(requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getAuthEmailVerifyUrl = () => {
  return `/auth/email/verify`;
};

/**
 * Verify an email address using a token that was emailed to one of the
 * account's email addresses either set via sign up or added later.
 *
 * The code is only six digits, so this endpoint MUST be rate limited to
 * keep the search space out of reach of an unauthenticated caller.
 */
export const authEmailVerify = async (
  authEmailVerifyBody?: AuthEmailVerifyBody,
  options?: Parameters<typeof fetcher>[1],
): Promise<AuthSuccessOKResponse> => {
  return fetcher<AuthSuccessOKResponse>(getAuthEmailVerifyUrl(), {
    ...options,
    method: "POST",
    headers: { "Content-Type": "application/json", ...options?.headers },
    body: JSON.stringify(authEmailVerifyBody),
  });
};

export const getAuthEmailVerifyMutationFetcher = (
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, { arg }: { arg: AuthEmailVerifyBody | undefined }) => {
    return authEmailVerify(arg, options);
  };
};
export const getAuthEmailVerifyMutationKey = () =>
  [`/auth/email/verify`] as const;

export type AuthEmailVerifyMutationResult = NonNullable<
  Awaited<ReturnType<typeof authEmailVerify>>
>;

export const useAuthEmailVerify = <
  TError =
    | UnauthorisedResponse
    | ForbiddenResponse
    | NotFoundResponse
    | InternalServerErrorResponse,
>(options?: {
  swr?: SWRMutationConfiguration<
    Awaited<ReturnType<typeof authEmailVerify>>,
    TError,
    Key,
    AuthEmailVerifyBody | undefined,
    Awaited<ReturnType<typeof authEmailVerify>>
  > & { swrKey?: string };
  request?: SecondParameter<typeof fetcher>;
}) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey = swrOptions?.swrKey ?? getAuthEmailVerifyMutationKey();
  const swrFn = getAuthEmailVerifyMutationFetcher(requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getOAuthProviderCallbackUrl = (oauthProvider: string) => {
  return `/auth/oauth/${oauthProvider}/callback`;
};

/**
 * OAuth2 callback.
 */
export const oAuthProviderCallback = async (
  oauthProvider: string,
  oAuthProviderCallbackBody?: OAuthProviderCallbackBody,
  options?: Parameters<typeof fetcher>[1],
): Promise<AuthSuccessOKResponse> => {
  return fetcher<AuthSuccessOKResponse>(
    getOAuthProviderCallbackUrl(oauthProvider),
    {
      ...options,
      method: "POST",
      headers: { "Content-Type": "application/json", ...options?.headers },
      body: JSON.stringify(oAuthProviderCallbackBody),
    },
  );
};

export const getOAuthProviderCallbackMutationFetcher = (
  oauthProvider: string,
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, { arg }: { arg: OAuthProviderCallbackBody | undefined }) => {
    return oAuthProviderCallback(oauthProvider, arg, options);
  };
};
export const getOAuthProviderCallbackMutationKey = (oauthProvider: string) =>
  [`/auth/oauth/${oauthProvider}/callback`] as const;

export type OAuthProviderCallbackMutationResult = NonNullable<
  Awaited<ReturnType<typeof oAuthProviderCallback>>
>;

export const useOAuthProviderCallback = <
  TError =
    | UnauthorisedResponse
    | ForbiddenResponse
    | NotFoundResponse
    | InternalServerErrorResponse,
>(
  oauthProvider: string,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof oAuthProviderCallback>>,
      TError,
      Key,
      OAuthProviderCallbackBody | undefined,
      Awaited<ReturnType<typeof oAuthProviderCallback>>
    > & { swrKey?: string };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey =
    swrOptions?.swrKey ?? getOAuthProviderCallbackMutationKey(oauthProvider);
  const swrFn = getOAuthProviderCallbackMutationFetcher(
    oauthProvider,
    requestOptions,
  );

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getOAuthJWKSUrl = () => {
  return `/oauth/jwks`;
};

/**
 * List public JSON Web Keys that clients can use to validate Storyden
 * OAuth access tokens and OpenID Connect ID tokens.
 *
 * This is advertised by `/.well-known/openid-configuration` as `jwks_uri`.
 * Storyden serves this under the API mount because the key set is an API
 * resource; the well-known discovery document itself is mounted at the
 * instance root and is intentionally not part of this OpenAPI document.
 */
export const oAuthJWKS = async (
  options?: Parameters<typeof fetcher>[1],
): Promise<OAuthJWKSOKResponse> => {
  return fetcher<OAuthJWKSOKResponse>(getOAuthJWKSUrl(), {
    ...options,
    method: "GET",
  });
};

export const getOAuthJWKSKey = () => [`/oauth/jwks`] as const;

export type OAuthJWKSQueryResult = NonNullable<
  Awaited<ReturnType<typeof oAuthJWKS>>
>;

export const useOAuthJWKS = <TError = InternalServerErrorResponse>(options?: {
  swr?: SWRConfiguration<Awaited<ReturnType<typeof oAuthJWKS>>, TError> & {
    swrKey?: Key;
    enabled?: boolean;
  };
  request?: SecondParameter<typeof fetcher>;
}) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const isEnabled = swrOptions?.enabled !== false;
  const swrKey =
    swrOptions?.swrKey ?? (() => (isEnabled ? getOAuthJWKSKey() : null));
  const swrFn = () => oAuthJWKS(requestOptions);

  const query = useSwr<Awaited<ReturnType<typeof swrFn>>, TError>(
    swrKey,
    swrFn,
    swrOptions,
  );

  return {
    swrKey,
    ...query,
  };
};
export const getOAuthDeviceAuthorisationUrl = () => {
  return `/oauth/device_authorization`;
};

/**
 * Start the OAuth 2.0 Device Authorization Grant for clients that cannot
 * receive a browser redirect directly, such as CLIs, terminals, and
 * desktop tools.
 *
 * The `scope` parameter follows OAuth 2.0 and is optional. Storyden
 * applies additional client policy after parsing the request:
 *
 * - Built-in first-party device clients, such as the default Storyden CLI
 *   client, must request exactly `openid profile offline_access`. On
 *   approval Storyden expands the issued scope to the approving account's
 *   current permissions.
 * - Third-party explicit-scope clients may omit `scope`; omitted scope
 *   means no requested scopes.
 *
 * `verification_uri` and `verification_uri_complete` point at the
 * configured frontend consent page, not at an API-rendered HTML page.
 * Custom frontends can change this URL with
 * `OAUTH_DEVICE_AUTHORISATION_CONSENT_URL`.
 */
export const oAuthDeviceAuthorisation = async (
  oAuthDeviceAuthorisationBody: OAuthDeviceAuthorisationBody,
  options?: Parameters<typeof fetcher>[1],
): Promise<OAuthDeviceAuthorisationOKResponse> => {
  const formUrlEncoded = new URLSearchParams();
  formUrlEncoded.append(`client_id`, oAuthDeviceAuthorisationBody.client_id);
  if (oAuthDeviceAuthorisationBody.scope !== undefined) {
    formUrlEncoded.append(`scope`, oAuthDeviceAuthorisationBody.scope);
  }

  return fetcher<OAuthDeviceAuthorisationOKResponse>(
    getOAuthDeviceAuthorisationUrl(),
    {
      ...options,
      method: "POST",
      headers: {
        "Content-Type": "application/x-www-form-urlencoded",
        ...options?.headers,
      },
      body: formUrlEncoded,
    },
  );
};

export const getOAuthDeviceAuthorisationMutationFetcher = (
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, { arg }: { arg: OAuthDeviceAuthorisationBody }) => {
    return oAuthDeviceAuthorisation(arg, options);
  };
};
export const getOAuthDeviceAuthorisationMutationKey = () =>
  [`/oauth/device_authorization`] as const;

export type OAuthDeviceAuthorisationMutationResult = NonNullable<
  Awaited<ReturnType<typeof oAuthDeviceAuthorisation>>
>;

export const useOAuthDeviceAuthorisation = <
  TError = OAuthErrorResponse | InternalServerErrorResponse,
>(options?: {
  swr?: SWRMutationConfiguration<
    Awaited<ReturnType<typeof oAuthDeviceAuthorisation>>,
    TError,
    Key,
    OAuthDeviceAuthorisationBody,
    Awaited<ReturnType<typeof oAuthDeviceAuthorisation>>
  > & { swrKey?: string };
  request?: SecondParameter<typeof fetcher>;
}) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey = swrOptions?.swrKey ?? getOAuthDeviceAuthorisationMutationKey();
  const swrFn = getOAuthDeviceAuthorisationMutationFetcher(requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getOAuthDeviceConsentUrl = (params?: OAuthDeviceConsentParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/oauth/device/consent?${stringifiedParams}`
    : `/oauth/device/consent`;
};

/**
 * Read a pending OAuth device authorisation request for a signed-in user
 * before they approve or deny consent in the frontend.
 *
 * This is a Storyden frontend/API integration endpoint, not an OAuth
 * protocol endpoint. The API never renders the consent UI directly. A
 * frontend reads this JSON, displays the client and scopes, then submits
 * the user's decision.
 *
 * Reading consent claims the user code for the signed-in account. This
 * prevents another account from approving the same code after it has been
 * displayed.
 */
export const oAuthDeviceConsent = async (
  params?: OAuthDeviceConsentParams,
  options?: Parameters<typeof fetcher>[1],
): Promise<OAuthDeviceConsentOKResponse> => {
  return fetcher<OAuthDeviceConsentOKResponse>(
    getOAuthDeviceConsentUrl(params),
    {
      ...options,
      method: "GET",
    },
  );
};

export const getOAuthDeviceConsentKey = (params?: OAuthDeviceConsentParams) =>
  [`/oauth/device/consent`, ...(params ? [params] : [])] as const;

export type OAuthDeviceConsentQueryResult = NonNullable<
  Awaited<ReturnType<typeof oAuthDeviceConsent>>
>;

export const useOAuthDeviceConsent = <
  TError =
    OAuthErrorResponse | UnauthorisedResponse | InternalServerErrorResponse,
>(
  params?: OAuthDeviceConsentParams,
  options?: {
    swr?: SWRConfiguration<
      Awaited<ReturnType<typeof oAuthDeviceConsent>>,
      TError
    > & { swrKey?: Key; enabled?: boolean };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const isEnabled = swrOptions?.enabled !== false;
  const swrKey =
    swrOptions?.swrKey ??
    (() => (isEnabled ? getOAuthDeviceConsentKey(params) : null));
  const swrFn = () => oAuthDeviceConsent(params, requestOptions);

  const query = useSwr<Awaited<ReturnType<typeof swrFn>>, TError>(
    swrKey,
    swrFn,
    swrOptions,
  );

  return {
    swrKey,
    ...query,
  };
};
export const getOAuthDeviceConsentSubmitUrl = () => {
  return `/oauth/device/consent`;
};

/**
 * Approve or deny a pending OAuth device authorisation request for the
 * currently signed-in account.
 *
 * On approval Storyden recomputes the granted scope from the current
 * account permissions and client policy. For first-party inherited clients
 * this means the final token scope may include Storyden permission scopes
 * that were not present in the original device authorisation request.
 */
export const oAuthDeviceConsentSubmit = async (
  oAuthDeviceConsentSubmitBody: OAuthDeviceConsentSubmitBody,
  options?: Parameters<typeof fetcher>[1],
): Promise<OAuthDeviceConsentSubmitOKResponse> => {
  return fetcher<OAuthDeviceConsentSubmitOKResponse>(
    getOAuthDeviceConsentSubmitUrl(),
    {
      ...options,
      method: "POST",
      headers: { "Content-Type": "application/json", ...options?.headers },
      body: JSON.stringify(oAuthDeviceConsentSubmitBody),
    },
  );
};

export const getOAuthDeviceConsentSubmitMutationFetcher = (
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, { arg }: { arg: OAuthDeviceConsentSubmitBody }) => {
    return oAuthDeviceConsentSubmit(arg, options);
  };
};
export const getOAuthDeviceConsentSubmitMutationKey = () =>
  [`/oauth/device/consent`] as const;

export type OAuthDeviceConsentSubmitMutationResult = NonNullable<
  Awaited<ReturnType<typeof oAuthDeviceConsentSubmit>>
>;

export const useOAuthDeviceConsentSubmit = <
  TError =
    OAuthErrorResponse | UnauthorisedResponse | InternalServerErrorResponse,
>(options?: {
  swr?: SWRMutationConfiguration<
    Awaited<ReturnType<typeof oAuthDeviceConsentSubmit>>,
    TError,
    Key,
    OAuthDeviceConsentSubmitBody,
    Awaited<ReturnType<typeof oAuthDeviceConsentSubmit>>
  > & { swrKey?: string };
  request?: SecondParameter<typeof fetcher>;
}) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey = swrOptions?.swrKey ?? getOAuthDeviceConsentSubmitMutationKey();
  const swrFn = getOAuthDeviceConsentSubmitMutationFetcher(requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getOAuthAuthoriseUrl = (params: OAuthAuthoriseParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/oauth/authorize?${stringifiedParams}`
    : `/oauth/authorize`;
};

/**
 * Start the browser-based OAuth 2.0 Authorization Code flow with PKCE.
 *
 * This endpoint requires a browser session. If the account is not signed
 * in, Storyden redirects to the frontend login route instead of returning
 * a protocol redirect to the client application.
 *
 * Unlike many OAuth servers, Storyden does not render a consent page from
 * this API endpoint. A valid request creates a short-lived pending
 * authorisation request and redirects the browser to the configured
 * frontend authorisation-code consent URL. Custom frontends can change
 * this URL with `OAUTH_AUTHORISATION_CODE_CONSENT_URL`.
 *
 * The `scope` parameter follows OAuth 2.0 and is optional. Empty or
 * omitted scope means no requested scopes. Storyden permission scopes are
 * granted only when allowed by the client and by the signed-in account's
 * current permissions.
 */
export const oAuthAuthorise = async (
  params: OAuthAuthoriseParams,
  options?: Parameters<typeof fetcher>[1],
): Promise<OAuthAuthoriseOKResponse> => {
  return fetcher<OAuthAuthoriseOKResponse>(getOAuthAuthoriseUrl(params), {
    ...options,
    method: "GET",
  });
};

export const getOAuthAuthoriseKey = (params: OAuthAuthoriseParams) =>
  [`/oauth/authorize`, ...(params ? [params] : [])] as const;

export type OAuthAuthoriseQueryResult = NonNullable<
  Awaited<ReturnType<typeof oAuthAuthorise>>
>;

export const useOAuthAuthorise = <
  TError =
    | OAuthAuthoriseFoundResponse
    | OAuthErrorResponse
    | InternalServerErrorResponse,
>(
  params: OAuthAuthoriseParams,
  options?: {
    swr?: SWRConfiguration<
      Awaited<ReturnType<typeof oAuthAuthorise>>,
      TError
    > & { swrKey?: Key; enabled?: boolean };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const isEnabled = swrOptions?.enabled !== false;
  const swrKey =
    swrOptions?.swrKey ??
    (() => (isEnabled ? getOAuthAuthoriseKey(params) : null));
  const swrFn = () => oAuthAuthorise(params, requestOptions);

  const query = useSwr<Awaited<ReturnType<typeof swrFn>>, TError>(
    swrKey,
    swrFn,
    swrOptions,
  );

  return {
    swrKey,
    ...query,
  };
};
export const getOAuthAuthoriseConsentUrl = (
  params?: OAuthAuthoriseConsentParams,
) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/oauth/authorize/consent?${stringifiedParams}`
    : `/oauth/authorize/consent`;
};

/**
 * Read a pending OAuth authorisation code request for a signed-in user
 * before they approve or deny consent in the frontend.
 *
 * This is a Storyden frontend/API integration endpoint, not an OAuth
 * protocol endpoint. It returns the client, redirect URI, requested
 * scopes, and currently grantable scopes so the frontend can render a
 * consent screen.
 */
export const oAuthAuthoriseConsent = async (
  params?: OAuthAuthoriseConsentParams,
  options?: Parameters<typeof fetcher>[1],
): Promise<OAuthAuthoriseConsentOKResponse> => {
  return fetcher<OAuthAuthoriseConsentOKResponse>(
    getOAuthAuthoriseConsentUrl(params),
    {
      ...options,
      method: "GET",
    },
  );
};

export const getOAuthAuthoriseConsentKey = (
  params?: OAuthAuthoriseConsentParams,
) => [`/oauth/authorize/consent`, ...(params ? [params] : [])] as const;

export type OAuthAuthoriseConsentQueryResult = NonNullable<
  Awaited<ReturnType<typeof oAuthAuthoriseConsent>>
>;

export const useOAuthAuthoriseConsent = <
  TError =
    OAuthErrorResponse | UnauthorisedResponse | InternalServerErrorResponse,
>(
  params?: OAuthAuthoriseConsentParams,
  options?: {
    swr?: SWRConfiguration<
      Awaited<ReturnType<typeof oAuthAuthoriseConsent>>,
      TError
    > & { swrKey?: Key; enabled?: boolean };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const isEnabled = swrOptions?.enabled !== false;
  const swrKey =
    swrOptions?.swrKey ??
    (() => (isEnabled ? getOAuthAuthoriseConsentKey(params) : null));
  const swrFn = () => oAuthAuthoriseConsent(params, requestOptions);

  const query = useSwr<Awaited<ReturnType<typeof swrFn>>, TError>(
    swrKey,
    swrFn,
    swrOptions,
  );

  return {
    swrKey,
    ...query,
  };
};
export const getOAuthAuthoriseConsentSubmitUrl = () => {
  return `/oauth/authorize/consent`;
};

/**
 * Approve or deny a pending OAuth authorisation code request for the
 * currently signed-in account.
 *
 * On approval this creates a short-lived authorisation code and returns
 * the client redirect URI containing `code` and optional `state`. On
 * denial the returned redirect URI contains `error=access_denied`.
 *
 * Storyden recomputes the granted scope at approval time from current
 * account permissions and client policy.
 */
export const oAuthAuthoriseConsentSubmit = async (
  oAuthAuthoriseConsentSubmitBody: OAuthAuthoriseConsentSubmitBody,
  options?: Parameters<typeof fetcher>[1],
): Promise<OAuthAuthoriseConsentSubmitOKResponse> => {
  return fetcher<OAuthAuthoriseConsentSubmitOKResponse>(
    getOAuthAuthoriseConsentSubmitUrl(),
    {
      ...options,
      method: "POST",
      headers: { "Content-Type": "application/json", ...options?.headers },
      body: JSON.stringify(oAuthAuthoriseConsentSubmitBody),
    },
  );
};

export const getOAuthAuthoriseConsentSubmitMutationFetcher = (
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, { arg }: { arg: OAuthAuthoriseConsentSubmitBody }) => {
    return oAuthAuthoriseConsentSubmit(arg, options);
  };
};
export const getOAuthAuthoriseConsentSubmitMutationKey = () =>
  [`/oauth/authorize/consent`] as const;

export type OAuthAuthoriseConsentSubmitMutationResult = NonNullable<
  Awaited<ReturnType<typeof oAuthAuthoriseConsentSubmit>>
>;

export const useOAuthAuthoriseConsentSubmit = <
  TError =
    OAuthErrorResponse | UnauthorisedResponse | InternalServerErrorResponse,
>(options?: {
  swr?: SWRMutationConfiguration<
    Awaited<ReturnType<typeof oAuthAuthoriseConsentSubmit>>,
    TError,
    Key,
    OAuthAuthoriseConsentSubmitBody,
    Awaited<ReturnType<typeof oAuthAuthoriseConsentSubmit>>
  > & { swrKey?: string };
  request?: SecondParameter<typeof fetcher>;
}) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey =
    swrOptions?.swrKey ?? getOAuthAuthoriseConsentSubmitMutationKey();
  const swrFn = getOAuthAuthoriseConsentSubmitMutationFetcher(requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getOAuthTokenUrl = () => {
  return `/oauth/token`;
};

/**
 * Exchange an OAuth authorisation code, device code, refresh token, or
 * client credentials grant for tokens.
 *
 * Supported grants are advertised by `/.well-known/openid-configuration`.
 * Public clients authenticate with `client_id` only and must use grants
 * suitable for public clients, such as device code or authorisation code
 * with PKCE. Confidential clients must provide `client_secret` for
 * authorisation-code, refresh-token, and client-credentials exchanges.
 *
 * Storyden access tokens are short-lived JWTs containing the issued
 * `scope`. Revoking a refresh token or changing account permissions does
 * not revoke an already-issued access token; permission changes are
 * applied on the next token issuance or refresh.
 *
 * Device-code polling returns OAuth-compatible errors such as
 * `authorization_pending`, `slow_down`, `expired_token`,
 * `access_denied`, and `invalid_grant`.
 */
export const oAuthToken = async (
  oAuthTokenBody: OAuthTokenBody,
  options?: Parameters<typeof fetcher>[1],
): Promise<OAuthTokenOKResponse> => {
  const formUrlEncoded = new URLSearchParams();
  formUrlEncoded.append(`grant_type`, oAuthTokenBody.grant_type);
  formUrlEncoded.append(`client_id`, oAuthTokenBody.client_id);
  if (oAuthTokenBody.client_secret !== undefined) {
    formUrlEncoded.append(`client_secret`, oAuthTokenBody.client_secret);
  }
  if (oAuthTokenBody.scope !== undefined) {
    formUrlEncoded.append(`scope`, oAuthTokenBody.scope);
  }
  if (oAuthTokenBody.device_code !== undefined) {
    formUrlEncoded.append(`device_code`, oAuthTokenBody.device_code);
  }
  if (oAuthTokenBody.code !== undefined) {
    formUrlEncoded.append(`code`, oAuthTokenBody.code);
  }
  if (oAuthTokenBody.redirect_uri !== undefined) {
    formUrlEncoded.append(`redirect_uri`, oAuthTokenBody.redirect_uri);
  }
  if (oAuthTokenBody.code_verifier !== undefined) {
    formUrlEncoded.append(`code_verifier`, oAuthTokenBody.code_verifier);
  }
  if (oAuthTokenBody.refresh_token !== undefined) {
    formUrlEncoded.append(`refresh_token`, oAuthTokenBody.refresh_token);
  }

  return fetcher<OAuthTokenOKResponse>(getOAuthTokenUrl(), {
    ...options,
    method: "POST",
    headers: {
      "Content-Type": "application/x-www-form-urlencoded",
      ...options?.headers,
    },
    body: formUrlEncoded,
  });
};

export const getOAuthTokenMutationFetcher = (
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, { arg }: { arg: OAuthTokenBody }) => {
    return oAuthToken(arg, options);
  };
};
export const getOAuthTokenMutationKey = () => [`/oauth/token`] as const;

export type OAuthTokenMutationResult = NonNullable<
  Awaited<ReturnType<typeof oAuthToken>>
>;

export const useOAuthToken = <
  TError =
    | OAuthTokenErrorResponse
    | OAuthTokenUnauthorisedResponse
    | InternalServerErrorResponse,
>(options?: {
  swr?: SWRMutationConfiguration<
    Awaited<ReturnType<typeof oAuthToken>>,
    TError,
    Key,
    OAuthTokenBody,
    Awaited<ReturnType<typeof oAuthToken>>
  > & { swrKey?: string };
  request?: SecondParameter<typeof fetcher>;
}) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey = swrOptions?.swrKey ?? getOAuthTokenMutationKey();
  const swrFn = getOAuthTokenMutationFetcher(requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getOAuthUserInfoUrl = () => {
  return `/oauth/userinfo`;
};

/**
 * Return OpenID Connect UserInfo claims for the account represented by a
 * valid bearer access token.
 *
 * Claims are scope-gated:
 *
 * - `openid` identifies the subject.
 * - `profile` enables profile claims such as display name.
 * - `email` enables email claims when the account has an email address.
 *
 * Storyden accounts do not always have email addresses, so email claims
 * may be absent even when the `email` scope is present.
 */
export const oAuthUserInfo = async (
  options?: Parameters<typeof fetcher>[1],
): Promise<OAuthUserInfoOKResponse> => {
  return fetcher<OAuthUserInfoOKResponse>(getOAuthUserInfoUrl(), {
    ...options,
    method: "GET",
  });
};

export const getOAuthUserInfoKey = () => [`/oauth/userinfo`] as const;

export type OAuthUserInfoQueryResult = NonNullable<
  Awaited<ReturnType<typeof oAuthUserInfo>>
>;

export const useOAuthUserInfo = <
  TError = UnauthorisedResponse | InternalServerErrorResponse,
>(options?: {
  swr?: SWRConfiguration<Awaited<ReturnType<typeof oAuthUserInfo>>, TError> & {
    swrKey?: Key;
    enabled?: boolean;
  };
  request?: SecondParameter<typeof fetcher>;
}) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const isEnabled = swrOptions?.enabled !== false;
  const swrKey =
    swrOptions?.swrKey ?? (() => (isEnabled ? getOAuthUserInfoKey() : null));
  const swrFn = () => oAuthUserInfo(requestOptions);

  const query = useSwr<Awaited<ReturnType<typeof swrFn>>, TError>(
    swrKey,
    swrFn,
    swrOptions,
  );

  return {
    swrKey,
    ...query,
  };
};
export const getOAuthRemoteCallbackUrl = (
  params: OAuthRemoteCallbackParams,
) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/oauth/remote/callback?${stringifiedParams}`
    : `/oauth/remote/callback`;
};

/**
 * Complete a remote OAuth authorization code callback. This validates the
 * saved state, exchanges the code with PKCE, and stores returned tokens
 * on the remote connection.
 */
export const oAuthRemoteCallback = async (
  params: OAuthRemoteCallbackParams,
  options?: Parameters<typeof fetcher>[1],
): Promise<OAuthRemoteCallbackOKResponse> => {
  return fetcher<OAuthRemoteCallbackOKResponse>(
    getOAuthRemoteCallbackUrl(params),
    {
      ...options,
      method: "GET",
    },
  );
};

export const getOAuthRemoteCallbackKey = (params: OAuthRemoteCallbackParams) =>
  [`/oauth/remote/callback`, ...(params ? [params] : [])] as const;

export type OAuthRemoteCallbackQueryResult = NonNullable<
  Awaited<ReturnType<typeof oAuthRemoteCallback>>
>;

export const useOAuthRemoteCallback = <
  TError =
    | BadRequestResponse
    | UnauthorisedResponse
    | ForbiddenResponse
    | InternalServerErrorResponse,
>(
  params: OAuthRemoteCallbackParams,
  options?: {
    swr?: SWRConfiguration<
      Awaited<ReturnType<typeof oAuthRemoteCallback>>,
      TError
    > & { swrKey?: Key; enabled?: boolean };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const isEnabled = swrOptions?.enabled !== false;
  const swrKey =
    swrOptions?.swrKey ??
    (() => (isEnabled ? getOAuthRemoteCallbackKey(params) : null));
  const swrFn = () => oAuthRemoteCallback(params, requestOptions);

  const query = useSwr<Awaited<ReturnType<typeof swrFn>>, TError>(
    swrKey,
    swrFn,
    swrOptions,
  );

  return {
    swrKey,
    ...query,
  };
};
export const getOAuthClientRegisterUrl = () => {
  return `/oauth/register`;
};

/**
 * RFC 7591 OAuth 2.0 Dynamic Client Registration.
 *
 * Allows clients such as MCP connectors to register themselves without
 * prior administrator configuration. Dynamically registered clients are
 * tenant-owned (they have no account owner), use the explicit scope
 * policy, and are restricted to a conservative grant and scope allowlist.
 *
 * Authorization Code clients must use PKCE; Storyden enforces PKCE (S256)
 * at the authorize and token endpoints for all clients.
 *
 * Public clients register with `token_endpoint_auth_method: none` and
 * receive no client secret. Confidential clients register with
 * `client_secret_basic` or `client_secret_post` and receive a one-time
 * `client_secret` in the response. The registration endpoint is advertised
 * as `registration_endpoint` by the authorization server metadata
 * documents.
 *
 * This is an unauthenticated endpoint that creates server state, so it
 * is heavily rate limited to prevent abuse.
 */
export const oAuthClientRegister = async (
  oAuthClientRegisterBody: OAuthClientRegisterBody,
  options?: Parameters<typeof fetcher>[1],
): Promise<OAuthClientRegisterOKResponse> => {
  return fetcher<OAuthClientRegisterOKResponse>(getOAuthClientRegisterUrl(), {
    ...options,
    method: "POST",
    headers: { "Content-Type": "application/json", ...options?.headers },
    body: JSON.stringify(oAuthClientRegisterBody),
  });
};

export const getOAuthClientRegisterMutationFetcher = (
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, { arg }: { arg: OAuthClientRegisterBody }) => {
    return oAuthClientRegister(arg, options);
  };
};
export const getOAuthClientRegisterMutationKey = () =>
  [`/oauth/register`] as const;

export type OAuthClientRegisterMutationResult = NonNullable<
  Awaited<ReturnType<typeof oAuthClientRegister>>
>;

export const useOAuthClientRegister = <
  TError = OAuthClientRegisterErrorResponse | InternalServerErrorResponse,
>(options?: {
  swr?: SWRMutationConfiguration<
    Awaited<ReturnType<typeof oAuthClientRegister>>,
    TError,
    Key,
    OAuthClientRegisterBody,
    Awaited<ReturnType<typeof oAuthClientRegister>>
  > & { swrKey?: string };
  request?: SecondParameter<typeof fetcher>;
}) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey = swrOptions?.swrKey ?? getOAuthClientRegisterMutationKey();
  const swrFn = getOAuthClientRegisterMutationFetcher(requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getWebAuthnRequestCredentialUrl = (accountHandle: string) => {
  return `/auth/webauthn/make/${accountHandle}`;
};

/**
 * Start the WebAuthn registration process by requesting a credential.
 */
export const webAuthnRequestCredential = async (
  accountHandle: string,
  options?: Parameters<typeof fetcher>[1],
): Promise<WebAuthnRequestCredentialOKResponse> => {
  return fetcher<WebAuthnRequestCredentialOKResponse>(
    getWebAuthnRequestCredentialUrl(accountHandle),
    {
      ...options,
      method: "GET",
    },
  );
};

export const getWebAuthnRequestCredentialKey = (accountHandle: string) =>
  [`/auth/webauthn/make/${accountHandle}`] as const;

export type WebAuthnRequestCredentialQueryResult = NonNullable<
  Awaited<ReturnType<typeof webAuthnRequestCredential>>
>;

export const useWebAuthnRequestCredential = <
  TError = BadRequestResponse | InternalServerErrorResponse,
>(
  accountHandle: string,
  options?: {
    swr?: SWRConfiguration<
      Awaited<ReturnType<typeof webAuthnRequestCredential>>,
      TError
    > & { swrKey?: Key; enabled?: boolean };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const isEnabled =
    swrOptions?.enabled !== false &&
    accountHandle !== null &&
    accountHandle !== undefined;
  const swrKey =
    swrOptions?.swrKey ??
    (() => (isEnabled ? getWebAuthnRequestCredentialKey(accountHandle) : null));
  const swrFn = () => webAuthnRequestCredential(accountHandle, requestOptions);

  const query = useSwr<Awaited<ReturnType<typeof swrFn>>, TError>(
    swrKey,
    swrFn,
    swrOptions,
  );

  return {
    swrKey,
    ...query,
  };
};
export const getWebAuthnMakeCredentialUrl = (
  params?: WebAuthnMakeCredentialParams,
) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/auth/webauthn/make?${stringifiedParams}`
    : `/auth/webauthn/make`;
};

/**
 * Complete WebAuthn registration by creating a new credential.
 */
export const webAuthnMakeCredential = async (
  webAuthnMakeCredentialBody?: WebAuthnMakeCredentialBody,
  params?: WebAuthnMakeCredentialParams,
  options?: Parameters<typeof fetcher>[1],
): Promise<AuthSuccessOKResponse> => {
  return fetcher<AuthSuccessOKResponse>(getWebAuthnMakeCredentialUrl(params), {
    ...options,
    method: "POST",
    headers: { "Content-Type": "application/json", ...options?.headers },
    body: JSON.stringify(webAuthnMakeCredentialBody),
  });
};

export const getWebAuthnMakeCredentialMutationFetcher = (
  params?: WebAuthnMakeCredentialParams,
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, { arg }: { arg: WebAuthnMakeCredentialBody | undefined }) => {
    return webAuthnMakeCredential(arg, params, options);
  };
};
export const getWebAuthnMakeCredentialMutationKey = (
  params?: WebAuthnMakeCredentialParams,
) => [`/auth/webauthn/make`, ...(params ? [params] : [])] as const;

export type WebAuthnMakeCredentialMutationResult = NonNullable<
  Awaited<ReturnType<typeof webAuthnMakeCredential>>
>;

export const useWebAuthnMakeCredential = <
  TError = BadRequestResponse | ConflictResponse | InternalServerErrorResponse,
>(
  params?: WebAuthnMakeCredentialParams,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof webAuthnMakeCredential>>,
      TError,
      Key,
      WebAuthnMakeCredentialBody | undefined,
      Awaited<ReturnType<typeof webAuthnMakeCredential>>
    > & { swrKey?: string };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey =
    swrOptions?.swrKey ?? getWebAuthnMakeCredentialMutationKey(params);
  const swrFn = getWebAuthnMakeCredentialMutationFetcher(
    params,
    requestOptions,
  );

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getWebAuthnGetAssertionUrl = (accountHandle: string) => {
  return `/auth/webauthn/assert/${accountHandle}`;
};

/**
 * Start the WebAuthn assertion for an existing account.
 */
export const webAuthnGetAssertion = async (
  accountHandle: string,
  options?: Parameters<typeof fetcher>[1],
): Promise<WebAuthnGetAssertionOKResponse> => {
  return fetcher<WebAuthnGetAssertionOKResponse>(
    getWebAuthnGetAssertionUrl(accountHandle),
    {
      ...options,
      method: "GET",
    },
  );
};

export const getWebAuthnGetAssertionKey = (accountHandle: string) =>
  [`/auth/webauthn/assert/${accountHandle}`] as const;

export type WebAuthnGetAssertionQueryResult = NonNullable<
  Awaited<ReturnType<typeof webAuthnGetAssertion>>
>;

export const useWebAuthnGetAssertion = <
  TError =
    UnauthorisedResponse | NotFoundResponse | InternalServerErrorResponse,
>(
  accountHandle: string,
  options?: {
    swr?: SWRConfiguration<
      Awaited<ReturnType<typeof webAuthnGetAssertion>>,
      TError
    > & { swrKey?: Key; enabled?: boolean };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const isEnabled =
    swrOptions?.enabled !== false &&
    accountHandle !== null &&
    accountHandle !== undefined;
  const swrKey =
    swrOptions?.swrKey ??
    (() => (isEnabled ? getWebAuthnGetAssertionKey(accountHandle) : null));
  const swrFn = () => webAuthnGetAssertion(accountHandle, requestOptions);

  const query = useSwr<Awaited<ReturnType<typeof swrFn>>, TError>(
    swrKey,
    swrFn,
    swrOptions,
  );

  return {
    swrKey,
    ...query,
  };
};
export const getWebAuthnMakeAssertionUrl = () => {
  return `/auth/webauthn/assert`;
};

/**
 * Complete the credential assertion and sign in to an account.
 */
export const webAuthnMakeAssertion = async (
  webAuthnMakeAssertionBody?: WebAuthnMakeAssertionBody,
  options?: Parameters<typeof fetcher>[1],
): Promise<AuthSuccessOKResponse> => {
  return fetcher<AuthSuccessOKResponse>(getWebAuthnMakeAssertionUrl(), {
    ...options,
    method: "POST",
    headers: { "Content-Type": "application/json", ...options?.headers },
    body: JSON.stringify(webAuthnMakeAssertionBody),
  });
};

export const getWebAuthnMakeAssertionMutationFetcher = (
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, { arg }: { arg: WebAuthnMakeAssertionBody | undefined }) => {
    return webAuthnMakeAssertion(arg, options);
  };
};
export const getWebAuthnMakeAssertionMutationKey = () =>
  [`/auth/webauthn/assert`] as const;

export type WebAuthnMakeAssertionMutationResult = NonNullable<
  Awaited<ReturnType<typeof webAuthnMakeAssertion>>
>;

export const useWebAuthnMakeAssertion = <
  TError =
    UnauthorisedResponse | NotFoundResponse | InternalServerErrorResponse,
>(options?: {
  swr?: SWRMutationConfiguration<
    Awaited<ReturnType<typeof webAuthnMakeAssertion>>,
    TError,
    Key,
    WebAuthnMakeAssertionBody | undefined,
    Awaited<ReturnType<typeof webAuthnMakeAssertion>>
  > & { swrKey?: string };
  request?: SecondParameter<typeof fetcher>;
}) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey = swrOptions?.swrKey ?? getWebAuthnMakeAssertionMutationKey();
  const swrFn = getWebAuthnMakeAssertionMutationFetcher(requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getPhoneRequestCodeUrl = (params?: PhoneRequestCodeParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/auth/phone?${stringifiedParams}`
    : `/auth/phone`;
};

/**
 * Start the authentication flow with a phone number. The handler will send
 * a one-time code to the provided phone number which must then be sent to
 * the other phone endpoint to verify the number and validate the account.
 */
export const phoneRequestCode = async (
  phoneRequestCodeBody?: PhoneRequestCodeBody,
  params?: PhoneRequestCodeParams,
  options?: Parameters<typeof fetcher>[1],
): Promise<AuthSuccessOKResponse> => {
  return fetcher<AuthSuccessOKResponse>(getPhoneRequestCodeUrl(params), {
    ...options,
    method: "POST",
    headers: { "Content-Type": "application/json", ...options?.headers },
    body: JSON.stringify(phoneRequestCodeBody),
  });
};

export const getPhoneRequestCodeMutationFetcher = (
  params?: PhoneRequestCodeParams,
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, { arg }: { arg: PhoneRequestCodeBody | undefined }) => {
    return phoneRequestCode(arg, params, options);
  };
};
export const getPhoneRequestCodeMutationKey = (
  params?: PhoneRequestCodeParams,
) => [`/auth/phone`, ...(params ? [params] : [])] as const;

export type PhoneRequestCodeMutationResult = NonNullable<
  Awaited<ReturnType<typeof phoneRequestCode>>
>;

export const usePhoneRequestCode = <
  TError = BadRequestResponse | ConflictResponse | InternalServerErrorResponse,
>(
  params?: PhoneRequestCodeParams,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof phoneRequestCode>>,
      TError,
      Key,
      PhoneRequestCodeBody | undefined,
      Awaited<ReturnType<typeof phoneRequestCode>>
    > & { swrKey?: string };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey = swrOptions?.swrKey ?? getPhoneRequestCodeMutationKey(params);
  const swrFn = getPhoneRequestCodeMutationFetcher(params, requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getPhoneSubmitCodeUrl = (accountHandle: string) => {
  return `/auth/phone/${accountHandle}`;
};

/**
 * Complete the phone number authentication flow by submitting the one-time
 * code that was sent to the user's phone.
 */
export const phoneSubmitCode = async (
  accountHandle: string,
  phoneSubmitCodeBody?: PhoneSubmitCodeBody,
  options?: Parameters<typeof fetcher>[1],
): Promise<AuthSuccessOKResponse> => {
  return fetcher<AuthSuccessOKResponse>(getPhoneSubmitCodeUrl(accountHandle), {
    ...options,
    method: "PUT",
    headers: { "Content-Type": "application/json", ...options?.headers },
    body: JSON.stringify(phoneSubmitCodeBody),
  });
};

export const getPhoneSubmitCodeMutationFetcher = (
  accountHandle: string,
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, { arg }: { arg: PhoneSubmitCodeBody | undefined }) => {
    return phoneSubmitCode(accountHandle, arg, options);
  };
};
export const getPhoneSubmitCodeMutationKey = (accountHandle: string) =>
  [`/auth/phone/${accountHandle}`] as const;

export type PhoneSubmitCodeMutationResult = NonNullable<
  Awaited<ReturnType<typeof phoneSubmitCode>>
>;

export const usePhoneSubmitCode = <
  TError = BadRequestResponse | InternalServerErrorResponse,
>(
  accountHandle: string,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof phoneSubmitCode>>,
      TError,
      Key,
      PhoneSubmitCodeBody | undefined,
      Awaited<ReturnType<typeof phoneSubmitCode>>
    > & { swrKey?: string };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey =
    swrOptions?.swrKey ?? getPhoneSubmitCodeMutationKey(accountHandle);
  const swrFn = getPhoneSubmitCodeMutationFetcher(
    accountHandle,
    requestOptions,
  );

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getAccessKeyListUrl = () => {
  return `/auth/access-keys`;
};

/**
 * List all access keys for the authenticated account or all access keys
 * that have been issued for the entire instance if and only if the request
 * parameters specify all keys and the requesting account is an admin.
 */
export const accessKeyList = async (
  options?: Parameters<typeof fetcher>[1],
): Promise<AccessKeyListOKResponse> => {
  return fetcher<AccessKeyListOKResponse>(getAccessKeyListUrl(), {
    ...options,
    method: "GET",
  });
};

export const getAccessKeyListKey = () => [`/auth/access-keys`] as const;

export type AccessKeyListQueryResult = NonNullable<
  Awaited<ReturnType<typeof accessKeyList>>
>;

export const useAccessKeyList = <
  TError = BadRequestResponse | ForbiddenResponse | InternalServerErrorResponse,
>(options?: {
  swr?: SWRConfiguration<Awaited<ReturnType<typeof accessKeyList>>, TError> & {
    swrKey?: Key;
    enabled?: boolean;
  };
  request?: SecondParameter<typeof fetcher>;
}) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const isEnabled = swrOptions?.enabled !== false;
  const swrKey =
    swrOptions?.swrKey ?? (() => (isEnabled ? getAccessKeyListKey() : null));
  const swrFn = () => accessKeyList(requestOptions);

  const query = useSwr<Awaited<ReturnType<typeof swrFn>>, TError>(
    swrKey,
    swrFn,
    swrOptions,
  );

  return {
    swrKey,
    ...query,
  };
};
export const getAccessKeyCreateUrl = () => {
  return `/auth/access-keys`;
};

/**
 * Create a new access key for the authenticated account. Access keys are
 * used to authenticate API requests on behalf of the account in a more
 * granular and service-friendly way than a session cookie.
 *
 * Access keys share the same roles and permissions as the owning account
 * and only provide a way to use an `Authorization` header as an way of
 * interacting with the Storyden API.
 *
 * Access keys also allow an expiry date to be set to limit how long a key
 * can be used to authenticate against the API.
 */
export const accessKeyCreate = async (
  accessKeyCreateBody?: AccessKeyCreateBody,
  options?: Parameters<typeof fetcher>[1],
): Promise<AccessKeyCreateOKResponse> => {
  return fetcher<AccessKeyCreateOKResponse>(getAccessKeyCreateUrl(), {
    ...options,
    method: "POST",
    headers: { "Content-Type": "application/json", ...options?.headers },
    body: JSON.stringify(accessKeyCreateBody),
  });
};

export const getAccessKeyCreateMutationFetcher = (
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, { arg }: { arg: AccessKeyCreateBody | undefined }) => {
    return accessKeyCreate(arg, options);
  };
};
export const getAccessKeyCreateMutationKey = () =>
  [`/auth/access-keys`] as const;

export type AccessKeyCreateMutationResult = NonNullable<
  Awaited<ReturnType<typeof accessKeyCreate>>
>;

export const useAccessKeyCreate = <
  TError = BadRequestResponse | ForbiddenResponse | InternalServerErrorResponse,
>(options?: {
  swr?: SWRMutationConfiguration<
    Awaited<ReturnType<typeof accessKeyCreate>>,
    TError,
    Key,
    AccessKeyCreateBody | undefined,
    Awaited<ReturnType<typeof accessKeyCreate>>
  > & { swrKey?: string };
  request?: SecondParameter<typeof fetcher>;
}) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey = swrOptions?.swrKey ?? getAccessKeyCreateMutationKey();
  const swrFn = getAccessKeyCreateMutationFetcher(requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getAccessKeyDeleteUrl = (accessKeyId: string) => {
  return `/auth/access-keys/${accessKeyId}`;
};

/**
 * Revoke an access key. This will immediately invalidate the key and it
 * will no longer be usable for authentication.
 */
export const accessKeyDelete = async (
  accessKeyId: string,
  options?: Parameters<typeof fetcher>[1],
): Promise<NoContentResponse> => {
  return fetcher<NoContentResponse>(getAccessKeyDeleteUrl(accessKeyId), {
    ...options,
    method: "DELETE",
  });
};

export const getAccessKeyDeleteMutationFetcher = (
  accessKeyId: string,
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, __: { arg: Arguments }) => {
    return accessKeyDelete(accessKeyId, options);
  };
};
export const getAccessKeyDeleteMutationKey = (accessKeyId: string) =>
  [`/auth/access-keys/${accessKeyId}`] as const;

export type AccessKeyDeleteMutationResult = NonNullable<
  Awaited<ReturnType<typeof accessKeyDelete>>
>;

export const useAccessKeyDelete = <
  TError = BadRequestResponse | ForbiddenResponse | InternalServerErrorResponse,
>(
  accessKeyId: string,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof accessKeyDelete>>,
      TError,
      Key,
      Arguments,
      Awaited<ReturnType<typeof accessKeyDelete>>
    > & { swrKey?: string };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey =
    swrOptions?.swrKey ?? getAccessKeyDeleteMutationKey(accessKeyId);
  const swrFn = getAccessKeyDeleteMutationFetcher(accessKeyId, requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getOAuthRefreshTokenListUrl = (
  params?: OAuthRefreshTokenListParams,
) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/auth/oauth/tokens?${stringifiedParams}`
    : `/auth/oauth/tokens`;
};

/**
 * List OAuth refresh tokens issued to the authenticated account.
 *
 * This is the member-facing "authorised applications" view: it lists apps
 * the signed-in account has authorised and can revoke. In OAuth terms,
 * these rows are grants/tokens, not application definitions.
 *
 * This may include grants for built-in first-party clients such as the
 * default Storyden CLI. Those clients are not created by the member and
 * therefore do not appear in the member OAuth client list.
 */
export const oAuthRefreshTokenList = async (
  params?: OAuthRefreshTokenListParams,
  options?: Parameters<typeof fetcher>[1],
): Promise<OAuthRefreshTokenListOKResponse> => {
  return fetcher<OAuthRefreshTokenListOKResponse>(
    getOAuthRefreshTokenListUrl(params),
    {
      ...options,
      method: "GET",
    },
  );
};

export const getOAuthRefreshTokenListKey = (
  params?: OAuthRefreshTokenListParams,
) => [`/auth/oauth/tokens`, ...(params ? [params] : [])] as const;

export type OAuthRefreshTokenListQueryResult = NonNullable<
  Awaited<ReturnType<typeof oAuthRefreshTokenList>>
>;

export const useOAuthRefreshTokenList = <
  TError = UnauthorisedResponse | InternalServerErrorResponse,
>(
  params?: OAuthRefreshTokenListParams,
  options?: {
    swr?: SWRConfiguration<
      Awaited<ReturnType<typeof oAuthRefreshTokenList>>,
      TError
    > & { swrKey?: Key; enabled?: boolean };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const isEnabled = swrOptions?.enabled !== false;
  const swrKey =
    swrOptions?.swrKey ??
    (() => (isEnabled ? getOAuthRefreshTokenListKey(params) : null));
  const swrFn = () => oAuthRefreshTokenList(params, requestOptions);

  const query = useSwr<Awaited<ReturnType<typeof swrFn>>, TError>(
    swrKey,
    swrFn,
    swrOptions,
  );

  return {
    swrKey,
    ...query,
  };
};
export const getOAuthRefreshTokenDeleteUrl = (oauthRefreshTokenId: string) => {
  return `/auth/oauth/tokens/${oauthRefreshTokenId}`;
};

/**
 * Revoke one OAuth refresh token issued to the authenticated account.
 *
 * This prevents future refresh-token use for the selected grant. Existing
 * JWT access tokens remain valid until their expiry.
 */
export const oAuthRefreshTokenDelete = async (
  oauthRefreshTokenId: string,
  options?: Parameters<typeof fetcher>[1],
): Promise<NoContentResponse> => {
  return fetcher<NoContentResponse>(
    getOAuthRefreshTokenDeleteUrl(oauthRefreshTokenId),
    {
      ...options,
      method: "DELETE",
    },
  );
};

export const getOAuthRefreshTokenDeleteMutationFetcher = (
  oauthRefreshTokenId: string,
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, __: { arg: Arguments }) => {
    return oAuthRefreshTokenDelete(oauthRefreshTokenId, options);
  };
};
export const getOAuthRefreshTokenDeleteMutationKey = (
  oauthRefreshTokenId: string,
) => [`/auth/oauth/tokens/${oauthRefreshTokenId}`] as const;

export type OAuthRefreshTokenDeleteMutationResult = NonNullable<
  Awaited<ReturnType<typeof oAuthRefreshTokenDelete>>
>;

export const useOAuthRefreshTokenDelete = <
  TError =
    OAuthErrorResponse | UnauthorisedResponse | InternalServerErrorResponse,
>(
  oauthRefreshTokenId: string,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof oAuthRefreshTokenDelete>>,
      TError,
      Key,
      Arguments,
      Awaited<ReturnType<typeof oAuthRefreshTokenDelete>>
    > & { swrKey?: string };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey =
    swrOptions?.swrKey ??
    getOAuthRefreshTokenDeleteMutationKey(oauthRefreshTokenId);
  const swrFn = getOAuthRefreshTokenDeleteMutationFetcher(
    oauthRefreshTokenId,
    requestOptions,
  );

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getOAuthClientListUrl = () => {
  return `/auth/oauth/clients`;
};

/**
 * List OAuth clients created by the authenticated account.
 *
 * This is the member-facing "apps I created" view. OAuth clients are
 * application definitions: client ID, client type, redirect URIs, allowed
 * scopes, and allowed grants.
 *
 * This does not list built-in first-party clients or third-party apps the
 * member has merely authorised. Use `/auth/oauth/tokens` for the
 * "apps I have authorised" view.
 */
export const oAuthClientList = async (
  options?: Parameters<typeof fetcher>[1],
): Promise<OAuthClientListOKResponse> => {
  return fetcher<OAuthClientListOKResponse>(getOAuthClientListUrl(), {
    ...options,
    method: "GET",
  });
};

export const getOAuthClientListKey = () => [`/auth/oauth/clients`] as const;

export type OAuthClientListQueryResult = NonNullable<
  Awaited<ReturnType<typeof oAuthClientList>>
>;

export const useOAuthClientList = <
  TError = UnauthorisedResponse | InternalServerErrorResponse,
>(options?: {
  swr?: SWRConfiguration<
    Awaited<ReturnType<typeof oAuthClientList>>,
    TError
  > & { swrKey?: Key; enabled?: boolean };
  request?: SecondParameter<typeof fetcher>;
}) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const isEnabled = swrOptions?.enabled !== false;
  const swrKey =
    swrOptions?.swrKey ?? (() => (isEnabled ? getOAuthClientListKey() : null));
  const swrFn = () => oAuthClientList(requestOptions);

  const query = useSwr<Awaited<ReturnType<typeof swrFn>>, TError>(
    swrKey,
    swrFn,
    swrOptions,
  );

  return {
    swrKey,
    ...query,
  };
};
export const getOAuthClientCreateUrl = () => {
  return `/auth/oauth/clients`;
};

/**
 * Create an OAuth client owned by the authenticated account.
 *
 * Member-created clients are third-party explicit-scope clients. The
 * requested allowed scopes must be a subset of the authenticated account's
 * current permissions.
 */
export const oAuthClientCreate = async (
  oAuthClientSelfCreateBody: OAuthClientSelfCreateBody,
  options?: Parameters<typeof fetcher>[1],
): Promise<OAuthClientIssuedOKResponse> => {
  return fetcher<OAuthClientIssuedOKResponse>(getOAuthClientCreateUrl(), {
    ...options,
    method: "POST",
    headers: { "Content-Type": "application/json", ...options?.headers },
    body: JSON.stringify(oAuthClientSelfCreateBody),
  });
};

export const getOAuthClientCreateMutationFetcher = (
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, { arg }: { arg: OAuthClientSelfCreateBody }) => {
    return oAuthClientCreate(arg, options);
  };
};
export const getOAuthClientCreateMutationKey = () =>
  [`/auth/oauth/clients`] as const;

export type OAuthClientCreateMutationResult = NonNullable<
  Awaited<ReturnType<typeof oAuthClientCreate>>
>;

export const useOAuthClientCreate = <
  TError =
    | BadRequestResponse
    | UnauthorisedResponse
    | ForbiddenResponse
    | InternalServerErrorResponse,
>(options?: {
  swr?: SWRMutationConfiguration<
    Awaited<ReturnType<typeof oAuthClientCreate>>,
    TError,
    Key,
    OAuthClientSelfCreateBody,
    Awaited<ReturnType<typeof oAuthClientCreate>>
  > & { swrKey?: string };
  request?: SecondParameter<typeof fetcher>;
}) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey = swrOptions?.swrKey ?? getOAuthClientCreateMutationKey();
  const swrFn = getOAuthClientCreateMutationFetcher(requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getOAuthClientGetUrl = (oauthClientId: string) => {
  return `/auth/oauth/clients/${oauthClientId}`;
};

/**
 * Read an OAuth client created by the authenticated account.
 *
 * Member-created clients are third-party application identities. They may
 * be public or confidential but are never first-party inherited-permission
 * clients.
 */
export const oAuthClientGet = async (
  oauthClientId: string,
  options?: Parameters<typeof fetcher>[1],
): Promise<OAuthClientOKResponse> => {
  return fetcher<OAuthClientOKResponse>(getOAuthClientGetUrl(oauthClientId), {
    ...options,
    method: "GET",
  });
};

export const getOAuthClientGetKey = (oauthClientId: string) =>
  [`/auth/oauth/clients/${oauthClientId}`] as const;

export type OAuthClientGetQueryResult = NonNullable<
  Awaited<ReturnType<typeof oAuthClientGet>>
>;

export const useOAuthClientGet = <
  TError =
    OAuthErrorResponse | UnauthorisedResponse | InternalServerErrorResponse,
>(
  oauthClientId: string,
  options?: {
    swr?: SWRConfiguration<
      Awaited<ReturnType<typeof oAuthClientGet>>,
      TError
    > & { swrKey?: Key; enabled?: boolean };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const isEnabled =
    swrOptions?.enabled !== false &&
    oauthClientId !== null &&
    oauthClientId !== undefined;
  const swrKey =
    swrOptions?.swrKey ??
    (() => (isEnabled ? getOAuthClientGetKey(oauthClientId) : null));
  const swrFn = () => oAuthClientGet(oauthClientId, requestOptions);

  const query = useSwr<Awaited<ReturnType<typeof swrFn>>, TError>(
    swrKey,
    swrFn,
    swrOptions,
  );

  return {
    swrKey,
    ...query,
  };
};
export const getOAuthClientUpdateUrl = (oauthClientId: string) => {
  return `/auth/oauth/clients/${oauthClientId}`;
};

/**
 * Update an OAuth client created by the authenticated account.
 *
 * Allowed scopes must remain within the authenticated account's current
 * permissions. If the account has `ADMINISTRATOR`, it may configure any
 * Storyden permission scope because `ADMINISTRATOR` implicitly grants all
 * permissions.
 *
 * Changing allowed scopes affects future grants and refreshes but does
 * not immediately invalidate already-issued JWT access tokens.
 */
export const oAuthClientUpdate = async (
  oauthClientId: string,
  oAuthClientSelfUpdateBody: OAuthClientSelfUpdateBody,
  options?: Parameters<typeof fetcher>[1],
): Promise<OAuthClientOKResponse> => {
  return fetcher<OAuthClientOKResponse>(
    getOAuthClientUpdateUrl(oauthClientId),
    {
      ...options,
      method: "PATCH",
      headers: { "Content-Type": "application/json", ...options?.headers },
      body: JSON.stringify(oAuthClientSelfUpdateBody),
    },
  );
};

export const getOAuthClientUpdateMutationFetcher = (
  oauthClientId: string,
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, { arg }: { arg: OAuthClientSelfUpdateBody }) => {
    return oAuthClientUpdate(oauthClientId, arg, options);
  };
};
export const getOAuthClientUpdateMutationKey = (oauthClientId: string) =>
  [`/auth/oauth/clients/${oauthClientId}`] as const;

export type OAuthClientUpdateMutationResult = NonNullable<
  Awaited<ReturnType<typeof oAuthClientUpdate>>
>;

export const useOAuthClientUpdate = <
  TError =
    | BadRequestResponse
    | UnauthorisedResponse
    | ForbiddenResponse
    | InternalServerErrorResponse,
>(
  oauthClientId: string,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof oAuthClientUpdate>>,
      TError,
      Key,
      OAuthClientSelfUpdateBody,
      Awaited<ReturnType<typeof oAuthClientUpdate>>
    > & { swrKey?: string };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey =
    swrOptions?.swrKey ?? getOAuthClientUpdateMutationKey(oauthClientId);
  const swrFn = getOAuthClientUpdateMutationFetcher(
    oauthClientId,
    requestOptions,
  );

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getOAuthClientDeleteUrl = (oauthClientId: string) => {
  return `/auth/oauth/clients/${oauthClientId}`;
};

/**
 * Delete an OAuth client created by the authenticated account.
 *
 * This prevents new OAuth flows for the client and removes associated
 * pending OAuth records and refresh tokens, preventing existing grants
 * from being renewed. Existing JWT access tokens remain valid until
 * expiry.
 */
export const oAuthClientDelete = async (
  oauthClientId: string,
  options?: Parameters<typeof fetcher>[1],
): Promise<NoContentResponse> => {
  return fetcher<NoContentResponse>(getOAuthClientDeleteUrl(oauthClientId), {
    ...options,
    method: "DELETE",
  });
};

export const getOAuthClientDeleteMutationFetcher = (
  oauthClientId: string,
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, __: { arg: Arguments }) => {
    return oAuthClientDelete(oauthClientId, options);
  };
};
export const getOAuthClientDeleteMutationKey = (oauthClientId: string) =>
  [`/auth/oauth/clients/${oauthClientId}`] as const;

export type OAuthClientDeleteMutationResult = NonNullable<
  Awaited<ReturnType<typeof oAuthClientDelete>>
>;

export const useOAuthClientDelete = <
  TError =
    OAuthErrorResponse | UnauthorisedResponse | InternalServerErrorResponse,
>(
  oauthClientId: string,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof oAuthClientDelete>>,
      TError,
      Key,
      Arguments,
      Awaited<ReturnType<typeof oAuthClientDelete>>
    > & { swrKey?: string };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey =
    swrOptions?.swrKey ?? getOAuthClientDeleteMutationKey(oauthClientId);
  const swrFn = getOAuthClientDeleteMutationFetcher(
    oauthClientId,
    requestOptions,
  );

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getAuthProviderLogoutUrl = (params?: AuthProviderLogoutParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/auth/logout?${stringifiedParams}`
    : `/auth/logout`;
};

/**
 * Performs a HTTP logout by clearing the session cookie and redirecting to
 * to the requested path at the frontend's `WEB_ADDRESS`. Typically this
 * may be a secondary logout route on the frontend implementation that can
 * handle any frontend-specific logout tasks. This is necessary in cases
 * where the frontend is running on a different origin to the API service
 * such as api.site.com vs site.com because Clear-Site-Data and other
 * headers are same-origin compliant and won't work cross-origin.
 */
export const authProviderLogout = async (
  params?: AuthProviderLogoutParams,
  options?: Parameters<typeof fetcher>[1],
): Promise<unknown> => {
  return fetcher<unknown>(getAuthProviderLogoutUrl(params), {
    ...options,
    method: "POST",
  });
};

export const getAuthProviderLogoutMutationFetcher = (
  params?: AuthProviderLogoutParams,
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, __: { arg: Arguments }) => {
    return authProviderLogout(params, options);
  };
};
export const getAuthProviderLogoutMutationKey = (
  params?: AuthProviderLogoutParams,
) => [`/auth/logout`, ...(params ? [params] : [])] as const;

export type AuthProviderLogoutMutationResult = NonNullable<
  Awaited<ReturnType<typeof authProviderLogout>>
>;

export const useAuthProviderLogout = <TError = void>(
  params?: AuthProviderLogoutParams,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof authProviderLogout>>,
      TError,
      Key,
      Arguments,
      Awaited<ReturnType<typeof authProviderLogout>>
    > & { swrKey?: string };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey = swrOptions?.swrKey ?? getAuthProviderLogoutMutationKey(params);
  const swrFn = getAuthProviderLogoutMutationFetcher(params, requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
