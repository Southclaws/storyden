/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * storyden
 * Storyden social API for building community driven platforms.
 * The Storyden API does not adhere to semantic versioning but instead applies a rolling strategy with deprecations and minimal breaking changes. This has been done mainly for a simpler development process and it may be changed to a more fixed versioning strategy in the future. Ultimately, the primary way Storyden tracks versions is dates, there are no set release tags currently.
 *
 * OpenAPI spec version: v1.26.15-post
 */
import useSwr from "swr";
import type { Arguments, Key, SWRConfiguration } from "swr";
import useSWRMutation from "swr/mutation";
import type { SWRMutationConfiguration } from "swr/mutation";

import { fetcher } from "../client";
import type {
  InternalServerErrorResponse,
  LikePostGetOKResponse,
  LikeProfileGetOKResponse,
  LikeProfileGetParams,
  NotFoundResponse,
  UnauthorisedResponse,
} from "../openapi-schema";

type SecondParameter<T extends (...args: never) => unknown> = Parameters<T>[1];

export const getLikePostGetUrl = (postId: string) => {
  return `/likes/posts/${postId}`;
};

/**
 * Retreives all likes for the given post. Not paginated (yet.)
 */
export const likePostGet = async (
  postId: string,
  options?: Parameters<typeof fetcher>[1],
): Promise<LikePostGetOKResponse> => {
  return fetcher<LikePostGetOKResponse>(getLikePostGetUrl(postId), {
    ...options,
    method: "GET",
  });
};

export const getLikePostGetKey = (postId: string) =>
  [`/likes/posts/${postId}`] as const;

export type LikePostGetQueryResult = NonNullable<
  Awaited<ReturnType<typeof likePostGet>>
>;

export const useLikePostGet = <
  TError =
    UnauthorisedResponse | NotFoundResponse | InternalServerErrorResponse,
>(
  postId: string,
  options?: {
    swr?: SWRConfiguration<Awaited<ReturnType<typeof likePostGet>>, TError> & {
      swrKey?: Key;
      enabled?: boolean;
    };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const isEnabled =
    swrOptions?.enabled !== false && postId !== null && postId !== undefined;
  const swrKey =
    swrOptions?.swrKey ??
    (() => (isEnabled ? getLikePostGetKey(postId) : null));
  const swrFn = () => likePostGet(postId, requestOptions);

  const query = useSwr<Awaited<ReturnType<typeof swrFn>>, TError>(
    swrKey,
    swrFn,
    swrOptions,
  );

  return {
    swrKey,
    ...query,
  };
};
export const getLikePostAddUrl = (postId: string) => {
  return `/likes/posts/${postId}`;
};

/**
 * Add a like/vote to a post. A "like" is pretty much what you'd expect for
 * any modern social platform, it will inform the feed algorithm and the
 * account's recommendations as well as listing the post on their profile.
 * Idempotent operation where repeated use will do nothing.
 */
export const likePostAdd = async (
  postId: string,
  options?: Parameters<typeof fetcher>[1],
): Promise<void> => {
  return fetcher<void>(getLikePostAddUrl(postId), {
    ...options,
    method: "PUT",
  });
};

export const getLikePostAddMutationFetcher = (
  postId: string,
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, __: { arg: Arguments }) => {
    return likePostAdd(postId, options);
  };
};
export const getLikePostAddMutationKey = (postId: string) =>
  [`/likes/posts/${postId}`] as const;

export type LikePostAddMutationResult = NonNullable<
  Awaited<ReturnType<typeof likePostAdd>>
>;

export const useLikePostAdd = <
  TError =
    UnauthorisedResponse | NotFoundResponse | InternalServerErrorResponse,
>(
  postId: string,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof likePostAdd>>,
      TError,
      Key,
      Arguments,
      Awaited<ReturnType<typeof likePostAdd>>
    > & { swrKey?: string };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey = swrOptions?.swrKey ?? getLikePostAddMutationKey(postId);
  const swrFn = getLikePostAddMutationFetcher(postId, requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getLikePostRemoveUrl = (postId: string) => {
  return `/likes/posts/${postId}`;
};

/**
 * Removes a like/vote from the authenticated account for the post. It will
 * perform the inverse of any changes to the account's algorithm. Also is
 * idempotent, so repeated use will do nothing after being actioned once.
 */
export const likePostRemove = async (
  postId: string,
  options?: Parameters<typeof fetcher>[1],
): Promise<void> => {
  return fetcher<void>(getLikePostRemoveUrl(postId), {
    ...options,
    method: "DELETE",
  });
};

export const getLikePostRemoveMutationFetcher = (
  postId: string,
  options?: SecondParameter<typeof fetcher>,
) => {
  return (_: Key, __: { arg: Arguments }) => {
    return likePostRemove(postId, options);
  };
};
export const getLikePostRemoveMutationKey = (postId: string) =>
  [`/likes/posts/${postId}`] as const;

export type LikePostRemoveMutationResult = NonNullable<
  Awaited<ReturnType<typeof likePostRemove>>
>;

export const useLikePostRemove = <
  TError =
    UnauthorisedResponse | NotFoundResponse | InternalServerErrorResponse,
>(
  postId: string,
  options?: {
    swr?: SWRMutationConfiguration<
      Awaited<ReturnType<typeof likePostRemove>>,
      TError,
      Key,
      Arguments,
      Awaited<ReturnType<typeof likePostRemove>>
    > & { swrKey?: string };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const swrKey = swrOptions?.swrKey ?? getLikePostRemoveMutationKey(postId);
  const swrFn = getLikePostRemoveMutationFetcher(postId, requestOptions);

  const query = useSWRMutation(swrKey, swrFn, swrOptions);

  return {
    swrKey,
    ...query,
  };
};
export const getLikeProfileGetUrl = (
  accountHandle: string,
  params?: LikeProfileGetParams,
) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/likes/profiles/${accountHandle}?${stringifiedParams}`
    : `/likes/profiles/${accountHandle}`;
};

/**
 * Retreives all the likes that the given profile has given.
 */
export const likeProfileGet = async (
  accountHandle: string,
  params?: LikeProfileGetParams,
  options?: Parameters<typeof fetcher>[1],
): Promise<LikeProfileGetOKResponse> => {
  return fetcher<LikeProfileGetOKResponse>(
    getLikeProfileGetUrl(accountHandle, params),
    {
      ...options,
      method: "GET",
    },
  );
};

export const getLikeProfileGetKey = (
  accountHandle: string,
  params?: LikeProfileGetParams,
) => [`/likes/profiles/${accountHandle}`, ...(params ? [params] : [])] as const;

export type LikeProfileGetQueryResult = NonNullable<
  Awaited<ReturnType<typeof likeProfileGet>>
>;

export const useLikeProfileGet = <
  TError =
    UnauthorisedResponse | NotFoundResponse | InternalServerErrorResponse,
>(
  accountHandle: string,
  params?: LikeProfileGetParams,
  options?: {
    swr?: SWRConfiguration<
      Awaited<ReturnType<typeof likeProfileGet>>,
      TError
    > & { swrKey?: Key; enabled?: boolean };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const isEnabled =
    swrOptions?.enabled !== false &&
    accountHandle !== null &&
    accountHandle !== undefined;
  const swrKey =
    swrOptions?.swrKey ??
    (() => (isEnabled ? getLikeProfileGetKey(accountHandle, params) : null));
  const swrFn = () => likeProfileGet(accountHandle, params, requestOptions);

  const query = useSwr<Awaited<ReturnType<typeof swrFn>>, TError>(
    swrKey,
    swrFn,
    swrOptions,
  );

  return {
    swrKey,
    ...query,
  };
};
