/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * storyden
 * Storyden social API for building community driven platforms.
 * The Storyden API does not adhere to semantic versioning but instead applies a rolling strategy with deprecations and minimal breaking changes. This has been done mainly for a simpler development process and it may be changed to a more fixed versioning strategy in the future. Ultimately, the primary way Storyden tracks versions is dates, there are no set release tags currently.
 *
 * OpenAPI spec version: v1.26.15-post
 */
import useSwr from "swr";
import type { Key, SWRConfiguration } from "swr";

import { fetcher } from "../client";
import type {
  InternalServerErrorResponse,
  NotFoundResponse,
  TagGetOKResponse,
  TagListOKResponse,
  TagListParams,
} from "../openapi-schema";

type SecondParameter<T extends (...args: never) => unknown> = Parameters<T>[1];

export const getTagListUrl = (params?: TagListParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/tags?${stringifiedParams}` : `/tags`;
};

/**
 * Get a list of tags on the site, most used first.
 *
 * The tag table grows with content, so results are paginated. Callers
 * that only need suggestions can read the first page and stop.
 */
export const tagList = async (
  params?: TagListParams,
  options?: Parameters<typeof fetcher>[1],
): Promise<TagListOKResponse> => {
  return fetcher<TagListOKResponse>(getTagListUrl(params), {
    ...options,
    method: "GET",
  });
};

export const getTagListKey = (params?: TagListParams) =>
  [`/tags`, ...(params ? [params] : [])] as const;

export type TagListQueryResult = NonNullable<
  Awaited<ReturnType<typeof tagList>>
>;

export const useTagList = <TError = InternalServerErrorResponse>(
  params?: TagListParams,
  options?: {
    swr?: SWRConfiguration<Awaited<ReturnType<typeof tagList>>, TError> & {
      swrKey?: Key;
      enabled?: boolean;
    };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const isEnabled = swrOptions?.enabled !== false;
  const swrKey =
    swrOptions?.swrKey ?? (() => (isEnabled ? getTagListKey(params) : null));
  const swrFn = () => tagList(params, requestOptions);

  const query = useSwr<Awaited<ReturnType<typeof swrFn>>, TError>(
    swrKey,
    swrFn,
    swrOptions,
  );

  return {
    swrKey,
    ...query,
  };
};
export const getTagGetUrl = (tagName: string) => {
  return `/tags/${tagName}`;
};

/**
 * Get information about a tag.
 */
export const tagGet = async (
  tagName: string,
  options?: Parameters<typeof fetcher>[1],
): Promise<TagGetOKResponse> => {
  return fetcher<TagGetOKResponse>(getTagGetUrl(tagName), {
    ...options,
    method: "GET",
  });
};

export const getTagGetKey = (tagName: string) => [`/tags/${tagName}`] as const;

export type TagGetQueryResult = NonNullable<Awaited<ReturnType<typeof tagGet>>>;

export const useTagGet = <
  TError = NotFoundResponse | InternalServerErrorResponse,
>(
  tagName: string,
  options?: {
    swr?: SWRConfiguration<Awaited<ReturnType<typeof tagGet>>, TError> & {
      swrKey?: Key;
      enabled?: boolean;
    };
    request?: SecondParameter<typeof fetcher>;
  },
) => {
  const { swr: swrOptions, request: requestOptions } = options ?? {};

  const isEnabled =
    swrOptions?.enabled !== false && tagName !== null && tagName !== undefined;
  const swrKey =
    swrOptions?.swrKey ?? (() => (isEnabled ? getTagGetKey(tagName) : null));
  const swrFn = () => tagGet(tagName, requestOptions);

  const query = useSwr<Awaited<ReturnType<typeof swrFn>>, TError>(
    swrKey,
    swrFn,
    swrOptions,
  );

  return {
    swrKey,
    ...query,
  };
};
