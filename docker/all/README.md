# all

# all 镜像

这个镜像在同一个容器中同时包含 API 和前端应用。

This image contains both the API and the frontend application, all in the same container.

对大多数小型/中型部署来说，这个镜像应该已经足够。

For most small/medium deployments, this should be sufficient.
