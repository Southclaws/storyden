// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/Southclaws/storyden/internal/ent/trailschedulerlease"
)

// TrailSchedulerLease is the model entity for the TrailSchedulerLease schema.
type TrailSchedulerLease struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// LeaseToken holds the value of the "lease_token" field.
	LeaseToken *string `json:"lease_token,omitempty"`
	// LeaseExpiresAt holds the value of the "lease_expires_at" field.
	LeaseExpiresAt *time.Time `json:"lease_expires_at,omitempty"`
	selectValues   sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*TrailSchedulerLease) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case trailschedulerlease.FieldID:
			values[i] = new(sql.NullInt64)
		case trailschedulerlease.FieldLeaseToken:
			values[i] = new(sql.NullString)
		case trailschedulerlease.FieldLeaseExpiresAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the TrailSchedulerLease fields.
func (_m *TrailSchedulerLease) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case trailschedulerlease.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case trailschedulerlease.FieldLeaseToken:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field lease_token", values[i])
			} else if value.Valid {
				_m.LeaseToken = new(string)
				*_m.LeaseToken = value.String
			}
		case trailschedulerlease.FieldLeaseExpiresAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field lease_expires_at", values[i])
			} else if value.Valid {
				_m.LeaseExpiresAt = new(time.Time)
				*_m.LeaseExpiresAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the TrailSchedulerLease.
// This includes values selected through modifiers, order, etc.
func (_m *TrailSchedulerLease) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this TrailSchedulerLease.
// Note that you need to call TrailSchedulerLease.Unwrap() before calling this method if this TrailSchedulerLease
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *TrailSchedulerLease) Update() *TrailSchedulerLeaseUpdateOne {
	return NewTrailSchedulerLeaseClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the TrailSchedulerLease entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *TrailSchedulerLease) Unwrap() *TrailSchedulerLease {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: TrailSchedulerLease is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *TrailSchedulerLease) String() string {
	var builder strings.Builder
	builder.WriteString("TrailSchedulerLease(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	if v := _m.LeaseToken; v != nil {
		builder.WriteString("lease_token=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	if v := _m.LeaseExpiresAt; v != nil {
		builder.WriteString("lease_expires_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteByte(')')
	return builder.String()
}

// TrailSchedulerLeases is a parsable slice of TrailSchedulerLease.
type TrailSchedulerLeases []*TrailSchedulerLease
