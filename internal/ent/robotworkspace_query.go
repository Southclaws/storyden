// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/Southclaws/storyden/internal/ent/account"
	"github.com/Southclaws/storyden/internal/ent/predicate"
	"github.com/Southclaws/storyden/internal/ent/robot"
	"github.com/Southclaws/storyden/internal/ent/robotworkspace"
	"github.com/Southclaws/storyden/internal/ent/robotworkspaceinstance"
	"github.com/rs/xid"
)

// RobotWorkspaceQuery is the builder for querying RobotWorkspace entities.
type RobotWorkspaceQuery struct {
	config
	ctx           *QueryContext
	order         []robotworkspace.OrderOption
	inters        []Interceptor
	predicates    []predicate.RobotWorkspace
	withCreator   *AccountQuery
	withRobots    *RobotQuery
	withInstances *RobotWorkspaceInstanceQuery
	modifiers     []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the RobotWorkspaceQuery builder.
func (_q *RobotWorkspaceQuery) Where(ps ...predicate.RobotWorkspace) *RobotWorkspaceQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *RobotWorkspaceQuery) Limit(limit int) *RobotWorkspaceQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *RobotWorkspaceQuery) Offset(offset int) *RobotWorkspaceQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *RobotWorkspaceQuery) Unique(unique bool) *RobotWorkspaceQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *RobotWorkspaceQuery) Order(o ...robotworkspace.OrderOption) *RobotWorkspaceQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryCreator chains the current query on the "creator" edge.
func (_q *RobotWorkspaceQuery) QueryCreator() *AccountQuery {
	query := (&AccountClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(robotworkspace.Table, robotworkspace.FieldID, selector),
			sqlgraph.To(account.Table, account.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, robotworkspace.CreatorTable, robotworkspace.CreatorColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryRobots chains the current query on the "robots" edge.
func (_q *RobotWorkspaceQuery) QueryRobots() *RobotQuery {
	query := (&RobotClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(robotworkspace.Table, robotworkspace.FieldID, selector),
			sqlgraph.To(robot.Table, robot.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, robotworkspace.RobotsTable, robotworkspace.RobotsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryInstances chains the current query on the "instances" edge.
func (_q *RobotWorkspaceQuery) QueryInstances() *RobotWorkspaceInstanceQuery {
	query := (&RobotWorkspaceInstanceClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(robotworkspace.Table, robotworkspace.FieldID, selector),
			sqlgraph.To(robotworkspaceinstance.Table, robotworkspaceinstance.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, robotworkspace.InstancesTable, robotworkspace.InstancesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first RobotWorkspace entity from the query.
// Returns a *NotFoundError when no RobotWorkspace was found.
func (_q *RobotWorkspaceQuery) First(ctx context.Context) (*RobotWorkspace, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{robotworkspace.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *RobotWorkspaceQuery) FirstX(ctx context.Context) *RobotWorkspace {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first RobotWorkspace ID from the query.
// Returns a *NotFoundError when no RobotWorkspace ID was found.
func (_q *RobotWorkspaceQuery) FirstID(ctx context.Context) (id xid.ID, err error) {
	var ids []xid.ID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{robotworkspace.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *RobotWorkspaceQuery) FirstIDX(ctx context.Context) xid.ID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single RobotWorkspace entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one RobotWorkspace entity is found.
// Returns a *NotFoundError when no RobotWorkspace entities are found.
func (_q *RobotWorkspaceQuery) Only(ctx context.Context) (*RobotWorkspace, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{robotworkspace.Label}
	default:
		return nil, &NotSingularError{robotworkspace.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *RobotWorkspaceQuery) OnlyX(ctx context.Context) *RobotWorkspace {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only RobotWorkspace ID in the query.
// Returns a *NotSingularError when more than one RobotWorkspace ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *RobotWorkspaceQuery) OnlyID(ctx context.Context) (id xid.ID, err error) {
	var ids []xid.ID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{robotworkspace.Label}
	default:
		err = &NotSingularError{robotworkspace.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *RobotWorkspaceQuery) OnlyIDX(ctx context.Context) xid.ID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of RobotWorkspaces.
func (_q *RobotWorkspaceQuery) All(ctx context.Context) ([]*RobotWorkspace, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*RobotWorkspace, *RobotWorkspaceQuery]()
	return withInterceptors[[]*RobotWorkspace](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *RobotWorkspaceQuery) AllX(ctx context.Context) []*RobotWorkspace {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of RobotWorkspace IDs.
func (_q *RobotWorkspaceQuery) IDs(ctx context.Context) (ids []xid.ID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(robotworkspace.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *RobotWorkspaceQuery) IDsX(ctx context.Context) []xid.ID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *RobotWorkspaceQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*RobotWorkspaceQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *RobotWorkspaceQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *RobotWorkspaceQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *RobotWorkspaceQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the RobotWorkspaceQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *RobotWorkspaceQuery) Clone() *RobotWorkspaceQuery {
	if _q == nil {
		return nil
	}
	return &RobotWorkspaceQuery{
		config:        _q.config,
		ctx:           _q.ctx.Clone(),
		order:         append([]robotworkspace.OrderOption{}, _q.order...),
		inters:        append([]Interceptor{}, _q.inters...),
		predicates:    append([]predicate.RobotWorkspace{}, _q.predicates...),
		withCreator:   _q.withCreator.Clone(),
		withRobots:    _q.withRobots.Clone(),
		withInstances: _q.withInstances.Clone(),
		// clone intermediate query.
		sql:       _q.sql.Clone(),
		path:      _q.path,
		modifiers: append([]func(*sql.Selector){}, _q.modifiers...),
	}
}

// WithCreator tells the query-builder to eager-load the nodes that are connected to
// the "creator" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *RobotWorkspaceQuery) WithCreator(opts ...func(*AccountQuery)) *RobotWorkspaceQuery {
	query := (&AccountClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withCreator = query
	return _q
}

// WithRobots tells the query-builder to eager-load the nodes that are connected to
// the "robots" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *RobotWorkspaceQuery) WithRobots(opts ...func(*RobotQuery)) *RobotWorkspaceQuery {
	query := (&RobotClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withRobots = query
	return _q
}

// WithInstances tells the query-builder to eager-load the nodes that are connected to
// the "instances" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *RobotWorkspaceQuery) WithInstances(opts ...func(*RobotWorkspaceInstanceQuery)) *RobotWorkspaceQuery {
	query := (&RobotWorkspaceInstanceClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withInstances = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.RobotWorkspace.Query().
//		GroupBy(robotworkspace.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *RobotWorkspaceQuery) GroupBy(field string, fields ...string) *RobotWorkspaceGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &RobotWorkspaceGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = robotworkspace.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.RobotWorkspace.Query().
//		Select(robotworkspace.FieldCreatedAt).
//		Scan(ctx, &v)
func (_q *RobotWorkspaceQuery) Select(fields ...string) *RobotWorkspaceSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &RobotWorkspaceSelect{RobotWorkspaceQuery: _q}
	sbuild.label = robotworkspace.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a RobotWorkspaceSelect configured with the given aggregations.
func (_q *RobotWorkspaceQuery) Aggregate(fns ...AggregateFunc) *RobotWorkspaceSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *RobotWorkspaceQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !robotworkspace.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *RobotWorkspaceQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*RobotWorkspace, error) {
	var (
		nodes       = []*RobotWorkspace{}
		_spec       = _q.querySpec()
		loadedTypes = [3]bool{
			_q.withCreator != nil,
			_q.withRobots != nil,
			_q.withInstances != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*RobotWorkspace).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &RobotWorkspace{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withCreator; query != nil {
		if err := _q.loadCreator(ctx, query, nodes, nil,
			func(n *RobotWorkspace, e *Account) { n.Edges.Creator = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withRobots; query != nil {
		if err := _q.loadRobots(ctx, query, nodes,
			func(n *RobotWorkspace) { n.Edges.Robots = []*Robot{} },
			func(n *RobotWorkspace, e *Robot) { n.Edges.Robots = append(n.Edges.Robots, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withInstances; query != nil {
		if err := _q.loadInstances(ctx, query, nodes,
			func(n *RobotWorkspace) { n.Edges.Instances = []*RobotWorkspaceInstance{} },
			func(n *RobotWorkspace, e *RobotWorkspaceInstance) { n.Edges.Instances = append(n.Edges.Instances, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *RobotWorkspaceQuery) loadCreator(ctx context.Context, query *AccountQuery, nodes []*RobotWorkspace, init func(*RobotWorkspace), assign func(*RobotWorkspace, *Account)) error {
	ids := make([]xid.ID, 0, len(nodes))
	nodeids := make(map[xid.ID][]*RobotWorkspace)
	for i := range nodes {
		fk := nodes[i].CreatedBy
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(account.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "created_by" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *RobotWorkspaceQuery) loadRobots(ctx context.Context, query *RobotQuery, nodes []*RobotWorkspace, init func(*RobotWorkspace), assign func(*RobotWorkspace, *Robot)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[xid.ID]*RobotWorkspace)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(robot.FieldWorkspaceID)
	}
	query.Where(predicate.Robot(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(robotworkspace.RobotsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.WorkspaceID
		if fk == nil {
			return fmt.Errorf(`foreign-key "workspace_id" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "workspace_id" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *RobotWorkspaceQuery) loadInstances(ctx context.Context, query *RobotWorkspaceInstanceQuery, nodes []*RobotWorkspace, init func(*RobotWorkspace), assign func(*RobotWorkspace, *RobotWorkspaceInstance)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[xid.ID]*RobotWorkspace)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(robotworkspaceinstance.FieldWorkspaceID)
	}
	query.Where(predicate.RobotWorkspaceInstance(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(robotworkspace.InstancesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.WorkspaceID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "workspace_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *RobotWorkspaceQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *RobotWorkspaceQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(robotworkspace.Table, robotworkspace.Columns, sqlgraph.NewFieldSpec(robotworkspace.FieldID, field.TypeString))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, robotworkspace.FieldID)
		for i := range fields {
			if fields[i] != robotworkspace.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withCreator != nil {
			_spec.Node.AddColumnOnce(robotworkspace.FieldCreatedBy)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *RobotWorkspaceQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(robotworkspace.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = robotworkspace.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range _q.modifiers {
		m(selector)
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (_q *RobotWorkspaceQuery) Modify(modifiers ...func(s *sql.Selector)) *RobotWorkspaceSelect {
	_q.modifiers = append(_q.modifiers, modifiers...)
	return _q.Select()
}

// RobotWorkspaceGroupBy is the group-by builder for RobotWorkspace entities.
type RobotWorkspaceGroupBy struct {
	selector
	build *RobotWorkspaceQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *RobotWorkspaceGroupBy) Aggregate(fns ...AggregateFunc) *RobotWorkspaceGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *RobotWorkspaceGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RobotWorkspaceQuery, *RobotWorkspaceGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *RobotWorkspaceGroupBy) sqlScan(ctx context.Context, root *RobotWorkspaceQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// RobotWorkspaceSelect is the builder for selecting fields of RobotWorkspace entities.
type RobotWorkspaceSelect struct {
	*RobotWorkspaceQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *RobotWorkspaceSelect) Aggregate(fns ...AggregateFunc) *RobotWorkspaceSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *RobotWorkspaceSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RobotWorkspaceQuery, *RobotWorkspaceSelect](ctx, _s.RobotWorkspaceQuery, _s, _s.inters, v)
}

func (_s *RobotWorkspaceSelect) sqlScan(ctx context.Context, root *RobotWorkspaceQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (_s *RobotWorkspaceSelect) Modify(modifiers ...func(s *sql.Selector)) *RobotWorkspaceSelect {
	_s.modifiers = append(_s.modifiers, modifiers...)
	return _s
}
