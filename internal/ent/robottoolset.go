// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/Southclaws/storyden/internal/ent/account"
	"github.com/Southclaws/storyden/internal/ent/robottoolset"
	"github.com/rs/xid"
)

// RobotToolset is the model entity for the RobotToolset schema.
type RobotToolset struct {
	config `json:"-"`
	// ID of the ent.
	ID xid.ID `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Human-readable summary used for Toolset discovery
	Description string `json:"description,omitempty"`
	// Prompt guidance injected whenever the Toolset is active
	Instruction string `json:"instruction,omitempty"`
	// Stable native, MCP, or plugin tool identifiers in this Toolset
	Tools []string `json:"tools,omitempty"`
	// Arbitrary metadata used by clients
	Metadata map[string]interface{} `json:"metadata,omitempty"`
	// AuthorID holds the value of the "author_id" field.
	AuthorID xid.ID `json:"author_id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the RobotToolsetQuery when eager-loading is set.
	Edges        RobotToolsetEdges `json:"edges"`
	selectValues sql.SelectValues
}

// RobotToolsetEdges holds the relations/edges for other nodes in the graph.
type RobotToolsetEdges struct {
	// Author holds the value of the author edge.
	Author *Account `json:"author,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// AuthorOrErr returns the Author value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e RobotToolsetEdges) AuthorOrErr() (*Account, error) {
	if e.Author != nil {
		return e.Author, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: account.Label}
	}
	return nil, &NotLoadedError{edge: "author"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*RobotToolset) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case robottoolset.FieldTools, robottoolset.FieldMetadata:
			values[i] = new([]byte)
		case robottoolset.FieldName, robottoolset.FieldDescription, robottoolset.FieldInstruction:
			values[i] = new(sql.NullString)
		case robottoolset.FieldCreatedAt, robottoolset.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case robottoolset.FieldID, robottoolset.FieldAuthorID:
			values[i] = new(xid.ID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the RobotToolset fields.
func (_m *RobotToolset) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case robottoolset.FieldID:
			if value, ok := values[i].(*xid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case robottoolset.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case robottoolset.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		case robottoolset.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case robottoolset.FieldDescription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description", values[i])
			} else if value.Valid {
				_m.Description = value.String
			}
		case robottoolset.FieldInstruction:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field instruction", values[i])
			} else if value.Valid {
				_m.Instruction = value.String
			}
		case robottoolset.FieldTools:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field tools", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.Tools); err != nil {
					return fmt.Errorf("unmarshal field tools: %w", err)
				}
			}
		case robottoolset.FieldMetadata:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field metadata", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.Metadata); err != nil {
					return fmt.Errorf("unmarshal field metadata: %w", err)
				}
			}
		case robottoolset.FieldAuthorID:
			if value, ok := values[i].(*xid.ID); !ok {
				return fmt.Errorf("unexpected type %T for field author_id", values[i])
			} else if value != nil {
				_m.AuthorID = *value
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the RobotToolset.
// This includes values selected through modifiers, order, etc.
func (_m *RobotToolset) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryAuthor queries the "author" edge of the RobotToolset entity.
func (_m *RobotToolset) QueryAuthor() *AccountQuery {
	return NewRobotToolsetClient(_m.config).QueryAuthor(_m)
}

// Update returns a builder for updating this RobotToolset.
// Note that you need to call RobotToolset.Unwrap() before calling this method if this RobotToolset
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *RobotToolset) Update() *RobotToolsetUpdateOne {
	return NewRobotToolsetClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the RobotToolset entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *RobotToolset) Unwrap() *RobotToolset {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: RobotToolset is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *RobotToolset) String() string {
	var builder strings.Builder
	builder.WriteString("RobotToolset(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("description=")
	builder.WriteString(_m.Description)
	builder.WriteString(", ")
	builder.WriteString("instruction=")
	builder.WriteString(_m.Instruction)
	builder.WriteString(", ")
	builder.WriteString("tools=")
	builder.WriteString(fmt.Sprintf("%v", _m.Tools))
	builder.WriteString(", ")
	builder.WriteString("metadata=")
	builder.WriteString(fmt.Sprintf("%v", _m.Metadata))
	builder.WriteString(", ")
	builder.WriteString("author_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.AuthorID))
	builder.WriteByte(')')
	return builder.String()
}

// RobotToolsets is a parsable slice of RobotToolset.
type RobotToolsets []*RobotToolset
