// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/Southclaws/storyden/internal/ent/account"
	"github.com/Southclaws/storyden/internal/ent/predicate"
	"github.com/Southclaws/storyden/internal/ent/trail"
	"github.com/Southclaws/storyden/internal/ent/trailactionrun"
	"github.com/Southclaws/storyden/internal/ent/trailrun"
	"github.com/rs/xid"
)

// TrailRunQuery is the builder for querying TrailRun entities.
type TrailRunQuery struct {
	config
	ctx            *QueryContext
	order          []trailrun.OrderOption
	inters         []Interceptor
	predicates     []predicate.TrailRun
	withTrail      *TrailQuery
	withInitiator  *AccountQuery
	withActionRuns *TrailActionRunQuery
	modifiers      []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the TrailRunQuery builder.
func (_q *TrailRunQuery) Where(ps ...predicate.TrailRun) *TrailRunQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *TrailRunQuery) Limit(limit int) *TrailRunQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *TrailRunQuery) Offset(offset int) *TrailRunQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *TrailRunQuery) Unique(unique bool) *TrailRunQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *TrailRunQuery) Order(o ...trailrun.OrderOption) *TrailRunQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryTrail chains the current query on the "trail" edge.
func (_q *TrailRunQuery) QueryTrail() *TrailQuery {
	query := (&TrailClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(trailrun.Table, trailrun.FieldID, selector),
			sqlgraph.To(trail.Table, trail.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, trailrun.TrailTable, trailrun.TrailColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryInitiator chains the current query on the "initiator" edge.
func (_q *TrailRunQuery) QueryInitiator() *AccountQuery {
	query := (&AccountClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(trailrun.Table, trailrun.FieldID, selector),
			sqlgraph.To(account.Table, account.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, trailrun.InitiatorTable, trailrun.InitiatorColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryActionRuns chains the current query on the "action_runs" edge.
func (_q *TrailRunQuery) QueryActionRuns() *TrailActionRunQuery {
	query := (&TrailActionRunClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(trailrun.Table, trailrun.FieldID, selector),
			sqlgraph.To(trailactionrun.Table, trailactionrun.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, trailrun.ActionRunsTable, trailrun.ActionRunsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first TrailRun entity from the query.
// Returns a *NotFoundError when no TrailRun was found.
func (_q *TrailRunQuery) First(ctx context.Context) (*TrailRun, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{trailrun.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *TrailRunQuery) FirstX(ctx context.Context) *TrailRun {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first TrailRun ID from the query.
// Returns a *NotFoundError when no TrailRun ID was found.
func (_q *TrailRunQuery) FirstID(ctx context.Context) (id xid.ID, err error) {
	var ids []xid.ID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{trailrun.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *TrailRunQuery) FirstIDX(ctx context.Context) xid.ID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single TrailRun entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one TrailRun entity is found.
// Returns a *NotFoundError when no TrailRun entities are found.
func (_q *TrailRunQuery) Only(ctx context.Context) (*TrailRun, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{trailrun.Label}
	default:
		return nil, &NotSingularError{trailrun.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *TrailRunQuery) OnlyX(ctx context.Context) *TrailRun {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only TrailRun ID in the query.
// Returns a *NotSingularError when more than one TrailRun ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *TrailRunQuery) OnlyID(ctx context.Context) (id xid.ID, err error) {
	var ids []xid.ID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{trailrun.Label}
	default:
		err = &NotSingularError{trailrun.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *TrailRunQuery) OnlyIDX(ctx context.Context) xid.ID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of TrailRuns.
func (_q *TrailRunQuery) All(ctx context.Context) ([]*TrailRun, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*TrailRun, *TrailRunQuery]()
	return withInterceptors[[]*TrailRun](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *TrailRunQuery) AllX(ctx context.Context) []*TrailRun {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of TrailRun IDs.
func (_q *TrailRunQuery) IDs(ctx context.Context) (ids []xid.ID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(trailrun.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *TrailRunQuery) IDsX(ctx context.Context) []xid.ID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *TrailRunQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*TrailRunQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *TrailRunQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *TrailRunQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *TrailRunQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the TrailRunQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *TrailRunQuery) Clone() *TrailRunQuery {
	if _q == nil {
		return nil
	}
	return &TrailRunQuery{
		config:         _q.config,
		ctx:            _q.ctx.Clone(),
		order:          append([]trailrun.OrderOption{}, _q.order...),
		inters:         append([]Interceptor{}, _q.inters...),
		predicates:     append([]predicate.TrailRun{}, _q.predicates...),
		withTrail:      _q.withTrail.Clone(),
		withInitiator:  _q.withInitiator.Clone(),
		withActionRuns: _q.withActionRuns.Clone(),
		// clone intermediate query.
		sql:       _q.sql.Clone(),
		path:      _q.path,
		modifiers: append([]func(*sql.Selector){}, _q.modifiers...),
	}
}

// WithTrail tells the query-builder to eager-load the nodes that are connected to
// the "trail" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *TrailRunQuery) WithTrail(opts ...func(*TrailQuery)) *TrailRunQuery {
	query := (&TrailClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withTrail = query
	return _q
}

// WithInitiator tells the query-builder to eager-load the nodes that are connected to
// the "initiator" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *TrailRunQuery) WithInitiator(opts ...func(*AccountQuery)) *TrailRunQuery {
	query := (&AccountClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withInitiator = query
	return _q
}

// WithActionRuns tells the query-builder to eager-load the nodes that are connected to
// the "action_runs" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *TrailRunQuery) WithActionRuns(opts ...func(*TrailActionRunQuery)) *TrailRunQuery {
	query := (&TrailActionRunClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withActionRuns = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.TrailRun.Query().
//		GroupBy(trailrun.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *TrailRunQuery) GroupBy(field string, fields ...string) *TrailRunGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &TrailRunGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = trailrun.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.TrailRun.Query().
//		Select(trailrun.FieldCreatedAt).
//		Scan(ctx, &v)
func (_q *TrailRunQuery) Select(fields ...string) *TrailRunSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &TrailRunSelect{TrailRunQuery: _q}
	sbuild.label = trailrun.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a TrailRunSelect configured with the given aggregations.
func (_q *TrailRunQuery) Aggregate(fns ...AggregateFunc) *TrailRunSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *TrailRunQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !trailrun.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *TrailRunQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*TrailRun, error) {
	var (
		nodes       = []*TrailRun{}
		_spec       = _q.querySpec()
		loadedTypes = [3]bool{
			_q.withTrail != nil,
			_q.withInitiator != nil,
			_q.withActionRuns != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*TrailRun).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &TrailRun{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withTrail; query != nil {
		if err := _q.loadTrail(ctx, query, nodes, nil,
			func(n *TrailRun, e *Trail) { n.Edges.Trail = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withInitiator; query != nil {
		if err := _q.loadInitiator(ctx, query, nodes, nil,
			func(n *TrailRun, e *Account) { n.Edges.Initiator = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withActionRuns; query != nil {
		if err := _q.loadActionRuns(ctx, query, nodes,
			func(n *TrailRun) { n.Edges.ActionRuns = []*TrailActionRun{} },
			func(n *TrailRun, e *TrailActionRun) { n.Edges.ActionRuns = append(n.Edges.ActionRuns, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *TrailRunQuery) loadTrail(ctx context.Context, query *TrailQuery, nodes []*TrailRun, init func(*TrailRun), assign func(*TrailRun, *Trail)) error {
	ids := make([]xid.ID, 0, len(nodes))
	nodeids := make(map[xid.ID][]*TrailRun)
	for i := range nodes {
		fk := nodes[i].TrailID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(trail.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "trail_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *TrailRunQuery) loadInitiator(ctx context.Context, query *AccountQuery, nodes []*TrailRun, init func(*TrailRun), assign func(*TrailRun, *Account)) error {
	ids := make([]xid.ID, 0, len(nodes))
	nodeids := make(map[xid.ID][]*TrailRun)
	for i := range nodes {
		if nodes[i].InitiatedByID == nil {
			continue
		}
		fk := *nodes[i].InitiatedByID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(account.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "initiated_by_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *TrailRunQuery) loadActionRuns(ctx context.Context, query *TrailActionRunQuery, nodes []*TrailRun, init func(*TrailRun), assign func(*TrailRun, *TrailActionRun)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[xid.ID]*TrailRun)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(trailactionrun.FieldRunID)
	}
	query.Where(predicate.TrailActionRun(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(trailrun.ActionRunsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.RunID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "run_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *TrailRunQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *TrailRunQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(trailrun.Table, trailrun.Columns, sqlgraph.NewFieldSpec(trailrun.FieldID, field.TypeString))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, trailrun.FieldID)
		for i := range fields {
			if fields[i] != trailrun.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withTrail != nil {
			_spec.Node.AddColumnOnce(trailrun.FieldTrailID)
		}
		if _q.withInitiator != nil {
			_spec.Node.AddColumnOnce(trailrun.FieldInitiatedByID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *TrailRunQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(trailrun.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = trailrun.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range _q.modifiers {
		m(selector)
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (_q *TrailRunQuery) Modify(modifiers ...func(s *sql.Selector)) *TrailRunSelect {
	_q.modifiers = append(_q.modifiers, modifiers...)
	return _q.Select()
}

// TrailRunGroupBy is the group-by builder for TrailRun entities.
type TrailRunGroupBy struct {
	selector
	build *TrailRunQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *TrailRunGroupBy) Aggregate(fns ...AggregateFunc) *TrailRunGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *TrailRunGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TrailRunQuery, *TrailRunGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *TrailRunGroupBy) sqlScan(ctx context.Context, root *TrailRunQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// TrailRunSelect is the builder for selecting fields of TrailRun entities.
type TrailRunSelect struct {
	*TrailRunQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *TrailRunSelect) Aggregate(fns ...AggregateFunc) *TrailRunSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *TrailRunSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TrailRunQuery, *TrailRunSelect](ctx, _s.TrailRunQuery, _s, _s.inters, v)
}

func (_s *TrailRunSelect) sqlScan(ctx context.Context, root *TrailRunQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (_s *TrailRunSelect) Modify(modifiers ...func(s *sql.Selector)) *TrailRunSelect {
	_s.modifiers = append(_s.modifiers, modifiers...)
	return _s
}
