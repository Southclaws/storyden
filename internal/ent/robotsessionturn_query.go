// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/Southclaws/storyden/internal/ent/account"
	"github.com/Southclaws/storyden/internal/ent/predicate"
	"github.com/Southclaws/storyden/internal/ent/robotsession"
	"github.com/Southclaws/storyden/internal/ent/robotsessioninput"
	"github.com/Southclaws/storyden/internal/ent/robotsessionturn"
	"github.com/rs/xid"
)

// RobotSessionTurnQuery is the builder for querying RobotSessionTurn entities.
type RobotSessionTurnQuery struct {
	config
	ctx           *QueryContext
	order         []robotsessionturn.OrderOption
	inters        []Interceptor
	predicates    []predicate.RobotSessionTurn
	withSession   *RobotSessionQuery
	withInitiator *AccountQuery
	withInputs    *RobotSessionInputQuery
	modifiers     []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the RobotSessionTurnQuery builder.
func (_q *RobotSessionTurnQuery) Where(ps ...predicate.RobotSessionTurn) *RobotSessionTurnQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *RobotSessionTurnQuery) Limit(limit int) *RobotSessionTurnQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *RobotSessionTurnQuery) Offset(offset int) *RobotSessionTurnQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *RobotSessionTurnQuery) Unique(unique bool) *RobotSessionTurnQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *RobotSessionTurnQuery) Order(o ...robotsessionturn.OrderOption) *RobotSessionTurnQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QuerySession chains the current query on the "session" edge.
func (_q *RobotSessionTurnQuery) QuerySession() *RobotSessionQuery {
	query := (&RobotSessionClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(robotsessionturn.Table, robotsessionturn.FieldID, selector),
			sqlgraph.To(robotsession.Table, robotsession.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, robotsessionturn.SessionTable, robotsessionturn.SessionColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryInitiator chains the current query on the "initiator" edge.
func (_q *RobotSessionTurnQuery) QueryInitiator() *AccountQuery {
	query := (&AccountClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(robotsessionturn.Table, robotsessionturn.FieldID, selector),
			sqlgraph.To(account.Table, account.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, robotsessionturn.InitiatorTable, robotsessionturn.InitiatorColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryInputs chains the current query on the "inputs" edge.
func (_q *RobotSessionTurnQuery) QueryInputs() *RobotSessionInputQuery {
	query := (&RobotSessionInputClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(robotsessionturn.Table, robotsessionturn.FieldID, selector),
			sqlgraph.To(robotsessioninput.Table, robotsessioninput.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, robotsessionturn.InputsTable, robotsessionturn.InputsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first RobotSessionTurn entity from the query.
// Returns a *NotFoundError when no RobotSessionTurn was found.
func (_q *RobotSessionTurnQuery) First(ctx context.Context) (*RobotSessionTurn, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{robotsessionturn.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *RobotSessionTurnQuery) FirstX(ctx context.Context) *RobotSessionTurn {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first RobotSessionTurn ID from the query.
// Returns a *NotFoundError when no RobotSessionTurn ID was found.
func (_q *RobotSessionTurnQuery) FirstID(ctx context.Context) (id xid.ID, err error) {
	var ids []xid.ID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{robotsessionturn.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *RobotSessionTurnQuery) FirstIDX(ctx context.Context) xid.ID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single RobotSessionTurn entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one RobotSessionTurn entity is found.
// Returns a *NotFoundError when no RobotSessionTurn entities are found.
func (_q *RobotSessionTurnQuery) Only(ctx context.Context) (*RobotSessionTurn, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{robotsessionturn.Label}
	default:
		return nil, &NotSingularError{robotsessionturn.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *RobotSessionTurnQuery) OnlyX(ctx context.Context) *RobotSessionTurn {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only RobotSessionTurn ID in the query.
// Returns a *NotSingularError when more than one RobotSessionTurn ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *RobotSessionTurnQuery) OnlyID(ctx context.Context) (id xid.ID, err error) {
	var ids []xid.ID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{robotsessionturn.Label}
	default:
		err = &NotSingularError{robotsessionturn.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *RobotSessionTurnQuery) OnlyIDX(ctx context.Context) xid.ID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of RobotSessionTurns.
func (_q *RobotSessionTurnQuery) All(ctx context.Context) ([]*RobotSessionTurn, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*RobotSessionTurn, *RobotSessionTurnQuery]()
	return withInterceptors[[]*RobotSessionTurn](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *RobotSessionTurnQuery) AllX(ctx context.Context) []*RobotSessionTurn {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of RobotSessionTurn IDs.
func (_q *RobotSessionTurnQuery) IDs(ctx context.Context) (ids []xid.ID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(robotsessionturn.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *RobotSessionTurnQuery) IDsX(ctx context.Context) []xid.ID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *RobotSessionTurnQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*RobotSessionTurnQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *RobotSessionTurnQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *RobotSessionTurnQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *RobotSessionTurnQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the RobotSessionTurnQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *RobotSessionTurnQuery) Clone() *RobotSessionTurnQuery {
	if _q == nil {
		return nil
	}
	return &RobotSessionTurnQuery{
		config:        _q.config,
		ctx:           _q.ctx.Clone(),
		order:         append([]robotsessionturn.OrderOption{}, _q.order...),
		inters:        append([]Interceptor{}, _q.inters...),
		predicates:    append([]predicate.RobotSessionTurn{}, _q.predicates...),
		withSession:   _q.withSession.Clone(),
		withInitiator: _q.withInitiator.Clone(),
		withInputs:    _q.withInputs.Clone(),
		// clone intermediate query.
		sql:       _q.sql.Clone(),
		path:      _q.path,
		modifiers: append([]func(*sql.Selector){}, _q.modifiers...),
	}
}

// WithSession tells the query-builder to eager-load the nodes that are connected to
// the "session" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *RobotSessionTurnQuery) WithSession(opts ...func(*RobotSessionQuery)) *RobotSessionTurnQuery {
	query := (&RobotSessionClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withSession = query
	return _q
}

// WithInitiator tells the query-builder to eager-load the nodes that are connected to
// the "initiator" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *RobotSessionTurnQuery) WithInitiator(opts ...func(*AccountQuery)) *RobotSessionTurnQuery {
	query := (&AccountClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withInitiator = query
	return _q
}

// WithInputs tells the query-builder to eager-load the nodes that are connected to
// the "inputs" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *RobotSessionTurnQuery) WithInputs(opts ...func(*RobotSessionInputQuery)) *RobotSessionTurnQuery {
	query := (&RobotSessionInputClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withInputs = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.RobotSessionTurn.Query().
//		GroupBy(robotsessionturn.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *RobotSessionTurnQuery) GroupBy(field string, fields ...string) *RobotSessionTurnGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &RobotSessionTurnGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = robotsessionturn.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.RobotSessionTurn.Query().
//		Select(robotsessionturn.FieldCreatedAt).
//		Scan(ctx, &v)
func (_q *RobotSessionTurnQuery) Select(fields ...string) *RobotSessionTurnSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &RobotSessionTurnSelect{RobotSessionTurnQuery: _q}
	sbuild.label = robotsessionturn.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a RobotSessionTurnSelect configured with the given aggregations.
func (_q *RobotSessionTurnQuery) Aggregate(fns ...AggregateFunc) *RobotSessionTurnSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *RobotSessionTurnQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !robotsessionturn.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *RobotSessionTurnQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*RobotSessionTurn, error) {
	var (
		nodes       = []*RobotSessionTurn{}
		_spec       = _q.querySpec()
		loadedTypes = [3]bool{
			_q.withSession != nil,
			_q.withInitiator != nil,
			_q.withInputs != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*RobotSessionTurn).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &RobotSessionTurn{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withSession; query != nil {
		if err := _q.loadSession(ctx, query, nodes, nil,
			func(n *RobotSessionTurn, e *RobotSession) { n.Edges.Session = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withInitiator; query != nil {
		if err := _q.loadInitiator(ctx, query, nodes, nil,
			func(n *RobotSessionTurn, e *Account) { n.Edges.Initiator = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withInputs; query != nil {
		if err := _q.loadInputs(ctx, query, nodes,
			func(n *RobotSessionTurn) { n.Edges.Inputs = []*RobotSessionInput{} },
			func(n *RobotSessionTurn, e *RobotSessionInput) { n.Edges.Inputs = append(n.Edges.Inputs, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *RobotSessionTurnQuery) loadSession(ctx context.Context, query *RobotSessionQuery, nodes []*RobotSessionTurn, init func(*RobotSessionTurn), assign func(*RobotSessionTurn, *RobotSession)) error {
	ids := make([]xid.ID, 0, len(nodes))
	nodeids := make(map[xid.ID][]*RobotSessionTurn)
	for i := range nodes {
		fk := nodes[i].SessionID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(robotsession.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "session_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *RobotSessionTurnQuery) loadInitiator(ctx context.Context, query *AccountQuery, nodes []*RobotSessionTurn, init func(*RobotSessionTurn), assign func(*RobotSessionTurn, *Account)) error {
	ids := make([]xid.ID, 0, len(nodes))
	nodeids := make(map[xid.ID][]*RobotSessionTurn)
	for i := range nodes {
		if nodes[i].InitiatedByAccountID == nil {
			continue
		}
		fk := *nodes[i].InitiatedByAccountID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(account.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "initiated_by_account_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *RobotSessionTurnQuery) loadInputs(ctx context.Context, query *RobotSessionInputQuery, nodes []*RobotSessionTurn, init func(*RobotSessionTurn), assign func(*RobotSessionTurn, *RobotSessionInput)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[xid.ID]*RobotSessionTurn)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(robotsessioninput.FieldTurnID)
	}
	query.Where(predicate.RobotSessionInput(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(robotsessionturn.InputsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.TurnID
		if fk == nil {
			return fmt.Errorf(`foreign-key "turn_id" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "turn_id" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *RobotSessionTurnQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *RobotSessionTurnQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(robotsessionturn.Table, robotsessionturn.Columns, sqlgraph.NewFieldSpec(robotsessionturn.FieldID, field.TypeString))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, robotsessionturn.FieldID)
		for i := range fields {
			if fields[i] != robotsessionturn.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withSession != nil {
			_spec.Node.AddColumnOnce(robotsessionturn.FieldSessionID)
		}
		if _q.withInitiator != nil {
			_spec.Node.AddColumnOnce(robotsessionturn.FieldInitiatedByAccountID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *RobotSessionTurnQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(robotsessionturn.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = robotsessionturn.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range _q.modifiers {
		m(selector)
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (_q *RobotSessionTurnQuery) Modify(modifiers ...func(s *sql.Selector)) *RobotSessionTurnSelect {
	_q.modifiers = append(_q.modifiers, modifiers...)
	return _q.Select()
}

// RobotSessionTurnGroupBy is the group-by builder for RobotSessionTurn entities.
type RobotSessionTurnGroupBy struct {
	selector
	build *RobotSessionTurnQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *RobotSessionTurnGroupBy) Aggregate(fns ...AggregateFunc) *RobotSessionTurnGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *RobotSessionTurnGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RobotSessionTurnQuery, *RobotSessionTurnGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *RobotSessionTurnGroupBy) sqlScan(ctx context.Context, root *RobotSessionTurnQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// RobotSessionTurnSelect is the builder for selecting fields of RobotSessionTurn entities.
type RobotSessionTurnSelect struct {
	*RobotSessionTurnQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *RobotSessionTurnSelect) Aggregate(fns ...AggregateFunc) *RobotSessionTurnSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *RobotSessionTurnSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RobotSessionTurnQuery, *RobotSessionTurnSelect](ctx, _s.RobotSessionTurnQuery, _s, _s.inters, v)
}

func (_s *RobotSessionTurnSelect) sqlScan(ctx context.Context, root *RobotSessionTurnQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (_s *RobotSessionTurnSelect) Modify(modifiers ...func(s *sql.Selector)) *RobotSessionTurnSelect {
	_s.modifiers = append(_s.modifiers, modifiers...)
	return _s
}
