steps:
  - match:
      tool_result: library_page_list
    respond:
      text: "I found the pages in the library."
      finish: "stop"
  - match:
      any: true
    respond:
      tool_calls:
        - id: call_list_1
          name: library_page_list
          args: {}
