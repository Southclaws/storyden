$schema: http://json-schema.org/draft-07/schema#
type: object
description: A user decision or missing input that blocked a Robot action.
required: [reason, message]
additionalProperties: false
properties:
  reason:
    type: string
    description: Short category for the required attention.
  message:
    type: string
    description: Explanation of what the Robot needs from the user.
