$schema: "http://json-schema.org/draft-07/schema#"
$defs:
  DatagraphRef:
    type: object
    required: ["kind", "id"]
    properties:
      kind:
        type: string
        description: "Resource kind (e.g., 'post', 'node', 'account')"
      id:
        type: string
        format: xid
        description: "Resource ID"
